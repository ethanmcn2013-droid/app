# Project Drive takeover · 4 September 2026

This task resumes the stopped `drive` task under the approved January programme.
The launch and first venue outreach target is **21 January 2027**. Work before
launch uses internal fixtures and designated test accounts; no public release,
real recipient email, or production migration is part of this takeover.

## Observed starting identity

- Repository: `ethanmcn2013-droid/app`.
- Worktree: `C:/Users/ethan/signal-studio-workspace/worktrees/app/feat-project-drive`.
- Branch: `feat/project-drive`.
- Starting HEAD: `b71f7e2c1ba45add239523cbdae2b32d3eff70d0`.
- Published HEAD: `c3a1b219eff2052564bd0102bab071381ecee915`.
- Thirteen local commits were unpublished. They are preserved, not recreated.
- Eight tracked working-tree entries and two untracked report files were inherited
  from the stopped task. Their documentation is checkpointed with this record;
  the workflow entries had no substantive diff after line-ending normalization.
- App main: `a709e22a9f19ffb4c45b1c420815f2edf6bfbf30`.
- Existing draft: https://github.com/ethanmcn2013-droid/app/pull/165.

## Evidence boundary

The published Linux candidate fails the workspace-deletion source contract in
`src/server/share-revocation-contract.test.mjs`. That is distinct from the prior
Windows native-test teardown failures. The current local candidate needs its own
complete check receipts. Prior task output and STATUS entries remain dated leads.

Production drift reports migrations 0028 and 0029 pending. This is an open
production readiness gate, not permission to apply them or to remove that check.
The isolated January integration environment must prove its own migration target.

## Decisions and ownership

The user approved the January programme and delegated ordinary design and scope
decisions. A Custodian is the proposed Drive thesis; the real implementation and
material states must be reviewed before recording a delegated lock. Do not call
that a specific founder selection. Historical founder checkpoints in older
documents are superseded only to this extent. Production remains held.

The principal integrator owns this worktree, Drive services, release workflows and
integration. Collaboration and commercial work use separate task worktrees.
Unrelated workspace and motion work is outside this takeover and stays untouched.

Canonical programme ownership is Studio's
`docs/execution/january-2027/PROGRAMME.md`; feature truth remains this project's
PROJECT, DECISIONS and STATUS, linked from the programme rather than duplicated.

## Next acceptance edges

1. Checkpoint inherited changes and publish the preserved candidate with automatic
   Git deployments disabled.
2. Run Linux code and database-contract gates at the published candidate SHA.
3. Complete Connections, live access and Resources UI against the existing backend.
4. Prove the in-product lifecycle with isolated data and designated test accounts.
5. Record final security, interaction, migration-rehearsal and recovery receipts.

No work package is accepted or launched by this checkpoint.
