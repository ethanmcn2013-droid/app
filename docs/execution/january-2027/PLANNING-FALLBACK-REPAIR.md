# Planning catalog error boundary

Fresh independent review of candidate0cb13716 confirmed the three original module fixes on their tested paths, but found JAN-CAND-01: the catalog's catch-all error path authorized archived or invalid-period projects through its older membership-only fallback. The real Briefing then read the requested archived project's task. This also reproduced on base77d; it is a pre-existing policy bypass within the bounded catalog repair, not a new cross-tenant disclosure. The preserved independent report is `outputs/january-module-candidate-review/review.md` in the programme task.

The principal traced the fallback and its actual callers, then restricted compatibility to positive schema evidence. Migration0013 introduced planning links and project archives together. The fallback is now permitted only when the physical workspace table exists, both planning objects are absent and none of0013's eight workspace columns exists. A partial schema, view replacing a planning table, unavailable metadata or operational query failure authorizes no scope. Offline mock-source behavior remains unchanged. No database migration, provider call, archived-project policy or public interface changed.

The targeted tests execute the real catalog, legacy membership source and Briefing against disposable SQL stores. A real renamed planning column and a separately injected catalog/metadata SQLITE_BUSY both refuse authorization while the actual legacy lookup still succeeds. Healthy grouped/loose owner/member, removed/foreign scope, archive exclusions and200-row bound continue to pass. The original owning0000 schema proves supported pre-planning membership still works; a planning view or partial archive-column adoption refuses. It is historical compatibility evidence, not a supported production bootstrap shortcut.

The first test run passed the security cases but failed a legacy fixture's incorrect relative SQL path before setup; its failed receipt remains preserved. After correcting that fixture path, all10 focused tests and the full TypeScript check pass on Node22.23.2. Relevant lint and module-boundary checks accompany this commit. Full receiving/default/build/browser verification still belongs to final App integration; this targeted result alone is not a release verdict.

Commands: `node --import tsx --import ./src/test/register-server-only.mjs --test --test-concurrency=1 src/modules/signal/lib/planning-periods/scope.test.ts src/modules/signal/server/briefing/signal-build-for-user.test.ts src/modules/signal/signal-suite-context.test.ts`; `node node_modules/typescript/bin/tsc --noEmit --incremental false`; scoped ESLint; `node scripts/check-module-boundaries.mjs`; `node scripts/check-ambient-workspace-ratchet.mjs`. The catalog regression file is already mandatory through `test:module-lifecycle`.

The principal owns this follow-up to the one independent candidate-review cycle. The separate legacy/null-lease Timeline writer race and full provider/erasure lifecycle acceptance remain open.
