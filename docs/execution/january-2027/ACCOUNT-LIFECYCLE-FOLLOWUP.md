# Account lifecycle verification follow-up

5 September2026. Internal integration branch feat/january-activation-integration. Reviewed calendar runtime f809cfa8 is integrated at92839119; the following changes close its test-reliability finding and a separate export error-hygiene observation. No provider state, migration, customer account or production environment was changed.

The independent calendar review proved retained credential custody, revocation-before-delete ordering, owner isolation, races during provider wait and actual account-route refusal. Its unmodified46-boundary workload is not claimed: the actual earlier42-test command returned30passes/12Windows EPERM cleanup failures. Those failures remain evidence; they were not product acceptance. The new fixture deliberately retains its synthetic OS-temp databases on Windows after closing clients, so native handles cannot make a finally-block cleanup exception replace an assertion. Linux continues to remove the fixture. This does not alter production erasure or swallow filesystem errors. The unrelated preserved invite-arrival fixture is untouched.

The same review demonstrated raw module exception text in account export. The unified exporter now gives each unavailable module a stable retry reason, including when it returns unavailable diagnostic metadata rather than throwing. Available account sections remain unchanged. The actual export HTTP boundary also gives a stable500message and private/no-store headers when host export fails. This is defensive error hygiene; the earlier synthetic error marker did not establish a live credential leak.

Forty-six focused tests pass in one serial command, with no skips: calendar erasure, unified erasure/export, account erasure and four new actual HTTP-boundary cases. The HTTP tests execute the real unified exporter against disposable Tasks SQLite and the real route, with explicit session/account-wiring seams. Error, string and object rejections, unavailable metadata, available-section preservation,401withoutstorage and500withoutdiagnosticdata are covered. Typecheck and focused lint pass; twelve existing unused-argument warnings remain in the old export test. New files have no lint warnings. The test cases are registered in the existing default and Drive gates.

Reproduce with the pinned Node/dependencies:

    node --import tsx --import ./src/test/register-server-only.mjs --test --test-concurrency=1 src/modules/notes/server/notes-calendar-erasure.test.ts src/server/account-unified-erasure.test.ts src/server/account-unified-export.test.ts src/server/account-erasure.test.ts src/app/api/account/export/route.test.ts

Fresh independent verification and full combined Linux/build gates remain required. The older detached-token adapter seam still cannot restore custody lost by an old eraser; current production wiring uses the repaired Notes eraser. Successful projection and sanitized failures do not certify every possible diagnostic elsewhere in the application. No current Notes calendar writer/poller or live populated credential table was observed. Preserve S5's independent account-deletion fence/usage-erasure queue when integrating that candidate.
