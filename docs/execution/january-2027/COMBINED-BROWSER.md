# Combined App browser checkpoint

## Current follow-up — 5 September

Runtime candidate182935f76b88398fae073f5f0f4e8ebbfb7353d2 built and passed the complete132-case matrix across all four declared widths in9.3 minutes. Canonical run: `tasks-playwright-776b131ccc1d285f9c76fe35`. Exact raw artifact SHA-256: `5b99dd431aff04392b1618420e2a4561e7b51a03f4f1209a37b26c4c4b7b78cb`. The deterministic server was bound to127.0.0.1 on port4450. Provider/data modes remain synthetic demo/preview.

Home source coverage is refreshed through its actual critical fixture. The23 prior active materiality receipts were preserved byte-for-byte under `experience/reviews/history/pre-january-followup-182935f7` before renewal against this actual run. Each named surface retains its unchanged source hash and previous partial-state limits. All32 earlier historical receipts remain untouched. Attestation verification,83-experience registry validation and37 critical fixture mappings pass. Neither the132 cases nor the23 scoped receipts establish all458 required state variants or a final council verdict.

The separately integrated Notes fixtures pass27 browser scenarios plus their actual-source server/page checks and six recovery units. These demonstrate private draft recovery and cold navigation consumers with explicit framework/identity adapters; they do not certify real Clerk, full Next account switching, native browser zoom or human comprehension. The fixture runners' generated bundles now live under the existing ignored evidence output directory. All five runners pass after that path correction, followed by the normal full lint: zero errors and71 inherited warnings. Source lint rules remain intact.

Performance from the same build: shared runtime246.2 KB gzip versus247 KB ceiling and170 KB target; total client929.7 KB versus940 KB ceiling; largest chunk62.5 KB versus63 KB. Ceilings pass, shared-runtime target remains unmet and production user performance remains unmeasured. Linux Verify33934085512 passed the integrated runtime. CI33934085471's generated-bundle lint failure is retained and requires a fresh successful run after the output-path correction. Receiving integration remains gated.

## Historical baseline — 4 September

Runtime candidate ca95830e, built locally with the repository's deterministic demo/preview Playwright contract on port 4350. The actual declared 132-case matrix passed across mobile, tablet, desktop and wide. Canonical run: tasks-playwright-eaebc5221a2ebebcaa062ab8. Raw artifact SHA-256: 3dd3e7388ce4997bedc26b4e68b1610d9c584d6be28389dc06dd3df308978a6d.

The new expired/unconfirmed redemption copy is visible in final desktop/mobile captures. Receipt verification and the experience registry pass. Thirty-two previous materiality receipts are preserved byte-for-byte under experience/reviews/history/pre-january-fixture-contract; 23 active scoped reviews cite this new run. Current UI/component/source tests remain separate evidence for states the generic browser cases do not exercise. No old verdict/date was rewritten and no gate was altered.

Performance on the same build: shared runtime 246.1 KB gzip against 247 KB ceiling (170 KB target remains unmet); total client 924.8 KB against 940 KB ceiling; largest chunk 62.5 KB against 63 KB. Production user performance remains unmeasured. The registry reports 83 experiences and 458 required state variants; passing these 132 cases is not full experience or golden-story closure.

Commands: pnpm exec playwright test --config experience/playwright.config.ts; node scripts/experience/attest-playwright.mjs --write-record; node scripts/experience/critical-fixtures.mjs --write; exact per-surface review-materiality calls; node scripts/experience/attest-playwright.mjs --verify-receipts; node scripts/experience/validate.mjs; node scripts/check-performance-budgets.mjs.

No Google, real database, Stripe, email or production release operation was performed. Earlier failed Linux visual runs are retained in GitHub, including 33924047875's stale-receipt rejection. Receiving Linux engineering, locale/switcher and design gates must still pass the exact committed integration candidate.

## Integration gate corrections

The 5099b981 Linux run exposed two check failures after the caller handoff: its portable evidence runners used CommonJS imports against the repo's ESM lint policy, and the deletion contract still expected the former action-local fence. The executable runners now use ESM; original bytes remain as .cjs.txt evidence beside the manifest. The rebuilt synthetic fixture and full lint pass (71 existing warnings, zero errors). The contract now verifies the actual completion-service fence and additionally forbids action-local database writes while requiring each action's delegation. All 33 hard-rule checks pass. Existing real SQLite deletion-fence/rollback tests remain in the default suite. No production source or visual fixture changed for these two corrections.
