{
  "id": "axis-max-and-live-week-print-the-same-number",
  "real": true,
  "severity": "defect",
  "reason": "Every load-bearing number in this finding reproduces to the tenth of a pixel, and the fix survives both gates. CODE: lately.html:762 max = Math.max(D.peak, D.bestPrior); D.peak (line 670) is taken over D.weeks, which includes the live week, so on the shipped fixture (fixture.js weeks end 13 Jul, v:12, partial:true; bestPrior 8) max === 12 === the live week's printed value, and the g=1 gridline at lately.html:785-788 prints Math.round(12*1) = 12 fifteen pixels from the bar-val that prints '12 SO FAR'. DRIVEN (chromium, 1440x960, ?v=light|dark&state=full|partial|quiet): the top .gridline b box is [1245.8,289.9,1265.0,304.7], the .bar-val box is [1162.7,283.0,1231.0,297.9] -> vertical overlap 8.0px, horizontal gap 14.8px, bar-val top 6.8px higher (seat said 6.9; subpixel rounding). Swept 360-1600 in 20px steps, 63 widths, zero misses: gap 14.8px and overlap 8.0px at EVERY width, because both labels are anchored to fixed right rails, so the pairing is width-invariant rather than a single-width accident. TYPE: both are t-label, computed identically as Geist Mono 11px / weight 400 / uppercase / 0.99px tracking - the two numerals are set in exactly the same voice, so nothing but hue separates them. RAILS: the top gridline rule sits at y=302.9-303.9; the axis label's box (289.9-304.7) straddles it while the bar-val (bottom 297.9) floats 5px clear - the seat's 'not on a shared baseline' is right. DARK: computed colours are Indigo 400 #818cf8 on the bar-val against #a1a1aa on the gridline (the seat named Ink 3; it is actually Ink 4, which only strengthens the point) - relative luminance 0.303 vs 0.360, a 1.16:1 separation, i.e. the hue cue that carries the light frame is effectively gone. The rendered 3x crop confirms the perceptual claim without argument: the top-right corner reads as one 11px tracked mono line, '12 SO FAR   12'. NOT A REGRESSION OF r1: bar-val-overprints-axis-max was box intersection; the 34px gutter that closed it measures 34.0px here (last .colw right 1231.0, .plot right 1265.0) and the boxes do not intersect. This is a distinct defect - the surface prints one numeral twice, in one voice, meaning two different things, in the corner the whole card is built to deliver. NOT GATE-RESTATEMENT: it contradicts interaction-check.mjs:258 rather than restating it. CLASS: 'defect' is correct and not overstated - nothing untrue is asserted (the ceiling IS 12 and the week IS 12), so it is not misleading; but it is not a refinement either, because there is a measurable typographic collision on a shipped fixture at every width on both grounds. FIX VERIFIED: I applied the seat's exact patch to a scratch copy of the lab and ran both gates. audit.mjs: fully green, all fifteen counters zero across 390/768/1280/1440. interaction-check.mjs: 461 assertions, exactly ONE failing - 'the chart states its own scale' {n:2, top:false} - which is precisely the breakage the seat declared and no other. The re-rendered crop is clean: the top rule runs uninterrupted to the right rail and one number stands in the corner. Nothing in the CSS or JS depends on .gridline b existing (.gridline draws its own border-top; the only JS touching the plot's labels is line 1231, which touches .bar-val).",
  "sharpenedFix": "Take the seat's chart() patch verbatim - it is minimal, data-conditional, and both labels return unchanged on any fixture where the live week is not the peak. Sharpen only the gate amendment, which the seat left as prose and which does not work as literally described. Two problems: (a) interaction-check.mjs:256-258 currently does Number(l.textContent) over .gridline elements; after the fix the top gridline's textContent is '' and Number('') === 0, and the .bar-val's is '12 so far', whose Number() is NaN - so the amendment must parse with a numeric match (e.g. parseInt(el.textContent, 10)), not Number(). (b) 'accept either a .gridline b or the .bar-val' makes the assertion near-vacuous on this fixture, because .bar-val always prints the live week's value and the live week is the max, so the check would pass by construction. Amend it to keep a real failing mode - assert both halves: (1) the maximum is printed somewhere in the plot at the top of the scale, by the top .gridline b OR the .bar-val (parsed with parseInt); and (2) the invariant the fix introduces - the top gridline carries a numeral if and only if that numeral differs from the number the .bar-val prints. Part (2) fails if the suppression ever fires on a fixture where the live week is not the peak, and fails if it stops firing when it should, which is the regression the change actually creates."
  ,
  "breaks": "Breaks exactly one thing, measured rather than predicted: interaction-check.mjs:258 'the chart states its own scale' goes red (461 assertions, 1 failing, {n:2,top:false}); patch it in the same commit per the sharpened amendment. Nothing else moves - audit.mjs stays fully green (colours, weights, families, WCAG contrast against the real composited backdrop, targets, radii, motion, ramp, leading, leading role and ladder, tracking and ladder, size and space ladders, all zero at 390/768/1280/1440). RE-MEASURE AFTER IT LANDS: (1) the top gridline rule now runs unbroken to the right rail, because the removed <b> carried background: var(--paper)/Ground 2 which was knocking a gap in it - confirm the rule's full-width run reads as intended on both grounds and does not now collide with the .bar-val's descender at the 14.8px rail (it does not at 1440; sweep the widths again); (2) the plot's only remaining axis numeral is the 50% label '6' - confirm a lone mid-axis label still reads as a scale rather than as a stray figure, at 390 in particular; (3) re-shoot light/dark full, partial and quiet, since all three states share this fixture and all three show the duplicate today; (4) confirm the suppression does NOT fire where it must not - chart() is called only from movementOne (lately.html:826); the first-run solo plot at line 1002 builds its own markup and is untouched, and the empty/loading branches return before the grid builder, so no other state can regress, but re-run the full seven-state gate to hold that. No risk to the record tag, the 34px gutter, the hatched partial column or the sr sentence - none of them reference the gridline label."
}
