{
  "id": "dead-rules-and-stale-comments",
  "real": false,
  "reason": "Driven in chromium across all seven states at 1440 and read against the source. Five of the six sub-claims are factually right. (1) document.querySelectorAll('.bar.live').length is 0 in all seven states, and the rule is unreachable by design rather than by fixture: the last column is the current week, and the protected partial-bucket contract makes the current week always partial, so the live branch of the ternary at line 559 can never be taken. (2) No .sk-row exists in any state; line 440 is dead. The finding caught the rule and missed that the block comment at line 365 also describes an '.sk-in block' that does not exist either (0 matches in every state) - the same class of staleness, one line above the rewrite it survived. (3) .meter i.zero really is emitted (D.waiting is 0, so it appears in full, partial, first-run and loading) but the same branch sets --f:0; computed transform is matrix(0, 0, 0, 1, 0, 0) at width 0, so var(--mark-ctx) cannot paint. (5) The 560px .t-hero override restates the base: computed font-size is 64px at 390, 561 and 1440. (6) The first-run hero numeral has no id='count'. The finding is refuted on its fix and on one of its stated facts. FACT: the count-up does not 'run in six states of seven'. It runs in three - full, partial, quiet. Loading is excluded deliberately at line 867, and empty and error render no hero numeral at all, so the real gap is one state out of the four that have one. FIX, item 6, is the decisive error: adding id='count' to the first-run hero makes it count 12 up from Math.max(0, 12 - 12) = 0, so the one state whose entire message is 'you have started' would open on a literal 0, and for 700ms the numeral would disagree with the '12 so far' bar label sitting directly beneath it inside the same card - exactly the self-contradiction the file's own header comment at line 459 exists to prevent. The from = target - 12 heuristic is tuned for the 31 of the full-state hero and degenerates on a low-teens figure. FIX, item 4, is half-aimed: the CSS comment 'Three gridlines, not a grid' is defensible as written, because the plot does rule three horizontal lines - two .gridline plus .baseline - and only the value-bearing ones carry the class; it is the JS comment 'Three gridlines with their values' that is wrong, since gridline b count is 2. Offering 'render a third at 0.25' as an equal alternative puts a design change on the table to satisfy a comment, and adding scaffolding to a chart whose own comment says more than three lines compete with the marks would make the work worse. FIX, items 1 and 3, delete only the CSS and leave the dead 'live ? bar live : bar' branch and the purposeless n === 0 ? 'zero' : '' emission in the JS - which reproduces the exact 'reads as a live decision' problem the finding is about, one layer down. Severity 'refinement' is correctly classed: nothing on screen is wrong in any state.",
  "severity": "refinement",
  "sharpenedFix": "Source hygiene only; touch no rendered pixel. (a) Delete .bar.live at line 235 AND collapse the ternary at line 559 to w.partial ? 'bar part' : 'bar', keeping the separate `live` variable, which is still load-bearing for the 'N so far' bar-val tag. (b) Delete the .sk-row rule at line 440 and fix the stale '.sk-in block' sentence in the comment at line 365, which describes a mechanism the skeleton rewrite removed. (c) Delete .meter i.zero at line 283 AND the n === 0 ? 'zero' : '' emission at line 534; a zero card is already honestly expressed by an empty track beside its 0 numeral, and is correctly distinct from the unavailable card's 'Not available'. (d) Leave the CSS comment at line 247 alone, or make it 'Two ruled values and the baseline' if the ambiguity bothers; correct only the JS comment at line 572 to 'Two gridlines with their values, above the baseline'. Do not add a third gridline. (e) Delete the .t-hero line inside the 560px block at line 446; if the intent was to document that the numeral keeps its scale on a phone, say that in the existing 900px block comment instead of as a no-op declaration. (f) Do NOT add id='count' to the first-run hero. If that state should have an entrance, it needs a different one - the figure is small, it is duplicated verbatim by the adjacent bar label, and the shared count-from heuristic would open it on 0. Either leave first-run without a count-up and delete nothing, or make the count-from proportional (from = Math.max(0, target - Math.max(4, Math.round(target * 0.4)))) and suppress the bar-val tag while the hero is counting - which is a build, not a comment edit, and belongs in its own finding.",
  "breaks": "Items (a)-(e) are provably inert: every deleted rule was measured to produce no paint, and no selector is shared. The one thing that can break is (a) and (c) if the JS is edited carelessly - `live` must survive in chart() or the 'N so far' tag disappears from the last column in full, partial, quiet and loading, and removing the zero class must not disturb the --f or --i custom properties the fill animation and its 60ms stagger read. After it lands, re-run audit.mjs (palette lock and the type ramp, since a t-hero declaration is being removed) and interaction-check.mjs (grid.n >= 2 still holds at 2, and the skeleton geometry assertions still hold), then re-shoot light and dark full at 390 and 1440 and diff against the current frames - the expected diff is zero pixels. If any pixel moves, one of these rules was not dead and the deletion is wrong."
}
