{
  "id": "undo-band-never-remeasured-on-resize",
  "real": true,
  "reason": "REAL, and the seat's figures reproduce to the pixel by driving live resizes in both rooms. Code: reserveUndo() (render-b.js:1176) is reached only from paintUndo() (:647) and closeEditor() (:1204); settle() (:2225-2233) runs place(), reserve() and markFolds() and never reserveUndo() - so --b-undo survives a resize unrecomputed. The refutation angles all fail. (1) The value IS consumed: b.css:1440 sets .b-field[data-undo=\"true\"] { padding-bottom: max(var(--b-sheet,0px), var(--b-undo,0px), var(--gap-block)) } inside @media (max-width:1023px), the same block that makes .b-undoHome .b-undo position:fixed - i.e. it is read at exactly the width where the bar is fixed and needs the reserve. Not a stale-but-unread variable. (2) Painted consequence, measured at MAXIMUM scroll (scrollTop === scrollHeight - clientHeight, no scroll left to free anything), .b-undo background-color:var(--back), opaque, z-index 4: (a) rotation 844x390 -> 390x844 with the bar filled: --b-undo holds 78px, measured while the bar was one line 54px tall; in portrait the bar is 84.5px, padding-bottom stays 78px, and the bar's rect (747.5-832) overlaps .b-foot (710.4-766.1) by 18.6px - document.elementFromPoint at the centre of \"Signal Timeline\" returns .b-undo. (b) 1440x960 -> 390x844: at 1440 the bar is static, so reserveUndo removes the property and --b-undo is never written; after narrowing, padding-bottom falls back to max(0px,0px,28px) = 28px and the bar (747.5-832) covers .b-foot (760.4-816.1) entirely - elementFromPoint at the centre of all three strings, \"Kept by Orla\", \"Updated 16 July 2026\" and \"Signal Timeline\", returns .b-undo / .b-undoAct. Screenshot confirms the footer is simply absent from the painted page. Identical numbers in ?v=ink, so it is not a one-room defect. (3) A FRESH load at 390x844, same move-then-Escape, writes 109px and clears the foot by 12.4px - so the two routes to the same screen disagree by 81px of reserve, which is the seat's claim exactly. (4) Realism: (a) is a phone rotating from landscape to portrait with an undo pending - the flagship device doing the one thing phones do; it needs no devtools. (5) The gate genuinely cannot see it: interaction-check.mjs contains ZERO occurrences of setViewportSize across all 787 assertions - every viewport case constructs a new page via open({viewport}), so nothing there ever resizes a live one. The renderer's own settle() comment claims the reserve is recomputed on every settle; that is true of the editor sheet and false of the bar, and the bar is the surface the reversibility model rests on.",
  "sharpenedFix": "In render-b.js settle() (lines 2225-2233), add reserveUndo(document.querySelector(\".b-field\")); immediately after markFolds(document.querySelector(\".b-field\"));. reserveUndo already returns early - and removeProperty(\"--b-undo\") - when the bar is missing, data-empty=\"true\", or not position:fixed, so it is a no-op at 1024+ and in every empty state; render-core.js already calls settle() from the debounced resize (line 161) and from fonts.ready (line 152). Verified sufficient: the value it would write at 390x844 is barH 84.5 -> offsetHeight 85 + 24 = 109px, which is exactly what a fresh 390 load produces and which clears .b-foot by 12.4px. Guard it in interaction-check.mjs with the gate's first LIVE resize, for both rooms: open owner-flight at 844x390, click the first .b-grab, click .b-step[aria-label=\"Move a week later\"], press Escape, page.setViewportSize({width:390,height:844}), wait a frame past the debounce, scroll to scrollHeight, and assert .b-undo's rect does not intersect .b-foot's rect AND that document.elementFromPoint at each .b-foot leaf's centre is not inside .b-undo; repeat the same sequence from 1440x960. Since the gate has no live-resize case at all, consider also asserting that --b-undo after a resize equals the value a fresh page at that width computes."
}
