# {name} · direction locked · {Direction name}

**Locked by the founder, {date}.** Branch `design/{name}-exploration`.
Still exploration: no PR, no merge, no deploy until product lock-in.

## What was chosen

**Direction {X} · {Name}**, from those presented in `DIRECTIONS.md`.
State the architecture in bullets — this is what every future round treats
as settled and every seat is told not to reopen.

## What was rejected with it

- **{A} · {Name}.** Not chosen. Frames and arguments remain in the lab;
  nothing is deleted, and elements may still be folded into the master if
  they earn it.

## What the founder called out as not yet good enough

Recorded VERBATIM in intent, because the next round is judged against it:

1. "{the objection, in the founder's words}"
2. …

## Decisions this lock supersedes

| Standing decision | Status after the lock |
|---|---|
| {decision} | Superseded / Narrowed / Stands — with the argument |

## The mechanical lock

The palette, families, weights, and ladders now live in
`elevate.config.json` and are enforced by the measured gate from this
moment. Fill `ladders.typeRamp` here, not later.

## What happens next

Rooms on the lock, gates green, artifacts live, then the loop.
