{
  "id": "the-approach-vanishes-while-you-place-a-moment",
  "real": true,
  "reason": "Reproduced exactly by driving state=timeline.owner-flight&layout=across: pressing Add a moment inserts .b-edit at y=497, height 554, into the .b-editHost between the horizon band and the plan, and every .b-grab lands at y>=1157 - 0 of 8 moments in view at 1280x900, 1440x960 and 1920x1000. Two +7 presses walk the new dot x=196 to 230 to 329 while the only scroller (.app.sheet, scrollHeight 1680/1776 vs clientHeight 924/864) stays at scrollTop 0, because keepInBand at src/timeline.js:1536 returns early unless the panel is position:fixed or .b-plan scrolls, and across desk .b-plan computes overflow-y:visible. The same flow at layout=down keeps 3 of 8 in view (there .b-plan is overflow-y:auto), so the protection its own comment at src/timeline.js:2431-2438 describes is given up in the layout whose premise (src/across.css:308-310, 'the whole approach is in frame') is false the instant anyone edits.",
  "sharpenedFix": "Do the two halves in the stated order and no more. (1) Relocate the host: the across desk editor must not sit between the horizon band and the track, because scrolling the sheet to follow the dot would then carry the steppers out from under the hand - the exact failure keepInBand's own comment forbids. Render the second host after the plan and select it only when data-layout=across and data-medium!=phone, mirroring src/timeline.css:1187's :empty { display: none }. (2) Only then teach keepInBand a third band: the real scroller here is the .app.sheet element (overflow-y:auto), not .b-plan or the window, so add a branch that takes the nearest scrollable ancestor when neither `fixed` nor `paneScrolls` holds, and apply the existing minimum-correction arithmetic to it unchanged. Do not re-centre and do not touch the down layout, which already passes."
}
