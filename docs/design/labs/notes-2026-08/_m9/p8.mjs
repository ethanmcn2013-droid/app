import { chromium } from "@playwright/test";
const BASE = "file:///C:/Users/ethan/signal-studio-workspace/_wt-design-notes/docs/design/labs/notes-2026-08/notebook.html";
const b = await chromium.launch();
const p = await b.newPage({ viewport: { width: 1440, height: 960 } });
await p.goto(BASE + "?state=pressure"); await p.waitForTimeout(500);
await p.locator(".readBody").focus();
await p.keyboard.press("ArrowDown"); await p.waitForTimeout(150);
await p.keyboard.press("Enter"); await p.waitForTimeout(400);
await p.locator(".peelField").fill("Something completely different that nobody picked");
await p.waitForTimeout(300);
console.log("pickBar:", await p.locator(".pickBar").allInnerTexts());
console.log("peelWhy/labels:", await p.locator(".peelWhy, .peel .peelLabel, #peel-label").allInnerTexts());
console.log("peel full:", (await p.locator(".peel").innerText()).replace(/\n/g," | "));
console.log("picked spans:", await p.evaluate(()=>[...document.querySelectorAll('.sent')].filter(s=>s.dataset.picked!==undefined||s.getAttribute('data-pick')!==null&&s.getAttribute('data-pick')!==undefined).length));
console.log("readBody aria:", await p.locator(".readBody").getAttribute("aria-label"));
// send
await p.locator(".peel button:has-text('Send to Tasks')").click(); await p.waitForTimeout(600);
console.log("after send peel:", (await p.locator(".peel").innerText()).replace(/\n/g," | "));
console.log("live:", await p.evaluate(()=>[...document.querySelectorAll('[aria-live]')].map(e=>e.textContent.trim())));
console.log("undo:", await p.locator(".undo").allInnerTexts());
await p.keyboard.press("Control+z"); await p.waitForTimeout(400);
console.log("after undo, peel:", await p.locator(".peel").count(), "| live:", await p.evaluate(()=>[...document.querySelectorAll('[aria-live]')].map(e=>e.textContent.trim())));
await b.close();
