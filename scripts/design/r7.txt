
##### SEAT Brand and copy = 7.8

=== carry-name-clipped-to-five-letters | cost 0.7
PR : Pick a card up and the pill reads "Moving Confi…". Measured at 1440, 1280, 1120, 900, 768 and 390: the name box renders 81px of a 302px string every single time, because .carry em is flex:none and the three hints refuse to yield, so the name absorbs 100% of the shortfall. This is not a responsive edge case, it is the default. At the one moment the operator is holding something and cannot see it in place, the product refuses to name it, and on the dense board where three titles begin "Confirm the…" the pill is worthless. The gate's truncation assertion only inspects .cardTitle and .cardNote, so
FIX: Rewrite the three hints in footPill() to one shape: `<kbd>↑↓←→</kbd> move`, `<kbd>Space</kbd> drop`, `<kbd>Esc</kbd> cancel`. Then give the name a floor rather than a share: change .carryName to `flex: 1 0 190px; min-width: 190px;` and extend the existing hint-dropping rule at line 1168 so hints are shed right-to-left (Esc first, then Space) before the name ever gives up a character.
SHARP: Do the copy rewrite as proposed — `<kbd>↑↓←→</kbd> move`, `<kbd>Space</kbd> drop`, `<kbd>Esc</kbd> cancel` — measured at 80 / 71 / 70px, which alone lifts the name from 81px to 171px ("Moving Confirm marq…"). But do not use `flex: 1 0 190px; min-width: 190px`: I applied it live and the pill overflows its 442px box by 3px with one hint rendered outside the ink, which is exactly the failure the 390 rule at line 1168 was written to prevent. Measured budgets with the short copy are name = 171px at t

=== chip-unit-mismatch | cost 0.5
PR : The three chips in one visual family carry three different units: "1 Aug" is a date, "Today" is a day, "2 days" is a duration. The stated rule is that the chip carries the value and the fill carries the condition. "1 Aug" and "Today" obey it. "2 days" does not: lateness is a condition, computed by daysLate(), so the overdue chip is carrying the condition twice and the value not at all. A venue owner reading a black pill marked "2 days" cannot tell from the chip alone whether that is two days late or two days left, and the sentence that would settle it lives in a tooltip she will never open on 
FIX: In timeOf(), return the due date for the overdue case exactly as the soon case does: `if (task.dueTone === "overdue") return { kind: "overdue", label: dayLabel(task.dueAt) };` so the chip reads "14 Jul" in filled ink. Keep SAID.overdue as `[daysLate(task.dueAt) + " overdue", "Overdue, due "]` so the accessible name and tooltip still count the days. Update the Overdue specimen note to "Filled ink, white type. The chip carries the day it was due; the fill says it has passed."
SHARP: Take the proposed change, with three tightenings.

1. floor.html:1339 — `if (task.dueTone === "overdue") return { kind: "overdue", label: dayLabel(task.dueAt) };`

2. floor.html:1356 — make the tooltip answer BOTH questions rather than replacing one with the other, since the days-late count is the thing being demoted and should still be reachable in one hover: `overdue: ["Due " + time.label + ", " + daysLate(task.dueAt) + " overdue", "Overdue, due "]`. The sr prefix "Overdue, due " keeps the acc

=== filter-stated-twice | cost 0.4
PR : Filter on overdue and the board says the same thing twice at once, in two different phrasings, both opening on the same word: the header chip becomes "Showing 1 overdue" and the foot strip says "Showing the 1 overdue task. 12 others are hidden." The file already fought this exact battle inside the columns and won, then lost it between the header and the foot. It also costs the operator two standing facts to gain a duplicated one: "5 of 13 done" and "Wedding season, day 11 of 97" are both dropped while filtered. And "the 1 overdue task" is not English anyone writes.
FIX: Leave the header chip as the value only, "1 overdue", with aria-pressed carrying the state and the close glyph the way out; delete the "Showing " prefix at line 1452. Keep the ratio and the season line rendered while filtered. Let the foot strip make the one statement, tightened: "1 overdue task. 12 others hidden." and for the empty case "Nothing is overdue. 13 others hidden."
SHARP: Three edits, all copy. (1) floor.html:1454 — drop the "Showing " prefix so the chip is the value in both states, "1 overdue", with aria-pressed and the close glyph carrying "this is on" and "this is the way out". (2) floor.html:1448 and 1456 — delete the `!lateOnly` guards so "5 of 13 done" and "Wedding season, day 11 of 97" stay put; they are project-wide standing facts by the file's own definition, and the foot strip is now the thing that explains what the board is withholding. (3) floor.html:

=== empty-board-five-voices | cost 0.4
PR : A venue owner's first ever screen is five sentences of equal weight competing across the sheet, three of which describe conditions that cannot yet occur ("Nothing to check yet", "Nothing held up", "The first thing you finish lands here"), plus five identical "Add here" buttons and a sixth "Add task" on the dock. Two sentences instruct, three report. Nothing tells her what to do first. The board already knows the right rule and applies it under filter, where the source comments say the board "says it once, not five times"; on the empty board it says it five times.
FIX: In board(), gate the per-column empty line on the board having work: `const empty = lateOnly || !allTasks().length ? "" : c.empty;`, and when allTasks() is zero render one line in the To do column only, set as the sheet's single sentence: "Nothing here yet. Add the first thing you have to do." Suppress the four other trayAdd buttons at zero so the one "Add here" under that sentence is the only create affordance on the sheet besides the dock.
SHARP: Confirm the sentence half, drop the button half. In board(), gate the per-column empty line to the first column when the board is at zero: `const empty = lateOnly || (!allTasks().length && c.id !== "todo") ? "" : c.empty;` — the four trayNote lines stay, so every column still teaches what it holds, and the sheet carries exactly one instruction. Keep the existing string verbatim: "Add the first thing you have to do." Do NOT prepend "Nothing here yet." as proposed — the trayCount already reads 0, 

=== schedule-word-collision | cost 0.2
PR : The word Schedule does three jobs within four inches. It is a top-level view in the segmented control, it is named as a destination in the help line ("drag it onto the Schedule or Calendar view"), and it is the label on all five row buttons, where it means something else entirely: give this task a date. A first-time operator reasonably reads those five buttons as "send this to the Schedule view". Then "Select all" is rendered ahead of the help sentence and lands at the end of its first line, so it reads as though it belongs to the sentence.
FIX: Relabel the row button to "Pick a day" in drawer() (keeping the chevron), which also matches the help line's own verb. Move the Select all button to render after the help paragraph and set it on its own line above the rows, so the sentence reads as a sentence.
SHARP: Two edits in drawer() (floor.html:1619-1624). 1) Relabel the row control to "Pick a day" keeping the chevron, and give it a real accessible name — `aria-label="Pick a day for ' + esc(t) + '"` — so screen-reader users hear five distinct commands instead of "Schedule" five times against a view tab of the same name. Leave the help sentence as it is: "Give each task a day" and "Pick a day" now share a verb, and "the Schedule or Calendar view" is left as the only place the word is a destination. 2) T

##### SEAT UX and information design = 7.4

=== add-is-inert | cost 9
PR : I clicked all seventeen chrome controls under Playwright and compared the sheet's innerHTML, #say and card count before and after each: every one is DEAD — not a state change, not a cursor hint, not a live-region word. The most prominent object on the sheet is the one filled-ink pill labelled "Add task", and it does nothing. On the empty board the copy reads "Add the first thing you have to do." and there is no working way to do it, so first contact for a venue owner ends in a dead click. Meanwhile four view tabs and three tools are painted at full strength and promise a Schedule and a Calenda
FIX: Make one path real and demote the rest. (1) Wire ".dockPrimary" and ".trayAdd" to the same composer: insert a draft card at the foot of the target column (To do for the dock) with the title as a contenteditable line already focused, Enter commits it into B.tasks and re-renders, Escape discards it, and the foot strip states "<title> added to To do · Undo" through the existing footPill/undone path so creation is reversible like completion. (2) Until the other views exist, render List/Schedule/Calendar and Filter/Sort/Display with disabled=true, aria-disabled="true" and the existing quiet ink-38 
SHARP: Two halves, and the second is where the proposed fix needs tightening.

(1) Make creation real, once. Give `.dockPrimary` and `.trayAdd` the same `data-act="compose"` and route both to one composer: insert a draft card at the foot of the target lane (To do for the dock, the own lane for a tray row), title as a focused contenteditable line, Enter commits into `B.tasks` and re-renders, Escape discards. Reuse the existing `arm()`/`footPill`/`undone` path so the strip reads "<title> added to To do ·

=== completion-erases-the-record | cost 8
PR : Ticking a card destroys its note. I ticked "Order tonic and the good olives" (note: "Two extra cases of tonic; last olive delivery was short.") and the card that lands in Done has no .cardNote at all — card() gates the note on `task.note && !done` — and clicking a done card opens nothing (0 elements with data-open), so the text is unreachable by any route. Done is exactly where a venue's memory lives: "Three asked for dates. Tea urn was the hero. Repeat the format in spring." is invisible on the board it was written on. Worse, the only reversal expires: unattended, the strip lived 5,356 ms and
FIX: In card(), drop the `&& !done` from the note branch so a completed card keeps its note, clipped to the same two lines, and allow openNoteId on done cards so click and Enter expand it in place exactly as they do elsewhere. Extend the undo window to 12 s, and when the strip retires push the entry onto an `undoStack` so Ctrl+Z still reverses the last three actions and the "…" button opens that list — this also converts the dead "…" into the board's history. In the aria-describedby string, strip a trailing full stop from task.note before the ". " join and remove task.tag from the list, since the f
SHARP: Three changes, in priority order. (1) In card() (floor.html:1385, 1405) drop `&& !done` from both the note branch and the aria-expanded branch, so a completed card keeps its note under the same two-line clamp (.cardNote max-height: calc(1.45em * 2) already holds, so the Done column does not grow unbounded) and click/Enter expand it in place exactly as elsewhere. Done is where a venue's memory lives; the note is the only thing on the card worth re-reading a month later. This alone also repairs th

=== couple-said-twice | cost 6
PR : Eight of the thirty-two dense cards print the client's name in the title and again in the slot immediately below it: "Nora & Cian deposit received" over "Nora & Cian"; "Book the tasting date for Nora & Cian" over "Nora & Cian"; "Walk Aisling & Tom through the orchard on Sunday" over "Aisling & Tom". In the peak-season To do column three consecutive cards each say the couple twice, so the slot stops carrying information and becomes an echo, and the eye learns to skip the line where priority and comment counts also live. The inverse also happens: "Price the extra hour on the bar for Nora & Cian"
FIX: In card(), before building `bits`, resolve the client: `const client = task.tag && CLIENTS.has(task.tag) ? task.tag : [...CLIENTS].find(c => c.split(" ")[0] && new RegExp("\\b" + c.split(" ")[0] + "\\b", "i").test(task.title));`. Push the .who chip only when `client && !new RegExp("\\b" + client.split(" ")[0] + "\\b", "i").test(task.title)` — so the foot never repeats a name the title already carries — and keep `client` in the aria-describedby string so the grouping stays complete to assistive tech and to any future filter.
SHARP: Leave card() alone — the .who chip stays unconditional and deterministic, which is what makes the foot scannable. Fix the eight titles in data.js so they are written the way an operator writes into a board that already knows whose wedding it is: "Nora & Cian deposit received" -> "Deposit received"; "Deposit invoice settled, Mara & Finn" -> "Deposit invoice settled"; "Book the tasting date for Nora & Cian" -> "Book the tasting date"; "Send Nora & Cian three photographer names" -> "Send three phot

=== undated-buried-season-said-twice | cost 6
PR : At peak season nineteen of thirty-two tasks carry no date at all — I counted four .when chips on thirty-two cards — and the board never says so. That fact exists only as the bare numeral in "Planning 19", whose meaning is hidden in a title attribute the operator will never hover, on a ghost button in the far top-right. What the header does promote instead, in the same ink and size as the date, is "Wedding season, day 11 of 97" — a number that changes nothing about today and which the Planning drawer then states again as "Day 11 of 97 · 86 days left". So the header's third-most prominent fact i
FIX: In head(), replace the `<span>Wedding season, day …</span>` fact with `undated ? '<button type="button" class="undated" data-act="planning">' + undated + ' with no date</button>' : ''`, styled in the plain ink-72 register with an underline on hover — no chip container, so the fill vocabulary is untouched — and have it open the Planning drawer. The season line already lives verbatim at the head of that drawer, so nothing is lost by removing it from the header.
SHARP: Keep the season line — it is not duplicated and it is the product's one differentiating fact. Fix the numeral instead. Give the count words where a thumb and a screen reader can both get them: add a second control to .headFacts, directly after the overdue chip so the two states that demand attention read as a pair — `undated ? '<button type="button" class="undated" data-act="planning">' + undated + ' with no date</button>' : ''` — in the plain ink-72 register with a hover underline and no chip c

##### SEAT Interaction and states = 8

=== undo-stack-is-one-deep | cost 0.6
PR : Undo is one entry deep and the strip silently overwrites it. I ticked two To-do cards in a row: the second strip replaced the first with no trace, the first Ctrl+Z reversed tick two, and the second Ctrl+Z did nothing at all — tick one is unreversible forever (todo went 3 to 2 and stayed there). The timing is right (6s idle, ~22s while the pointer rests on it) but the depth is wrong, and ticking a run of cards is the single most common thing a venue operator does on a Saturday. The board promises a way back on its most mis-tappable action and then quietly withdraws it the moment you act twice.
FIX: Replace the single pending-undo record with an array acting as a stack. Every reversible act (completion, un-completion, menu move, pointer drop) pushes {label, revert} instead of overwriting. Ctrl/Cmd+Z pops the top and runs its revert, so a second press reaches the previous act; keep the stack alive for the session, not for the strip's 6s. The strip always renders the top entry and, when stack.length > 1, its label reads "Undo · 2 more" so the depth is visible. Add Ctrl/Cmd+Shift+Z to re-apply the last popped entry, and announce each pop through #say exactly as the current single undo does.
SHARP: Separate the record from the strip. Replace `undone` with `history`, an array; `arm(act)` pushes instead of assigning. The strip's contract does not change at all: it renders only `history[history.length - 1]`, only for the existing 6s, still holds while hovered or focused, still yields to the in-hand pill and still steps aside for the filter statement once it lapses — so the foot of the sheet behaves exactly as it does today and no gate assertion moves. What changes is the keyboard: Ctrl/Cmd+Z 

=== escape-contract-incomplete | cost 0.3
PR : The product teaches one key and then honours it three times out of five. Escape returns a carried card, closes the card menu and clears the overdue filter — but an opened note (click or Enter, .card[data-open]) ignores Escape entirely, by pointer and by keyboard, and the Planning drawer ignores it too despite carrying a Close button and 15 focusables. Worse, with a note open, Space still picks the card up and an arrow still moves it across columns while it is expanded, so the operator ends up carrying a card in a state they cannot dismiss. The one universal escape hatch in the board has a hole
FIX: In the document keydown handler, before the filter branch, add: if a .card[data-open] exists, remove data-open, move focus back to that card, announce "Note closed" through #say and return; else if the Planning aside is open, close it, return focus to the Planning button and return. Keep the existing carried-card and menu branches ahead of both so the innermost state always wins.
SHARP: Scope to the note only; drop the Planning drawer entirely (it is a URL scene, not a live surface). Do not touch the document-level Escape block. Inside onKey, after the existing `if (key === "Escape" && carriedId)` branch at line 1974 so the carried card still wins, add: `if (key === "Escape" && openNoteId === id) { event.preventDefault(); openNoteId = null; say("Note closed."); refocus = true; mount(); return; }` — same announcement and same refocus path Enter already uses, so the card keeps th

=== overdue-chip-reads-as-a-badge | cost 0.3
PR : The board's only filter is drawn as a status badge. It is a 72x22 ink-filled pill, pixel-identical in form to the non-interactive overdue chip on a card, with no title, no aria-label beyond its own value, and a hover state of a 3px ring at 0.08 alpha, which is below the threshold a first-time venue owner will register as a state change. The cards beneath it all carry proper tooltips ("2 days overdue", "Due today"), so the one element in the view that is a control is the one element that explains itself least. Orla will read it as a count and never discover that the board can narrow to the thin
FIX: Reserve a 14px slot at the head of .late and render the existing I.filter glyph in it at opacity 0, so the chip's width never changes; raise it to opacity 1 on :hover and :focus-visible alongside the existing ring. Add title="Show only the 1 overdue task" and aria-label="Show only the 1 overdue task" (and the pressed-state inverse, "Show all 13 tasks", when aria-pressed is true), generated from the same count that prints the label. No new hue, no weight change, no reflow.
SHARP: Split it, and treat the name as the load-bearing half. (1) Name it, at floor.html:1453, from the same `overdue` count that prints the label: `title` and `aria-label` = "Show only the 1 overdue task" / "Show only the 3 overdue tasks", inverting to "Show all 13 tasks" when aria-pressed is true. This alone closes the AT gap and matches the tooltip convention the cards already keep. (2) For the hover signal, the reserved-slot glyph reveal is the right instinct — a glyph appearing is louder than any 

=== null-drag-announced-as-a-move | cost 0.2
PR : I picked up the first To-do card, moved it 40px and dropped it back exactly where it began. The board announced "Confirm marquee sides with the hire company moved to position 1 of 3 in To do" and raised an undo strip offering to reverse a move that never happened. A dropped-and-recovered card is one of the most common pointer accidents on a dense board, and every one of them costs the operator a false announcement, a false safety prompt, and — given the one-deep stack above — the loss of their real last undo.
FIX: In the pointer-drop handler, capture the origin lane and index at pickup. On drop, compare the resolved lane and index to the origin; if both match, restore the card without pushing an undo entry, without writing to #say, and without showing .carry. Apply the same guard to the keyboard drop path when Space is pressed on the card's starting square.
SHARP: Guard only the pointer path. In `onDrop`, before `moveTo`, compare the resolved target to the origin captured at `dragstart`: `if (overLane.lane === carriedFrom.lane && overLane.index === carriedFrom.index) { carriedId = null; carriedFrom = null; overLane = null; mount(); return; }`. `dropIndex()` already excludes the carried card, so the filtered index is directly comparable to `carriedFrom.index` — no off-by-one. The card settles back with the existing drop transition, no `arm()`, no `say()`, 

##### SEAT Product taste and emotional resonance = 7.4

=== undo-is-only-one-deep | cost 0.7
PR : Reversibility is the emotional spine of this board and it is one act deep. I ticked two cards in a row: the second arm() overwrote the first with no trace, the strip simply re-labelled itself, and the second Ctrl+Z answered "Nothing left to undo" — a flat lie, since I had just undone one of two. The 6s timer then discards the act silently for anyone looking at the screen (only the live region ever mentions expiry). A venue manager clearing a Saturday ticks four things in thirty seconds and mis-taps one; the promise the strip makes is not the promise it keeps.
FIX: Make `undone` a stack (array, cap 10). arm() pushes, undoLast() pops and re-arms the strip on whatever is beneath. When depth > 1 the strip's action reads "Undo" with a trailing count ("2 more"). The 6s timer hides the strip only — never clears the stack — so Ctrl+Z keeps working after the strip has gone; delete the "That undo has expired." line, which becomes untrue. Each pop says the task and the lane it returned to, as undoLast() already does.
SHARP: Keep the stack, drop the invisible-persistence half. (1) Make `undone` a bounded stack (cap 10). arm() pushes; the strip always renders the top act. (2) Make the depth visible so the promise is honest: when depth > 1 the strip carries a quiet trailing count in Geist 400 — "Undo" plus "2 more" — no new hue, no new weight. (3) Ctrl+Z / the button pops, re-arms the strip on the act beneath, and frames it as history rather than news ("Before that: Confirm marquee sides is done.") so a stale act neve

=== add-task-is-the-loudest-dead-object | cost 0.6
PR : The single heaviest object on the sheet — the filled ink pill at the centre of the dock — has no data-act and does nothing. Nor do the five "Add here" rows, nor the search field. The board can be reorganised and completed but never grown. On the empty frame this collapses the whole promise: five columns say "Add the first thing you have to do", "Move something across when you start it", and every route offered is inert. A season is a thing you fill, not a thing you tidy; the direction doc itself named "the dock's Add expands in place into the composer" as a candidate delight moment and it was 
FIX: Build the composer as a card, not a dialog. Clicking .dockPrimary (or an "Add here" row) inserts a real .card at the head of the target column with its title as a contenteditable focused on insert; the dock pill collapses to a rule while it is open. Enter commits and leaves the caret in a fresh card beneath; Escape discards. "Add task" targets the first column, "Add here" targets its own. Announce "Added, in To do" through say(), and arm({kind:"add"}) so the new card inherits the same undo the tick and the move already have.
SHARP: Build the composer as a card, never a dialog. Give `.dockPrimary` `data-act="add"` and each `.trayAdd` `data-act="add" data-lane="<id>"`, and branch them in `onClick` alongside the existing seven acts. The act inserts a real `.card` at the head of the target lane (dock targets the first lane, "Add here" targets its own) with `.cardTitle` as `contenteditable="plaintext-only"` focused on insert; while it is open the dock pill collapses to a rule so the composer is unmistakably the same object that

=== flight-is-a-teleport-then-a-hang | cost 0.5
PR : The one moment this board authored as memorable does not read. Measured frame by frame at 1440: the ghost covers 128px to 940px of a 1033px journey in the first 63ms — four frames — then spends the remaining 180ms crawling the last 18px, below the perceptual threshold. It is a blur followed by a stall, not an arrival. The cause is --curve (cubic-bezier(0.23,1,0.32,1), easeOutQuint) applied to macro travel: correct for a 4px hover, wrong for a card crossing the whole sheet. Worse, a Review-to-Done hop of 516px takes the identical 244ms, so the long one moves at twice the speed of the short one.
FIX: In flyCompleted(), after computing dx/dy set `const d = Math.hypot(dx, dy)` then `ghost.style.transitionDuration = Math.round(Math.min(460, Math.max(220, 170 + d * 0.22))) + "ms"` and `ghost.style.transitionTimingFunction = "cubic-bezier(0.32, 0.72, 0, 1)"`, and change the safety `setTimeout(finish, 400)` to that duration + 140. Record the exception in the comment beside --curve, which already requires any other value to be argued in writing: the token governs state change, the flight is travel.
SHARP: In flyCompleted() (floor.html:1774-1804), after `const dy = was.top - now.top;` and the sub-pixel early return, compute the travel and hold it:

  const d = Math.hypot(dx, dy);
  const flight = Math.round(Math.min(520, Math.max(220, 170 + d * 0.26)));

Then, on the ghost before the forced `getBoundingClientRect()` reflow (the offset must be committed with the final timing already in place):

  ghost.style.transitionDuration = flight + "ms";
  ghost.style.transitionTimingFunction = "cubic-bezier(

=== carry-pill-cannot-say-the-name | cost 0.4
PR : Space picks a card up and the pill that appears reads "Moving Confi…". Measured: .carryName is given 81px of the 302px it needs at 1440, 1120, 900 and 768 — it is clipped at every desktop width, on a 1300px sheet. The three hint chunks are flex:none and eat the pill; the name is the only flexible child, so it collapses first. This is the one instant the product speaks about the object in your hand, and it cannot pronounce it. The typesetting gate does not catch it because that gate only inspects .cardTitle and .cardNote.
FIX: Invert the flex priority: `.carryName { flex: 1 1 auto; min-width: 22ch }` and `.carry em { flex: 0 1 auto; min-width: 0; overflow: hidden }`, then extend the existing progressive-drop rule so the hints go in order — "Esc cancel the move" first, then "Space drop it here" — before a single character of the name is allowed to clip. Keep the strip's width as it is so the dock silhouette is unchanged. Add an assertion that .carryName has scrollWidth <= clientWidth at 1440, 1120, 900 and 768.
SHARP: Keep the diagnosis, change the remedy — the proposed one does not survive the arithmetic and would ship a worse pill. Content budget inside the 442px strip is 392px (20px padding, 3×10px gaps). The three hints cost 311px; a 48-char `shortTitle` costs up to ~330px. There is no ordering of drops that fits the full name beside even two hints, so "not a single character of the name may clip" silently means "delete Space drop it here AND Esc cancel the move" at 1440 — trading the escape hatch, the on

##### SEAT Measured evidence = 7.9

=== loading-state-has-no-words | cost 0.5
PR : The loading state's only sentence is invisible. Measured at 1440×960: .loadSay is at y 886–903, the dock at y 876–924, both z-index 3, and the dock is written later in the DOM, so it paints over it — document.elementFromPoint at the centre of the say line returns a dock SPAN. The shipped frame locked-loading--1440x960.png confirms it: zero words on screen. Worse, the idle dock is drawn at full live strength — a filled ink "Add task" button and a search field, aria-hidden and inert, with no visual difference from the working dock, because data-idle (line 1659) has no CSS rule anywhere in the fi
FIX: Set .loadSay { bottom: 78px; z-index: 4; } so it sits clear above the dock band. Add the missing idle styling: .dock[data-idle] { box-shadow: var(--ring); } .dock[data-idle] .dockPrimary { background: var(--wash-2); color: var(--ink-3); } .dock[data-idle] .dockField, .dock[data-idle] .dockAvatar { color: var(--ink-3); }. Render the ⌘K kbd inside the idle dockField too, so the foot is 442px wide from the first paint through to the board. Add aria-busy="true" to the loading .board and call say("Opening " + B.workspace) on mount so the wait is announced once.
SHARP: Fix the cause, not just the symptom, in four moves. (1) Lift the sentence clear of the dock band: .loadSay { bottom: 78px; z-index: 4; } and raise the narrow-viewport override at line 1183 in step (.loadSay { bottom: 72px; }) — leaving that rule at 16px reinstates the same collision on the phone frame. (2) Give the ⌘K keycap to the idle dockField as well (line 1660), so the foot is one object at one width from first paint to board; while there, delete the dead `width: var(--dock-w)` at line 687 

=== the-flight-carries-a-hole | cost 0.5
PR : The product's one deliberate delight moment is built from the wrong card. flyCompleted clones the node AFTER the repaint, so the ghost is the done version — the note is stripped by card() (task.note && !done) — but it is sized to the pre-completion rect. Measured: ghost 234×136 with no .cardNote and its foot bottom at 124px, landing into a card of 233×98. The card therefore flies with a visible empty band where the note was (see the t=120ms capture), then snaps 38px shorter on arrival with no height transition, because .cardGhost only transitions transform. And the acknowledgement is in the wr
FIX: Clone the card BEFORE the state change: in toggleDone() and moveTo(), capture flyNode = node.cloneNode(true) at the same moment as flyFrom, and use that clone as the ghost so the card flies as it looked when it was clicked, note and all. Give .cardGhost `transition: transform 260ms var(--curve), height 260ms var(--curve)` and set ghost.style.height to the landed node's measured height on the same frame the transform is released, so the card resolves to its done size in flight rather than snapping. Move the beat to the origin: put data-just-done on the ghost at t=0 so the circle settles and the
SHARP: Fly the card that was clicked, and let it become done in flight — once. (1) In toggleDone() and moveTo(), capture `flyNode = node.cloneNode(true)` on the same line as `flyFrom`, before any state change, and use that clone as the ghost so the note and the un-ticked circle travel with it. (2) Give .cardGhost `transition: transform 260ms var(--curve), height 260ms var(--curve)` plus `overflow: hidden`, and on the frame the transform is released also set `ghost.style.height` to the landed node's mea

=== application-role-mutes-the-teaching-copy | cost 0.45
PR : role="application" on the board switches screen readers out of browse mode for everything inside it, so only focusable nodes are reachable. That silently deletes exactly the copy this product leans on to teach a first-time venue owner: the five column sentences ("Agreed and ready to start.", "Held by a reply, a delivery, or a decision.") and every .cardNote are unreachable text. On ?state=empty I measured five .trayEmpty sentences, none of them in any accessible name, no live-region text, and five focusable buttons — all five named the identical, context-free "Add here". A screen-reader user m
FIX: Keep role="application" (the arrow model needs focus mode) and move the copy into names instead of relying on browse mode. Build the tray label as esc(c.name) + ", " + (rows.length ? rows.length + " tasks. " : "empty. ") + esc(rows.length ? c.note : c.empty) — e.g. "To Do, empty. Add the first thing you have to do." Name each add button for its column: aria-label="Add a task to " + esc(c.name). Give the tick one stable name — aria-label="Done" always — and let aria-checked carry the state, so a completed task never announces as "Not done yet, checked".
SHARP: Do not stuff the sentences into the region aria-label — arrowing across columns would re-read a long name on every crossing. Keep the region name short and attach the copy where focus lands. (1) Give the .trayNote and .trayEmpty paragraphs ids (e.g. tn-{c.id}, te-{c.id}) and point the tray at the live one: aria-describedby="tn-{c.id}" when populated, "te-{c.id}" when empty. (2) Name each add button for its column and describe it with the empty sentence when the column is empty: aria-label="Add a

=== a-timer-repaints-into-a-live-gesture | cost 0.3
PR : arm()'s 6-second undo timeout ends in mount(), which does target.innerHTML = renderApp() — an unconditional full nuke of the board, fired by a clock rather than by the operator. Driven at ?state=dense: tick a card to arm the timer, then start a drag and hold it. At 6.2s into the hold I measured data-over count 0 and .dropLine count 0 — the warmed target column and the line promising where the card will land both vanished from under a card the operator is still holding, mid-gesture, with no input from them. The dragged node itself is destroyed and replaced at the same instant. The strip's clock
FIX: Guard the timeout instead of firing it blind: inside arm()'s setTimeout callback, before clearing, add `if (carriedId || menuFor) { undoTimer = setTimeout(arguments.callee, 1000); return; }` — or hoist the callback to a named function and re-arm it for 1000ms — so the strip never repaints while a card is in hand or a menu is open. Additionally call holdUndo() at the top of onDragStart() and releaseUndo() at the end of onDrop() and onDragEnd(), so a pointer drag suspends the undo clock the same way a hover already does.
SHARP: Take the second half of the proposed fix and drop the first. Do not use arguments.callee or a 1000ms re-arm poll — it is invalid under strict mode and it re-implements a suspend mechanism the file already has. Instead extend the existing one: call holdUndo() at the top of onDragStart() (right after carriedId is set), and releaseUndo() at the end of onDragEnd() and at the end of onDrop() after its own arm() call — arm() clears first, so re-arming the fresh move act is harmless and simply gives th

##### SEAT UI composition — grid, optical balance,  = 7.4

=== carry-strip-starves-the-card-name | cost 9
PR : The in-hand strip is width-locked to the dock (`width: var(--dock-w)` = 442px). At 1440 — with 1322px of sheet available — the strip renders "Moving Confi…". Thirteen characters. Measured live: the three keycap hints (↑↓←→ move · Space drop it here · Esc cancel the move) are `flex: none` and keep every word, while `.carryName { flex: 1 1 auto; min-width: 0 }` absorbs the entire shortfall. The one fact the operator needs while holding a card — which card am I holding — is the only thing the composition sacrifices, and it is sacrificed to a width contract, not to a lack of room. The undo strip d
FIX: Stop pinning the strip's width to the dock's. In `.carry`: replace `width: var(--dock-w)` with `width: max-content; max-width: min(760px, calc(100% - var(--inset) * 2));`. In `.carryName`: `flex: 0 1 auto; min-width: 24ch;`. Shorten the last hint from "cancel the move" to "cancel". The strip keeps the dock's height (36), radius (--r-pill), centre (left:50% / translateX(-50%)) and shadow (--lift), so the silhouette still reads as one family. Amend interaction-check.mjs's "the undo strip shares the dock's silhouette" assertion to compare centre-x and height rather than width — that assertion is 
SHARP: Apply the width change but NOT the copy change. In .carry (floor.html:863) replace `width: var(--dock-w); max-width: calc(100% - 32px)` with `width: max-content; max-width: min(760px, calc(100% - 36px))`. In .carryName (floor.html:877) replace `flex: 1 1 auto; min-width: 0` with `flex: 0 1 auto; min-width: 24ch`. Keep "cancel the move" as written: widening alone already clears truncation at 1440/1280/1024 (measured 663px strip, name untruncated), and at max-width 720px floor.html:1168 hides ever

=== head-sheds-the-venue-name-first | cost 6
PR : `.headName` is `flex: 0 1 auto; min-width: 8ch` and `.headActions` is `flex: none`. At 768 the venue name is measured at 121px and truncated — "The Orchard, …" — losing "events", the word that says what kind of board this is, while Share, Planning 5 and the overflow keep full labels and 228px between them. The subject of the page is the first thing the header gives up and the secondary chrome is the last. The truncation order is exactly backwards.
FIX: Reverse the priority: `.headName { flex: 1 1 auto; min-width: 12ch }` and `.headActions { flex: 0 1 auto; min-width: 0 }`. Inside the existing `@media (max-width: 900px)` block, wrap the Share and Planning word labels in a `<span class="lbl">` and add `.head .ghost .lbl { display: none }`, keeping the icons and the Planning count. That returns roughly 110px to the name at 768 and lets it set in full.
SHARP: Leave the flex values alone — .headName stays flex: 0 1 auto and .headActions stays flex: none, which is what keeps word · rule · name · facts reading as one left-hand cluster at 1200 and 1440. Buy the space from the chrome instead. Add, above the 900px block: @media (max-width: 1100px) { .head .ghost > span { position: absolute; width: 1px; height: 1px; overflow: hidden; clip-path: inset(50%); white-space: nowrap; } } — visually hidden rather than display:none, so Share and Planning keep their 

=== views-row-breaks-the-28px-right-edge | cost 3
PR : The sheet declares one leading edge at 28px and holds it exactly on the left: wordmark 128, first segmented item 128, tray note 128, card edge 128 (sheet at x=100). The right stack does not hold it. The header's last action ends at 1394 (28 from the sheet edge) and every card's right edge ends at 1394 — but Filter/Sort/Display end at 1397, 25 from the edge. `.views` uses `padding: 0 25px 14px` so that the seg's 3px inner padding lands the Board pill on the 28 line; nobody applied the same compensation on the right, where `.ghost` has no inner inset. So the two toolbar rows, stacked directly on
FIX: `.views { padding: 0 28px 14px; }` and `.seg { margin-left: -3px; }`. Left edge stays at 128, right edge moves to 1394 and joins the header actions and the card column on the single 28px line.
SHARP: Do not use `.seg { margin-left: -3px }` — it is unscoped and leaks into the max-width:720px block, where `.seg { width: 100% }` and `.views { padding: 14px 16px 12px }` would put the phone seg at left 13 and right edge−19, trading a desktop defect for a new phone asymmetry. Instead change only the base rule at floor.html:310 to a four-value padding, leaving `.seg` untouched: `.views { flex: none; display: flex; align-items: center; padding: 0 28px 14px 25px; }` with a comment saying the 25 on th

##### SEAT Typography = 7.8

=== carry-pill-elides-its-own-subject | cost 8
PR : Picked a card up with Space and the pill reads "Moving Confi… ↑↓←→ move Space drop it here Esc cancel the move". The strip is a fixed 442px, the three hint phrases are all flex:none, and .carryName is the only flexible item at min-width:0 — so the one token that says WHAT you are holding is elided to five glyphs while three instructional phrases keep every word, including "it here" and "the move". The same component gives the name 306px in the undo variant, so the product treats the identical token two opposite ways in one silhouette. The behaviour gate proves the strip stays inside the sheet 
FIX: Set .carryName { flex: 1 1 auto; min-width: 20ch; } and in the move variant of the strip drop the leading word "Moving" (the #say live region already says "Picked up …") so the title leads at 600, and shorten the hints to `<kbd data-keys="arrows">↑↓←→</kbd> move`, `<kbd>Space</kbd> drop`, `<kbd>Esc</kbd> cancel`. Measured: those three hints plus padding total ~310px of the 442px pill, leaving ~132px ≈ 21 characters, so "Confirm marquee sides" survives instead of "Confi…".
SHARP: Apply the fix, but correct its arithmetic and add one thing it misses. I patched the live DOM and measured: with `.carryName { flex: 1 1 auto; min-width: 20ch }`, "Moving" dropped, and hints shortened to `↑↓←→ move` / `Space drop` / `Esc cancel`, the name gets **171.3px**, not the 132px claimed — about 26 glyphs, rendering "Confirm marquee sides with …". Verified zero overflow at 730/760/900/1100/1440 and at 390 (where the pill is 340px and the min-width still clears). So the subject becomes ide

=== card-title-rag-is-unmanaged | cost 8
PR : Measured every title's line boxes at 1440. The rag is untypeset: "Deposit invoice settled, Mara / & Finn" runs 179px then 38px — a 21%-fill last line that starts with an ampersand and splits the couple's name, the single highest-value token on a wedding board. "Build the Saturday / run-sheet" is a one-word widow at 64px. "Chase the / florist quote for the / spring date" staircases 66/127/74. Two to four cards per screen begin a line on a stranded article or preposition ("and / the good olives", "in / the diary", "before / the date") at 1440, 1680 and 1920 alike. `text-wrap: pretty` only protec
FIX: Change .cardTitle to `text-wrap: balance` (keep `pretty` on .cardNote — balance is capped at short blocks and titles are clamped to three lines). Verified by overriding it live: 15 of 32 dense-board titles re-break, "Deposit invoice settled, Mara / & Finn" [179/38] becomes "Deposit invoice / settled, Mara & Finn" [95/123], "Pencil the autumn date in / the diary" [165/57] becomes [118/105], "Gravel delivered for the top / car park" [168/52] becomes [100/120]. Then bind the couples in data.js with U+00A0 either side of the ampersand ("Mara & Finn") so a name can never split at any measure.
SHARP: Set .cardTitle to text-wrap: balance (floor.html:454), leaving pretty on .cardNote — titles are clamped to three lines so they sit inside the balancer's line cap, and balance has wider browser support than pretty. Do NOT bind the ampersand by editing data.js: floor.html:1251 holds `const CLIENTS = new Set(["Mara & Finn", "Nora & Cian", "Aisling & Tom"])`, compared literally against task.tag at line 1369, so U+00A0 in the data would silently drop every card out of client-tag styling. Bind at rend

=== the-one-tracking-curve-is-not-one-curve | cost 6
PR : The file declares a single tracking ladder and comments that every letter-spacing is a token from it. Walking the computed styles of every visible text node: the board carries 9 off-curve nodes, the planning drawer 17, the specimen sheet 15. The 13px band still holds two values and the 12px band three — the exact drift the token block says was fixed. Worst case is the empty board, where 100% of the visible sentences are off-curve: all five column sentences and all five "Add here" rows sit at 0 tracking while every other 13px string on the sheet sits at −0.010em, so the first screen a new venue
FIX: Add `letter-spacing: var(--tr-13)` to .trayEmpty, .trayAdd, .drawerLine, .drawerSummary, .drawerTab, .selectAll and .specIntro; add `letter-spacing: var(--tr-12)` to .drawerHelp, .specNote and .sched; correct .railAvatar and .dockAvatar from 0.04em to var(--tr-10). Then add one assertion to interaction-check.mjs that walks every visible Geist-sans text node and fails any whose letter-spacing/font-size is not the curve value for its size band, so the invariant is enforced rather than asserted in a comment.
SHARP: Fix the cause, not only the twelve symptoms. The drift is not random: almost every off-curve node is either a bare <p> that inherits from an untracked body, or a <button> — and Chrome's UA `font` shorthand on button/input resets inherited letter-spacing to normal, which is why .trayAdd, .drawerTab, .selectAll and .sched silently drop to 0 even when their parent carries a token. So (1) set the band token on the element that owns the font-size, and (2) add letter-spacing to the existing `font: inh

=== tray-count-breaks-the-micro-label-rule | cost 5
PR : The column label is Geist Mono 11px/0.1em uppercase; the count beside it in the same 24px row is Geist Sans 12px with letter-spacing:normal. Measured glyph boxes: the label runs top 146 to bottom 160, the count 145 to 161 — the number is optically larger than the label it belongs to and overshoots it on both edges, so the tray head reads as two objects rather than one instrument. Every other micro-label in the product (.tag, .specLabel, .drawerKicker) obeys the mono/11/0.1em rule; the count is the only one that does not, and it appears five times on every screen at every breakpoint.
FIX: Set .trayCount { font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.1em; } — it already declares tabular-nums — so the label and its number share one family, one size and one optical box, and the count column stays rigid as digits change from 3 to 10.
SHARP: .trayCount { font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.1em; margin-right: -0.1em; } — the negative margin is the part the original fix misses: .trayCount is pushed flush right by margin-left:auto, and 0.1em tracking leaves a 1.1px phantom gap after the final digit, so without it the number would sit 1.1px inboard of the tray's content edge that .trayName and .trayNote align to. Also mirror the variant: [data-type="expressive"] .trayName jumps to 0.14em at line 1086 with n

=== wordmark-and-project-name-share-one-rank | cost 4
PR : The product signature and the name of the venue's board sit at 17px and 18px, both 600, both full ink-1, both on the same tracking token, separated only by a 1px rule. A 1px step at that size is below the just-noticeable difference, so the top of the hierarchy reads as one continuous 600 string — "tasks. The Orchard, events" — and the sheet's subject never outranks the app's branding. The phone layout already proves the intent (17 against 19); the desktop header, the frame every reviewer looks at first, does not.
FIX: Set .word { font-size: 15px; letter-spacing: var(--tr-15); } so the wordmark drops a real step below the project name (15/600 against 18/600) and the hairline rule divides two ranks instead of two equals. The indigo dot keeps the mark identifiable at the smaller size.
SHARP: Set .word { font-size: 15px; letter-spacing: var(--tr-15); } so the mark drops a full step below the h1 (15/600 against 18/600) and the hairline divides two ranks rather than two equals. Leave the [data-type="expressive"] override (.word 19px / .headName 22px) and the phone .headName 19px as they are — both already carry a real step, and the phone becomes 15/19 which is if anything better. Keep the dot at 4.5px; it still reads as the mark's terminal at 15px. Bonus to state in the diff note: this
