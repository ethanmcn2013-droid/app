======================================================================
UI composition | sticky-rule-guillotines-the-row-under-it | cost 0.4
  [element] .idxDay (position: sticky, top: 0, background: var(--white)) over .idxRow inside the .index scroller — master.css ~line 524ff; reproduced at ?state=pressure, index.scrollTop 300, and at 390 on the phone at any mid-scroll rest
  [problem] The group rule is opaque across its own 52px band and nothing else. Any scroll offset that leaves a row mid-transit under it cuts the row horizontally through the x-height: you see the bottom halves of the letters of somebody's own sentence, cleanly sliced. At scrollTop 300 the row under "Mara & Finn · Saturday 18 July" reads as a strip of severed glyphs; at 455 it happens to land clean. So the defect is present at most resting positions, not a few. This is the same failure the code already reasoned about horizontally — the comment at .idxText says a half-sliced letter "reads as damage rather than as a truncation" — solved on the x axis and left open on the y. It is the single most frequently visible flaw in the build, and it lands on the one object the whole product exists to protect.
  [fix] Give the sticky band a lead-out instead of a hard edge. Add `.idxDay { position: sticky; top: 0; z-index: 2; }` and `.idxDay::after { content:""; position:absolute; left:0; right:0; top:100%; height:16px; background:linear-gradient(to bottom, var(--white), rgba(255,255,255,0)); pointer-events:none; }` so a row dissolves into the rule rather than being cut by it. Then add `.index { scroll-padding-top: 68px; }` and `.idxRow { scroll-margin-top: 68px; }` so arrow-walking never parks a row half under the rule either.
  [verdict] I tried to kill this and it survived, but not for the reason the finding gives. Two separate claims are bundled here and they do not stand or fall together.

REFUTED — the headline. The rest-scroll "guillotine" is real but is not a defect. Measured at 1440 in ?state=pressure (.index scrollTop stepped 0→800): a row's .idxText box is 21.8px and the partial coverage at 100/300/340/600/700/800 is 11.0/11.3/11.4/8.4/5.0/8.7px, so yes, letters are bisected mid-x-height at most offsets. But that is the behaviour of every opaque grouped-list header shipped — Things, Bear, Apple Notes, and Linear all hard-clip rows under a sticky section rule with a hairline. The analogy to the .idxText comment is a category error: that comment is about a letter sliced at REST by a static pill, with no motion to explain it. A row mid-transit under a header is explained by the scroll itself and is resolved by the act of scrolling. Taste stated as a defect.

REFUTED — the primary fix, which would make the work worse. `.idxDay { z-index: 2 }` is already in the file (master.css:484), so half of it is a no-op. Worse, `.idxDay::after` is ALREADY the group hairline (master.css:489-491, `bottom:0; height:1px; background: var(--line-soft)`) — the separator an earlier round explicitly built so "the heading takes the step above" its rows. Redefining `::after` as a 16px gradient at `top:100%` deletes it. And the gradient reintroduces exactly what this file's own comment at master.css:460 records as already rejected: "the last row of the pile needs clearance rather than being scrimmed out of legibility." It does not stop the cut either — it replaces a clean edge with 16px of part-opacity ink, which is text that would fail the audit's WCAG-AA-against-the-real-composited-backdrop check inside that band.

CONFIRMED — the second clause, and it is bigger than the finding claims. Driving the arrow walk at ?state=pressure: walking DOWN never parks a row under the rule, but walking UP does, because notebook.js:1641 uses `row.scrollIntoView({ block: "nearest" })` and "nearest" treats a row hidden behind a sticky header as already in view. Over 26 ArrowUp presses at 1440, 18 of them leave the keyboard cursor row obscured by the stuck .idxDay, and at the worst it is 100% hidden — the row you are on is entirely gone, indigo wash, ring and all, verified in a screenshot. At 390 it is 21 of 26 and up to 51% hidden. This escapes the 276-assertion gate: notes-interaction-check.mjs:153 asserts "the walked row is visible without a pointer" by counting `.idxRow[data-cursor]` elements, which is the attribute, not the pixels — the assertion's own sentence is the thing that is false. It also escapes the "inside the pile the person is looking at" check at :921, which compares only against the scroller's top and bottom and so passes for a fully hidden row. A keyboard cursor you cannot see is the code's own stated requirement ("the row it is on has to be visible without hovering", master.css:535) and a standing-checklist item, not taste.

Confirming the finding on that ground alone, with the fix narrowed.
  [sharpenedFix] Drop the ::after rewrite entirely — it deletes the group hairline and scrims the person's own words. Keep only the scroll offset, and set it to the real band height rather than 68px (measured: 51.6px at 1440, 43.8px at 390, but the phone rule wraps to two lines).

In master.css, one line on the scroller: `.index { scroll-padding-top: 56px; }`, and in the 430-and-below block `.index { scroll-padding-top: 76px; }` to cover the wrapped rule. Verified: 18 obscured cursor positions at 1440 and 21 at 390 both go to zero, worst-case hidden fraction 100% → 0% and 51% → 0%, with no change to any resting frame.

Then fix the assertion that let it through. notes-interaction-check.mjs:153 should stop counting the attribute and measure the pixels: after each of a run of ArrowUp presses, assert the cursor row's box is not overlapped by any `.idxDay` whose rect is stuck to the scroller top, at 1440 and at 390. The current sentence, "the walked row is visible without a pointer", is the right sentence; it just needs to be true.
======================================================================
UI composition | resting-index-wears-the-keyboard-cursor | cost 0.3
  [element] .idxRow[data-cursor] { background: var(--indigo-wash); box-shadow: inset 0 0 0 1px var(--indigo-line); } — master.css line 537, painted unconditionally; ?state=notebook at load, and after Ctrl+Enter
  [problem] The loudest object inside the resting sheet is a keyboard cursor that nobody has moved yet. At load, before a key is pressed, a full-width indigo band with an indigo ring sits on row one — so the eye's first stop inside the paper is a cursor, not the person's newest words and not the field they are meant to write in. It gets worse after a capture: the cursor holds its note by id, so the moment you keep a thought, the new row appears at the top of the group unemphasised while the indigo band stays on the row below it. The strongest ink in the index points at the note you did not just write. And the paint is not even the index's own: `var(--indigo-wash)` + `var(--indigo-line)` is verbatim the pair used for `mark` (search hits) and for the picked sentence in the seam, so at rest the pile wears the exact visual language of "these are the words that will cross into Tasks". Indigo is currently doing five jobs (focus ring, keyboard cursor, arrival flash, search hit, picked words); in a three-colour lock it can afford one or two.
  [fix] Scope the cursor to focus: `.index:focus-within .idxRow[data-cursor] { background: var(--indigo-wash); box-shadow: inset 0 0 0 1px var(--indigo-line); }` and give the unfocused cursor the ink tint the list already owns — `.idxRow[data-cursor] { background: var(--wash); }` — keeping the roving tabindex exactly where it is. Then, on keep, move data-cursor onto the row that just arrived so the emphasis and the arrival flash are the same row instead of two rows fighting.
  [verdict] I tried to kill this and could not. Every load-bearing claim checks out in the live file.

VERIFIED IN SOURCE. master.css:537 paints `.idxRow[data-cursor]` with `background: var(--indigo-wash); box-shadow: inset 0 0 0 1px var(--indigo-line)` with no focus qualifier. notebook.js:2081 seeds `cursorId` to the first visible note at boot, and indexOf() (line 1123) re-seeds it to `notes[0]` whenever the held id falls out of the list.

VERIFIED IN THE BROWSER (chromium, 1440x960). At `?state=notebook` load: `document.activeElement` is `.topField`, `.index` contains no focus at all, and row 0 computes to `rgba(79,70,229,0.08)` fill plus `rgba(79,70,229,0.32) 0 0 0 1px inset`. A keyboard indicator is painted at full strength with no keyboard anywhere in the list.

THE POST-KEEP CLAIM IS EXACT. I typed into the field and pressed Ctrl+Enter. 1.4s later (arrival flash long gone, it is 620ms): rowCount 15, row 0 is "Check the marquee side panels before Friday. / Just now" with no emphasis, cursorIndex is 1, still on "Saturday wedding, Mara & Finn / 35 minutes ago". The screenshot is unambiguous: the single strongest band in the pile sits one row below the note the person just saved, at the exact moment the product exists for. keepDraft() (line 244) never touches cursorId, and neither does the readback keep path (line 1889-1891), so dictation that lands several notes leaves the band below all of them too.

A SECOND CORROBORATION THE FINDING DID NOT CLAIM. At `?state=search` focus is in the query field, the band is still on row 1, and that row's "orchard" hit is drawn in the identical `--indigo-wash` + `--indigo-line` pair on top of it. The wash cancels against the band, so the top result's hit is visibly weaker than the two below it. The overloaded token is not only a taste point; it measurably degrades the search room's most important ink.

WHY THE STANDARD KILL SHOTS DO NOT LAND. Not covered by the measured gate: the palette lock passes because indigo is in the palette and contrast passes on an 8% wash. Not covered by the behaviour gate: notes-interaction-check.mjs line 153 asserts only that exactly one `.idxRow[data-cursor]` exists after the arrow walk, and at that point focus IS inside the index, so the fix keeps both the attribute and the paint. Not a constraint breach: it removes an indigo job rather than adding a hue, changes no weight, touches no architecture. Not taste-without-argument: the wrong-row emphasis at the signature moment is a composition fact I reproduced.

ONE OVERSTATEMENT, WHICH DOES NOT SAVE IT. "The eye's first stop inside the paper is a cursor, not the field they are meant to write in" is not right. The capture field carries its own indigo ring and is far the larger object, so the band is the second indigo thing on the sheet, not the first. And at load the cursor happens to sit on the newest note, so the rest state alone is defensible. The defect is the post-keep divergence and the token overload, not the resting glance. Confirmed on that narrower, provable ground.
  [sharpenedFix] Two changes, and drop the `--wash` fallback the finding proposed.

1. Scope the paint to focus: `.index:focus-within .idxRow[data-cursor] { background: var(--indigo-wash); box-shadow: inset 0 0 0 1px var(--indigo-line); }`, and move the `::before` rule-suppression (line 538) and the `.idxMark i` halo (line 545) inside the same guard so the hairline and the dot halo do not go missing on a row with no visible band. Leave the roving tabindex, the `data-cursor` attribute and moveCursor() exactly as they are, so interaction-check line 153 still passes.

2. Give the unfocused cursor NO paint at all rather than `var(--wash)`. `--wash` is already `.idxRow:hover` and `.idxRow[data-open]`; a third meaning on the same tint makes a resting row read as hovered and an unfocused cursor indistinguishable from the note currently on the desk. Nothing else in the file paints an unfocused keyboard position, and it does not need one.

3. On keep, move the cursor to the row that arrived, so the emphasis and the arrival flash are the same row: set `cursorId = note.id` in keepDraft() beside `arriving = note.id` (notebook.js:287), and `cursorId = made[0].id` beside `arriving = made.length ? made[0].id : null` in the readback path (line 1891). Guard both with `if (!document.querySelector('.index')?.contains(document.activeElement))` so a keyboard user mid-walk in the pile does not have their place stolen by a capture.
======================================================================
UI composition | head-prints-the-same-phrase-twice | cost 0.2
  [element] .headNext span ("Saturday 18 July, in 2 days, 1 still to decide", right edge 817) and .chip ("8 still to decide", left edge 830) — 13px apart in .head at 1440
  [problem] The head ends one sentence with "1 still to decide" and starts the next object with "8 still to decide", thirteen pixels later, in the same baseline. Two different numbers of the same phrase, at two different scopes (this wedding vs the whole notebook), with nothing in the composition saying which is which — at a glance it reads as one broken string. And the chip is full ink on white, the heaviest object anywhere above the sheet, so it wins the first fixation: the eye's opening move in the product is a count it has to disambiguate, not the couple and the date the head was rebuilt to lead with. The group rule for Mara & Finn already prints "1 still to decide" on the right of its own row, so the head's copy of it is the third statement of the same fact on one screen.
  [fix] Let one object own the count. Drop the trailing ", 1 still to decide" clause from .headNext (the group rule already carries it at the row it belongs to), keep the chip as the notebook-wide figure, and open the gap to at least 24px (`.head { gap: 13px } .chip { margin-left: 11px }`) so the chip reads as a separate object rather than the tail of the sentence beside it. If both counts must stay, the chip has to name its scope in its own words so the two can never be read as one.
  [verdict] CONFIRMED, but only its core and not its measurements or its fix.

What I verified live (chromium, ?state=notebook&v=locked, 1440x960):
- .headNext span renders "Saturday 18 July, in 2 days, 1 still to decide" (13px/400/ink 0.72), .chip renders "8 still to decide" (12px/600, white on #111). Box edges 817 -> 830.
- Source: notebook.js:722 appends the clause from `work().filter(n => n.pending && n.aboutKey === "mara-finn").length`; notebook.js:732 prints `counts().pending`, which at notebook.js:150-155 is every pending note in the notebook. So 8 CONTAINS the 1. Two nested counts of one four-word phrase in one 62px row, no scope word on either side of the larger one.
- Same at 1080, 1280, 1920 and in quiet/studio/press. "still to decide" is on screen seven times at 1440 (head span, chip, five .idxDayTail rules), so this is the product's fixed lexicon for the state, not a slip - which is exactly why two different numbers of it sitting adjacent is a collision rather than a variation.

Where the finding is wrong, and these parts should not be actioned:
1. "Thirteen pixels later" is a box measurement that counts the chip's own 11px left padding as gap. Range-measured glyph to glyph, "decide" ends at 816.5 and "8" starts at 840.5: 24.0px, with a filled black pill edge between them. The fix asks to "open the gap to at least 24px" - that is already the rendered distance. The spacing half of the fix is a no-op.
2. "Nothing in the composition saying which is which" is not accurate visually: the 1 is a subordinate clause of a sentence whose subject is "Mara & Finn" set in the darkest, heaviest type in the head (15px/600/#111), and the chip is inverted ink and a control. Grammar, weight, fill and interactivity all separate them.
3. "The chip wins the first fixation" is asserted, not shown. It is the fifth object left to right, 109x26 at 12px, after four ink objects.
4. The primary remedy - drop ", 1 still to decide" - makes the artifact worse and reverses a deliberate round-5 change (the head was rebuilt to lead with the wedding AND how many of its notes are undecided). Delete the scoped count and the only count left beside "Mara & Finn, Saturday 18 July, in 2 days" is the unscoped notebook-wide 8, which then reads as the wedding's. That worsens the exact ambiguity the finding names.

What survives, and why it is real at 9.5 rather than taste: the pill boundary that defends the visual reading does not exist in the accessible name. The chip's label is "8 still to decide. Go through the notes waiting on a decision." Read in order, a screen reader gives "...in 2 days, 1 still to decide" immediately followed by "8 still to decide" with nothing naming either scope and no object boundary at all. In audio the two counts are indistinguishable and one contains the other. That is a fact stated in two grammars on one row, which is on the standing checklist, and it is not covered by notes-interaction-check.mjs: line 96 only asserts /\d+ still to decide/ matches the chip, and the head/index count agreement assertion checks the chip against the index, never the chip against the head clause beside it.
  [sharpenedFix] Keep the head clause - it is the round-5 lede and the scoped count is the correct one to keep. Fix the unscoped object instead, in two moves, both cheap and neither touching architecture, palette or weights:

1. The chip names its scope in its own words, in the visible label and in the accessible name, so the two counts can never be read or heard as one. It is the only one of the two with no subject, and 8 contains the 1, so the scope word has to be the notebook, not a repeat of the wedding. On-voice candidate from the product's existing nouns: "8 in the notebook still to decide" (aria: "8 notes in the notebook still to decide. Go through the notes waiting on a decision."). Exact wording is notes-copy.ts's call; the requirement is that the chip states its scope.

2. Put a .headRule hairline before the chip, as already exists between the wordmark and the workspace name, so the chip reads as a new object rather than the tail of the sentence beside it. Change no spacing: the strings are already 24px apart glyph to glyph and the fix's own target is met.

Then add the assertion the gate is missing: the head's scoped clause and the chip may never render the same phrase without the chip naming its scope - i.e. assert the chip's text and aria-label both contain the scope noun whenever .headNext also prints a count.
======================================================================
Typography | lede-budget-runs-to-88-characters | cost 7
  [element] `.idxText b` — `ledeOf()` at notebook.js:210-216; every room, every state that renders the index
  [problem] The 600-weight lede has two budgets and neither works. If a note's first sentence ends inside 88 characters the whole sentence goes semibold; if it does not, an arbitrary 46-character prefix goes semibold. Measured live at 1440 in ?state=notebook: 67% of the index's 1,498 rendered characters are set at 600, the mean bold run is 66% of row width, and four of fourteen rows carry a 100-plus-character semibold run with no 400 text visible at all (105, 105, 104, 80 chars). At 390 it collapses completely — three of six visible rows show zero regular type inside the two-line clamp, with bold runs of 105, 196 and 104 characters against a ~56-character window, so on the primary capture device the 400/600 pairing does not exist. The 46-character path is worse in kind: a note I typed live rendered as 'ring the florist about the ceremony arch **she** | said she would confirm the width by friday' — semibold ending on a dangling pronoun, creating an emphasis unit that is not a unit. The only hierarchy device this product has inside a person's own writing is either spent on everything or applied to a fragment. Bear and Things bold a title; this bolds a paragraph prefix.
  [fix] In `ledeOf`, collapse the two branches into one earned budget. Find the first terminal stop; return it only if it is ≤48 characters AND leaves a non-empty remainder; otherwise return `null`. Delete the `lastIndexOf(" ", 46)` prefix branch entirely so a lede can never cut mid-clause. Rows with no lede render whole at 400 — the `note.lede === false` path already exists at notebook.js:940 and 1043 and needs no change. In the trim loop at notebook.js:646-660, drop the `head.pop()` lede-shortening stage for the same reason: a shortened lede is a fragment. Expected result: index bold share falls from 67% to roughly 25-30%, every semibold run is a complete clause, and the phone regains a light texture.
  [verdict] CONFIRMED on measurement, with the proposed remedy refuted. I drove the file and counted rendered characters. At 1440 ?state=notebook&v=locked the index renders 1,498 characters of which 1,004 are set at 600 — 67.0%, matching the finding exactly, including the four rows at 105/105/104/80 bold with 0/0/7/22 regular. The finding actually understates it: at 768 the share is 97.2%, ten of fourteen rows carry no 400 text at all, and rows 3, 11 and 13 are bold fragments ending in an ellipsis because the trim loop's head.pop() stage at notebook.js:646-660 shortens the lede itself. At 390, measuring only characters rendered inside the two-line clamp (Range rects, not DOM text), 81.8% of visible characters are 600 and 2 of 7 first-screen rows show zero regular — the finding's per-row counts (three of six, a 196-char run) are DOM figures rather than visible ones, so those specific numbers are loose, but the charge holds and holds harder than claimed. Typing live reproduced the dangling-pronoun case verbatim: bold ran to "ring the florist about the ceremony arch she". So a two-weight system is spending its only emphasis device on two thirds of the index at desktop and effectively all of it at tablet, which is not a hierarchy; Bear and Things bold a title and leave the body regular. This is not covered by the measured baseline (the audit gates weights 400/600 exist, not how much of the page is 600) and not covered by the fixed list, whose claim that the lede is "not a search for a full stop" is contradicted by the still-live 88-character terminal-stop branch at notebook.js:211. What I refute is the fix. I implemented the proposed ledeOf against the real fixture bodies: bold share falls to 1.5%, not the predicted 25-30%. Thirteen of fourteen notes lose their lede outright, and every live three-second capture returns null because it contains no terminal stop, so the 400/600 pairing would stop existing and hierarchy would again be decided by punctuation — the precise defect the comment at notebook.js:204-209 records an earlier round fixing. Deleting the word-safe branch trades a lede that is too long for no lede at all.
  [sharpenedFix] Keep a lede on every note; make it short and make it a unit. In ledeOf: (1) accept a terminal stop only if it is <=48 characters AND leaves a non-empty remainder; (2) otherwise prefer the last clause boundary — comma, colon, semicolon, en/em dash followed by a space — within the first 41 characters; (3) otherwise fall back to the last word boundary at 34, not 46; (4) then step back over a trailing weak word (article, pronoun, preposition, conjunction, auxiliary) so a lede can never end on "she", "the", "before"; (5) return null unless at least three words of remainder survive. I ran this over the live fixture: bold share falls from 67% to 21.0%, every lede is a readable unit ("Bar restock: tonic running low,", "Quote from today that I want to keep:", "Teacher onboarding,"), and the live capture keeps a lede ("ring the florist") instead of losing hierarchy. Separately, in trimRows at notebook.js:646-660 the head.pop() stage is what produces the 97.2% tablet reading: replace it, when lede plus one word of remainder will not fit, with dropping to the existing no-lede path (render the row whole at 400) rather than shortening the lede into an ellipsised fragment. That fixes the tablet and honours the finding's own principle that a shortened lede is a fragment, without deleting the budget.
======================================================================
Typography | undo-strip-paints-over-the-writing | cost 4
  [element] `.undo` (master.css:599) — notebook state, for 30 seconds after every Ctrl+Enter, 1440x960
  [problem] The confirmation that a note is safe covers a note. Measured live after a capture: the strip occupies x 642-880, y 818-864, and the row 'Follow up with the motion designer about the venue film. She needs the final music choice…' sits at y 829 — 238px of a person's own sentence is blacked out for the full undo window. The comment directly above the rule asserts the opposite: 'Outside the pile's own scroll region, so it can never land on a person's sentences.' Moving it out of the scroll region stopped it scrolling with the rows, not overlapping them; the index's `padding-bottom: 132px` clears the dock and only takes effect at scroll-end, so at scroll-top a row is always underneath. This is the standing checklist's 'controls painting over content', unfixed, in the exact moment the whole direction exists for.
  [fix] Shorten the pile while the strip is live instead of floating over it. Add one rule: `.sheet[data-undo] .indexWrap { padding-bottom: 58px; }` and set/clear `data-undo` on the sheet element in the same place the strip is shown and hidden. Because `.index` is `flex: 1` inside `.indexWrap`, its scroll box shrinks by 58px, so the last visible row ends above the strip's band at every scroll position rather than only at the end. Leave `.undo` at `bottom: 78px`. Then delete the comment claim at master.css:596-598, which the shipped build disproves.
  [verdict] Confirmed live, not just from frames. At 1440x960 in ?state=notebook&v=locked, after typing and Ctrl+Enter, .undo measures x 642-880 / y 818-864 (z-index 5, offsetParent .sheet) and the .index clip box runs to y 942, so rows paint through the strip's band. The row 'Follow up with the motion designer about the venue film…' sits at y 819-859, x 251-1271. Range-walking its text nodes against the strip rect returns the exact covered glyphs: 'ue film. She needs the final music ch' — 36 characters, 238px of a person's own sentence, blacked out by an opaque var(--ink) pill for the full 30s window. The screenshot confirms it visually, and the note the strip is about ('Ask the florist…') is at the top of the pile, nowhere near it. At scroll-mid a different row ('Heating: orchard room…') is under it; only at scroll-end is the band clear, exactly because padding-bottom:132px lives inside the scroll box. The comment at master.css:596-598 ('Outside the pile's own scroll region, so it can never land on a person's sentences') is disproven by the rule directly beneath it — an earlier round fixed only the scrolling-with-rows half. Refutations tried and failed: not covered by the measured baseline (notes-interaction-check.mjs carries no occlusion/geometry assertion; notes-audit covers palette/type/contrast/targets/radii/motion); not the locked architecture (the dock is locked chrome, a transient toast is not, and the build's own comment states non-occlusion as the intent); not taste — it is the standing checklist's 'controls painting over content', in the keep moment the direction exists for. Breaks no constraint: no colour, no weight, no architecture change.
  [sharpenedFix] Keep the mechanism, fix the number, and drop the conditional. 58px is wrong: .index bottom is 942 and the strip top is 818, so clearing the band needs 124px — at 58px the box still ends at 884 and rows still show under the strip. Toggling on data-undo also reflows the pile twice per capture. Instead move the existing clearance from inside the scroll box to outside it, permanently: `.indexWrap { padding-bottom: 132px }` and `.index { padding-bottom: 0 }`, mirrored per breakpoint (128px at the 1063 query, 148px at 1069, 22px at 1165). Verified by injection at 1440x960: index bottom becomes 810, zero visible rows under the strip at scroll top / mid / end, scrollable distance identical at 352px both ways (so no pile height is lost), no layout jump when the strip appears or expires, and it additionally stops the floating dock cutting the 'Bar restock' row at scroll-top — a second occlusion present in the shipped build. Leave .undo at bottom:78px, and delete the master.css:596-598 comment claim.
======================================================================
Interaction and states | keyboard-cannot-pick | cost 5
  [element] .readBody — pickedRange() / document selectionchange; [data-act="peel"]
  [problem] The second promise of the product is reachable only with a pointer. pickedRange() reads document.getSelection() inside .readBody, and .readBody is neither focusable nor contenteditable, so shift+ArrowRight eight times leaves getSelection().toString() empty and .pickBar at 0. Only 3 of 14 fixture notes carry a fallback pick, so for the other 11 the desk's primary refuses with "Nothing picked yet. Pick the words in the note that should become the task." — an instruction a keyboard or screen-reader user physically cannot follow. The hand degrades gracefully on the same intent (T opens the peel with an empty field and says "Write what the task should say, or pick words in the note"); the desk blocks outright. One product, two grammars for the same act, and the sealed edge is closed to anyone not holding a mouse.
  [fix] Give the read note a keyboard pick: mark sentence boundaries in .readBody at render, make each a roving tabbable span inside the single readBody tab stop, and let ArrowDown/ArrowUp move a drawn pick between sentences with Space toggling it into the selection (state as fill, not colour), announcing "N words picked" through the existing say(). Then make the desk's refusal path match the hand's: open the peel with an empty .peelField rather than refusing, so the primary never dead-ends.
  [verdict] Verified live, and every load-bearing claim survived. .readBody has no tabindex anywhere in notebook.js (grep returns only the index's roving stop at :1040 and the dark-room query at :1996). Driving the file: Tab reaches .idxRow in 6 presses, ArrowDown+Enter opens the desk with focus on .act; ten Shift+ArrowRight presses leave getSelection().toString() empty and .pickBar at 0. Forcing focus onto .readBody and repeating gives the same result. No undocumented pick key exists (p/w/s/Space all leave .pickBar and .peel at 0). Walking all 14 index rows and pressing the desk primary: peel opened on 3, refused on 11 — exactly the split claimed; data.js carries pick: on three seeds (:109, :168, :189) and startPeel (:502) is o.words || picked || note.pick with an early return and a 4.2s nudge. The asymmetry is in the source: decide("task") at :373-386 opens the peel with pickedWords null and says "Nothing picked yet. Write what the task should say, or pick words in the note," while startPeel refuses outright. Neither gate covers this: notes-interaction-check.mjs:1026-1034 builds the pick with page.evaluate + document.createRange(), a synthetic selection that proves the pick bar renders once a selection exists but asserts nothing about a keyboard creating one; the keyboard block at :1329 covers the index tab stop only; the measured audit covers palette, type, contrast, targets, radii, motion. In scope and constraint-safe: roving tabindex, a mark drawn by fill and density, and reuse of the existing say() line introduce no hue, no weight, no architecture change, and no new copy. The only counter-route is OS-level caret browsing (F7) or an NVDA browse-mode selection, which is a platform feature and not an affordance a venue operator will discover. This is a WCAG 2.1.1 failure sitting on the product's stated second promise, and at the Linear/Things bar a primary whose label reads "Pick the words, then send" must be followable without a mouse.
  [sharpenedFix] Two changes, both in the file's existing grammar. (1) Give the desk one tab stop and a drawn keyboard pick. Add tabindex="0" to .readBody, keeping the deskOf label as its accessible name so the desk gains exactly one stop and the index's single-stop discipline is untouched. In the same escaped-string pass that already inserts .lede and .pick (notebook.js:938-942), split note.body on sentence boundaries and wrap each run in <span class="sent" data-i="n"> — no new escaping path, so the bracket-safety already gated stays gated. Inside .readBody: ArrowDown/ArrowUp move a drawn sentence cursor, Space toggles the cursored sentence into the pick, Shift+ArrowDown extends across sentences, Escape clears. Distinguish cursor from picked by fill and density only — the cursor a hairline ink outline, the picked run the existing .pick wash — so the three-colour lock holds and state is never carried by hue. Route the result through offerPick() rather than a parallel path, so the .pickBar, the primary's label swap to "Send to Tasks", and the drawn mark all come for free, and announce with the string offerPick already uses ("N words picked. Begin to make them a task."), so no new copy enters the product. (2) Make the desk's refusal match the hand's invitation. In startPeel (:500-511), delete the early return and the 4.2s nudge; when no words are picked, open the peel with an empty .peelField, focus it, and say the line decide("task") already ships verbatim: "Nothing picked yet. Write what the task should say, or pick words in the note." One grammar for one act across both surfaces, and the primary never dead-ends. Gate both: extend notes-interaction-check.mjs with a real keyboard pick — Tab to .readBody, ArrowDown, Space, assert .pickBar count 1 and .readBody .pick count 1 with no page.evaluate selection anywhere — and assert that pressing the desk primary opens a .peel on all 14 notes, not 3. The existing synthetic-Range assertion at :1026 should stay but be renamed so it never again reads as proof of a keyboard route.
======================================================================
Interaction and states | refusal-drops-focus | cost 4
  [element] startPeel() early-return branch when words is falsy — [data-act="peel"] on a note with no pick
  [problem] Pressing the read note's own primary with nothing picked repaints and drops focus to <body> — verified for both a click and an Enter press. Every other repaint in this file restores place through refocus; this branch sets none, so a keyboard user who presses the note's main action is dumped to the top of the document and has to Tab back through the filing chip, both verbs, both group buttons and the index to reach the note again. It is the exact defect the standing checklist names (repaints that annihilate focus), and it lands on the one press most likely to be a first-timer's first attempt at the seam. The replacement instruction then self-destructs: the .nudge is removed on a 4200ms timer whether or not it has been read, and after it goes the button still reads "Pick the words, then send" with no route to what that means.
  [fix] Set refocus = { kind: "act", sel: '[data-act="peel"]' } before the paint() in that early return so the press returns focus to the control that was pressed, and drop the 4200ms setTimeout — clear the nudge on the next pick or the next paint that changes state, not on a clock.
  [verdict] Confirmed in source and by driving the file. notebook.js:503-513 is the only action path in the file that repaints without setting `refocus`; paint() (:1633-1660) restores focus only from `refocus` or from a caret path guarded to TEXTAREA/INPUT, so a pressed button is unrecoverable. Driven in Chromium at 1440x960, ?state=notebook, first index row opened (label reads "Pick the words, then send", and 11 of 14 seeded notes carry pick: null per data.js): both Enter and click on [data-act="peel"] leave document.activeElement === BODY, and the button is tab stop 10 of 34, so recovery is 11 Tabs. The 4200ms timer is a second, worse instance of the same defect: with focus deliberately parked on [data-act="close"] and nothing touched, the timer's paint() fires with refocus null and dumps focus to BODY again. After it fires the nudge is gone while the button still reads "Pick the words, then send" with no route to what picking means. Not covered by the behaviour gate: notes-interaction-check.mjs contains no nudge/"Nothing picked" assertion and none of its activeElement checks reach this branch. The fix follows the file's own established pattern (cancelPeel at :530 uses the identical selector) and breaks nothing, since nudge is already cleared on the next pick (:490), on a successful peel (:514) and on the close/open paths (:579, :590). The adjacent hand route d-task goes through decide("task") (:1832, :370-386), which handles no-pick separately and is correctly out of scope.
  [sharpenedFix] In the words-falsy early return of startPeel() (notebook.js:503-513) set `refocus = { kind: "act", sel: '[data-act="peel"]' }` before paint(), mirroring cancelPeel at :530, so the press returns focus to the control that was pressed on both the desk and the phone sheet. Delete the 4200ms setTimeout entirely rather than shortening it: its repaint is itself a focus-annihilating repaint (verified — focus parked on "Put it back" is dumped to body 4.2s later with no user input), and the nudge is already cleared on the next pick (:490), on a successful peel (:514) and on close/open (:579, :590), so the instruction dies when it has been acted on instead of on a clock. While the nudge stands, keep it and the button label telling the same story rather than letting the sentence vanish and leave "Pick the words, then send" pointing at nothing.
======================================================================
Interaction and states | search-cursor-is-inert | cost 3
  [element] #q with .idxRow[data-cursor] painted behind it
  [problem] While the caret is in search, the index still paints its indigo keyboard cursor on the first result (rgba(79,70,229,0.08) plus an indigo ring) — the same mark that means "press Enter to open this" everywhere else in the product. From #q it means nothing: ArrowDown does not move it, ArrowUp does not move it, and Enter opens nothing (.readBody stays 0) with no announcement at all. A person types three letters, sees the note they want highlighted, presses Enter and the product does not react. The fastest path in a capture product — type, arrow, Enter — is drawn and not wired; the real route is two Tab presses past the Esc button.
  [fix] Bind ArrowDown/ArrowUp in the #q keydown to moveCursor(±1) with preventDefault so the drawn cursor walks the filtered rows, and bind Enter in #q to open the cursor row (same path as the index's Enter), announcing through say() as opening does elsewhere. If that is not wired, suppress data-cursor while query is non-empty so no row claims a selection it cannot honour.
  [verdict] Verified live in Playwright at ?state=search. With the caret in #q and the query "mar" filtering to 2 rows, .idxRow[data-cursor] is painted on the first result at background rgba(79,70,229,0.08) with box-shadow rgba(79,70,229,0.32) 0 0 0 1px inset — the same mark master.css:536-537 documents as "the row a keyboard is on". From #q it honours nothing: ArrowDown and ArrowUp leave cursorIndex at 0 with an empty live region, and Enter leaves .readBody at 0, focus still on #q, and announces nothing. Cause is in source: notebook.js:2008 `if (typing) return;` sits above the ArrowDown/ArrowUp/Enter block at 2027-2040, and the file has exactly one keydown listener (2007), so nothing else answers those keys; the `input` branch at 1687 only assigns query and repaints. Worse, indexOf() at 1123 re-seeds cursorId onto the first filtered row on every search repaint, so the inert mark actively tracks the query. The real route to the highlighted row is two Tab presses (measured: tab 1 = .esc "Esc", tab 2 = .idxRow). This is not a restatement of the measured baseline: notes-interaction-check.mjs's search block (lines 243-274) asserts filtering, caret survival, mark highlighting, the no-hit exit and the two Escapes, and its only arrow-walk assertions (143-156) run with focus already in the index — no assertion presses an arrow or Enter from inside #q. It is the standing-checklist defect "keyboard models advertised but not implemented", in the one flow where type-then-Enter is table stakes at the Linear/Things bar, and it introduces no colour, no weight and no architectural change to fix.
  [sharpenedFix] Make search a combobox, not a focus move. In the single keydown handler, above `if (typing) return;` (notebook.js:2008), add a branch for `state === "search" && e.target.id === "q"`: ArrowDown/ArrowUp call a walk that sets cursorId along walkOrder() and say()s "n of m, {title}" but keeps `refocus = { kind: "field", sel: "#q" }` — do NOT call moveCursor() as written, because it sets `refocus = { kind: "row", id }` and would yank the caret out of the field mid-query, ending the person's typing. preventDefault so the arrows do not jump the caret to the ends of the input. Enter (no modifier) calls openNote(cursorId) — the same path the row's own Enter takes, with the same "Open. {title}" announcement — and does nothing when visible() is empty, so a no-hit query still dead-ends only into the existing emptyMove exits. Wire the field as the combobox that the paint already implies: role="combobox" with aria-expanded and aria-controls on #q pointing at the index list, aria-activedescendant set to the cursor row's id, and an id on each .idxRow, so the announcement is the real one a screen reader makes rather than a second story told through the live region. Keep the row's tabindex="0" so the existing single tab stop and its arrow walk are untouched. If the walk is not wired this round, the fallback is narrower than "suppress data-cursor": suppress only the paint (`data-cursor`) while state === "search", leaving the tabindex="0" seeding intact so the index keeps its tab stop, and let :focus-visible mark the row once Tab actually reaches it.
======================================================================
UX and information design | picking-has-no-keyboard-route | cost 4
  [element] .readBody / .handBody, notebook.js:2040-2048 (selectionchange) and :364-388 (d-task)
  [problem] The product's second promise — only the exact words a person picks ever cross — is a mouse-drag gesture and nothing else. `.readBody` and `.handBody` carry no tabindex and no contenteditable; I focused the open note, pressed Shift+ArrowRight twelve times and `getSelection()` stayed empty and no `.pickBar` appeared. The source comment at :2040 states "Picking is a pointer gesture and a keyboard one, so both are watched" — that is a keyboard model advertised and not implemented. The consequence is worse than an access gap: in the hand, pressing T with nothing picked opens the peel on an empty free-text field ("What should the task say?"), so the only keyboard path across the seam is a compose box in which a person can type words that appear nowhere in the note, sitting directly under the sentence "Tasks only ever receives the exact words you pick". A venue operator on a keyboard is told the edge is sealed and handed an unsealed one.
  [fix] Make the note body a real pick surface. Give `.readBody`/`.handBody` `tabindex="0"`, `role="group"` and the accessible name "The note. Pick the words that should cross."; render each sentence inside it as a `<button class="sentence">` walked with ArrowLeft/ArrowRight, extended with Shift+Arrow, and toggled into the pick with Space, writing the joined text into `picked` through the existing `offerPick()` so pointer and keyboard converge on one state and the existing `.pickBar` ("n words picked. Send to Tasks will use exactly these.") reports both identically. Then delete the blank-peel fallback in the `d-task` branch (notebook.js:375-376) so no room can open the seam from nothing. Correct the false comment at :2040.
  [verdict] Confirmed on the live file. `.readBody`/`.handBody` carry tabindex=null, role=null, contenteditable=null; tab-walking all 20 stops and pressing Shift+ArrowRight at each left getSelection() empty and .pickBar at 0 every time, while a real mouse drag raises "9 words picked. Send to Tasks will use exactly these." and flips the primary to "Send to Tasks". So the product's second promise is pointer-only, and the source comment at notebook.js:2040 ("Picking is a pointer gesture and a keyboard one") states a model the build does not have — exactly the standing-checklist item "keyboard models advertised but not implemented". The measured baseline does not cover it: notes-interaction-check.mjs:1021-1037 builds the selection programmatically with range.selectNodeContents, so no assertion touches a keyboard pick. Two parts of the finding are wrong and are stripped from the verdict. First, the boundary copy is "Tasks only ever receives the exact words you pick and check below" — the finding truncates it at "pick"; the wording field IS the check, and it is equally free-text for pointer users (verified in ?state=seam, the seeded field edits freely), so the "keyboard users are handed an unsealed edge" escalation is false; this is a parity gap, not a broken seal. Second, deleting the blank-peel fallback at :375-376 would strip the hand of its only keyboard route to a task and would break a gated assertion (notes-interaction-check.mjs:182-184 asserts .peelField opens empty so nothing claims words nobody picked) — that half of the proposed fix makes the work worse and is discarded.
  [sharpenedFix] Give the note body a keyboard pick that converges on the existing state, and change nothing about the seam's copy or the hand's blank peel. On `.readBody` and `.handBody` set `tabindex="0"` with an accessible name that states the model in the product's own voice ("The note. Use the arrow keys to pick the words that should cross."). On focus, place a drawn word cursor on the first word; ArrowLeft/ArrowRight move it, Shift+Arrow extends it, Space commits the run and Escape clears it — build a real Range over the text nodes and feed the joined text through the existing `offerPick()` so pointer and keyboard write the same `picked`, draw the same `.pick` mark, and raise the same `.pickBar` with the identical "n words picked. Send to Tasks will use exactly these." sentence and the same live-region announcement. Do not restructure the prose into `<button class="sentence">` elements: that risks the gated "a person's own words are set identically everywhere" leading/type check and the hit-target ladder for no gain. Keep the d-task blank peel and its "Nothing picked yet. Write what the task should say, or pick words in the note." announcement exactly as they are. Rewrite the comment at notebook.js:2040 to describe what selectionchange actually watches. Add an assertion to notes-interaction-check.mjs that reaches a pick using keys only, with no page.evaluate on the selection.
======================================================================
UX and information design | spoken-words-printed-twice | cost 3
  [element] ?state=readback, the "WHAT YOU SAID, ONCE" block above the piece cards
  [problem] The transcript is printed in full in a grey block, and then the identical two sentences are printed again immediately below it as the two editable cards, word for word, with no difference between the two renderings. The heading "WHAT YOU SAID, ONCE" is doing rhetorical work to excuse a duplication it is committing on the same screen. A stranger reads the same two sentences twice and has to deduce which copy is live. The product already solved exactly this problem at the seam, where the picked sentence is only printed beside the wording once the wording has been edited away from it — that rule is simply not applied here. The same duplication pattern recurs twice more: the head prints "…, 1 still to decide" (group-scoped) immediately beside a pill reading "8 still to decide" (notebook-scoped) in the same words with no scope on either, and in ?state=search the sheet holds the query field while the dock below still shows an empty field labelled "Search everything you wrote · Ctrl K".
  [fix] Apply the seam's own rule to the read-back: render the transcript block only when `pieces.join(" ") !== N.speech.transcript`, i.e. once the person has edited a piece away from what was heard; until then show the piece cards alone and move the fact into their heading ("What you said, split into 2 notes"). Scope the two head counts so the phrase is never repeated — the group lede keeps "1 still to decide" and the pill becomes "8 across all your notes", or the pill stands down whenever the lede already carries a count. In the search state, replace the dock's search field with the result count and the way out, so only one field on screen is ever named search.
  [verdict] I tried to kill this and could not. All three instances verified live in the file, not inferred from frames.

READ-BACK. `data.js:363` defines `speech.transcript` and `speech.separated`; `separated.join(" ")` is byte-identical to `transcript`. `notebook.js:65` sets `pieces = N.speech.separated.slice()`, and `STATES.readback` (notebook.js:1348-1374) prints `.saidWas` with the full transcript at line 1351 and then the same two sentences as `.pieceField` textareas at 1353-1360. Driven: `.saidWas` innerText minus its label === `pieces.join(" ")` returns true. Both renderings are 17px Geist 400; the block is ink-0.72 on an ink-0.035 tint, 629x107px, occupying the top third of the desk in `locked-readback--1440x960.png`. Twenty-six words printed twice under a heading that says "once". The refutation I expected to work — that the block shows the *operation*, one utterance becoming two notes — fails, because the split is already stated twice more on the same sheet: the paper's label is "What came back" and `topFoot` already prints "2 notes, not one" (line 1366). The block adds no fact, only length.

The decisive point is that this build already owns the rule. `peelPanel` at notebook.js:907-908 renders `.peelFrom` only when `taskWording.trim() !== pickedWords.replace(/[.]$/,"")` — the source words are printed beside the wording only once the wording diverges. That is the exact same relationship (machine-produced words vs the person's edit of them) and the exact same suppression. It is applied at the seam and not at the read-back. I edited a piece in Playwright and confirmed `.saidWas` is unconditional: present before the edit, present after.

HEAD COUNTS. Driven at ?state=notebook, `.head` innerText is "…Mara & Finn / Saturday 18 July, in 2 days, 1 still to decide / 8 still to decide". `nextUp()` (line 717-722) counts only `aboutKey === "mara-finn"`; `head()`'s chip (line 732) counts `counts().pending` across the notebook. Same five words, two numbers, neither carrying its scope, 40px apart, in both `locked-notebook--1440x960.png` and the read-back frame. This is not the item on the do-not-restate list — that list credits the head for *adding* the lede; the collision is what the addition (commit 62c53a7) created and nothing yet resolves. The behaviour gate does not catch it: notes-interaction-check.mjs:96 only asserts `/\d+ still to decide/` appears in the head, which any single count satisfies.

SEARCH. At ?state=search the desk carries `<input id="q" aria-label="Search everything you wrote">` (line 1436) while `dock()` line 784 still renders `<button class="dockField" aria-label="Search everything you wrote">…Ctrl K</button>`. Two controls with an identical accessible name on screen at once, one live, one a pointer to the live one — clicking the dock one just moves focus to #q. Weakest of the three, but real, and not covered by the "one name for the search" gate, which polices naming consistency and in fact produced the collision.

Nothing here needs a colour, a weight, or an architecture change. It costs one condition in each of three places.

One honest cost the fix must carry: notes-interaction-check.mjs:303 currently asserts `(await page.locator(".saidWas").count()) === 1` under the label "what was said stays on screen beside what it became". That assertion has to be amended, not deleted — it is the gate for the diverged case.
  [sharpenedFix] Three conditions, and one gate amendment. Do not take the finding's copy rewrite.

1. Read-back. In `STATES.readback` (notebook.js:1348), render the `.saidWas` block only when `pieces.join(" ") !== N.speech.transcript` — the same test `peelPanel` already applies at line 907. At rest the person sees the two editable cards alone, which are already labelled by the paper ("What came back") and already counted by `topFoot` ("2 notes, not one"). The moment any piece is edited or dropped, the block appears, and only then does its heading earn the word "once". Do NOT change the heading to "What you said, split into 2 notes" as proposed — that would be a third statement of a fact `topFoot` and the paper label already carry.

2. Head counts. Scope exactly one of the two so the phrase is never repeated. Cheapest correct version: `head()` (line 726-733) suppresses the chip whenever `nextUp()` already rendered a count, since the lede's count is the one that presses on this person today. If the notebook-wide number must survive, the chip reads "8 across all your notes" and keeps its existing aria-label, which is already scoped ("Go through the notes waiting on a decision").

3. Search. In `dock()` line 784, stand the `.dockField` button down while `state === "search"` — keep the rule, mic, photo and avatar so the dock's shape does not jump — or replace it with the live result count. Do not add "the way out" as the finding proposes: the desk's search bar already carries its own Esc button (line 1437), so that half of the fix would recreate the duplication it is meant to remove.

4. Gate. Amend notes-interaction-check.mjs:303 to assert the conditional relationship in both directions: `.saidWas` count === 0 immediately after `voice-stop`, and === 1 after typing into a `.pieceField`. Add the sibling assertions for the other two: the head renders `/still to decide/` exactly once, and exactly one element on screen carries the accessible name "Search everything you wrote" in every state.
======================================================================
UX and information design | undo-strip-prints-over-the-pile | cost 3
  [element] .undo — master.css:599-604, `position: absolute; bottom: 78px; z-index: 5`
  [problem] Standing-checklist item found: controls painting over content. I typed a note, pressed Ctrl+Enter, and the black "Saved. / Undo Ctrl+Z / for 30s" pill landed squarely on top of an index row, cutting "Follow up with the motion designer about the ven[ue film. She needs the final music] choice…" in half. Nothing reserves its height: the index has no bottom padding for it, so at the exact moment the product delivers its first promise it obliterates a sentence in the pile it just added to, and holds it obliterated for thirty seconds. It also stacks a second floating ink object 78px above the dock, so the foot of the sheet carries two competing black shapes over live text.
  [fix] Reserve the strip instead of floating it over words. Declare `--undo-h: 46px`, set `data-undo` on `.sheet` whenever `undone` is non-null, and add `.sheet[data-undo] #index { padding-bottom: calc(var(--dock-h) + var(--undo-h) + 12px); }` so the pill can only ever land on paper. Keep its `bottom: 78px` and z-index; the change is that no row can occupy that band while it exists.
  [verdict] CONFIRMED by driving the file, not by reading frames. Typed a note, pressed Ctrl+Enter at 1440x960 ?v=locked, screenshotted the true post-capture state (scrollTop 0): the black "Saved. / Undo Ctrl+Z / for 30s" pill lands squarely across a live index row and renders exactly the mutilation the finding quotes — "Follow up with the motion designer about the ven[STRIP]hoice before she can lock…". Measured overlap 40px vertical against a 40px row, 239px horizontal. elementFromPoint under the strip returns real glyphs ("She needs the final music choice before…"), so this is opaque ink over words, held for thirty seconds, at the exact moment the product delivers its first promise. It is the standing-checklist item "controls painting over content" and it is not a 1440 artefact: occlusion reproduces in 20 of 20 combinations across 1920/1440/1180/768/390 and all four rooms locked/quiet/studio/press, covering a different live sentence each time. Not a restatement of the measured baseline: grep of notes-interaction-check.mjs shows its undo assertions cover the strip's label ("Saved."), its stated duration (30s), and its reversibility, but nothing about its geometry; notes-audit.mjs has no occlusion or overlap check at all. The CSS comment at master.css:594-598 claims the strip now sits "outside the pile's own scroll region, so it can never land on a person's sentences" — that claim is false, since leaving the scroll region only stops the strip scrolling with the pile, it does not stop it painting over whichever row occupies the band. TWO CORRECTIONS to the finding, neither of which kills it. First, its rationale is factually wrong that "the index has no bottom padding for it": master.css:470 sets .index padding-bottom 132px, already larger than dock plus strip. Second, and consequently, its proposed fix is close to a no-op — I forced the index to the bottom of its scroll with the strip up and measured zero occlusion, proving the padding already protects the end of the list. The defect lives mid-scroll, where padding-bottom cannot reach. The finding's diagnosis and evidence are right; only its mechanism is wrong, so it needs the sharpened fix below rather than a refutation.
  [sharpenedFix] Do NOT apply the proposed padding-bottom rule — .index already carries 132px (master.css:470) and at the foot of the scroll the strip occludes nothing. Padding grows the content box, so it can only ever protect the last row; the sentence being destroyed is mid-list. The rule would measure as fixed and still read as broken. Reserve the band by shrinking the SCROLL VIEWPORT, not the content: declare --undo-h: 46px, set data-undo on .sheet whenever undone is non-null, and add `.sheet[data-undo] .index { margin-bottom: var(--undo-h); }`. Because .index is `flex: 1; min-height: 0; overflow: auto`, this reduces its clientHeight by 46px, so no row can render in the strip's band at ANY scroll position, which is the guarantee the finding actually wanted. It also satisfies the standing checklist's scroll rule for free: rows do not move, scrollTop is untouched and scrollHeight is unchanged, so the pile is clipped 46px earlier rather than repainted under the reader's eye. Verify with a new assertion in notes-interaction-check.mjs, since nothing there covers geometry today: with the strip up, assert that no .idxRow rect intersects the .undo rect, at scrollTop 0, at mid-scroll and at the bottom of the scroll, across all four rooms and at 1920/1440/1180/768/390 — the same 20-cell matrix that currently fails 20 of 20. The finding's second charge, two competing black shapes at the foot of the sheet, is weaker and should not drive the fix: the dock is permanent chrome and the strip is a 30-second transient, and the phone's "one ink object at the foot" rule is already met elsewhere. Fix the occlusion; leave the two-object composition alone.
======================================================================
UX and information design | one-primary-two-behaviours | cost 3
  [element] [data-act="peel"] in the read view vs [data-act="d-task"] / key T in the hand — notebook.js:498-512 and :364-388
  [problem] The same label, "Pick the words, then send", does two different things in two rooms. In the read view, pressing it with nothing picked refuses: a bordered box with an alert triangle is injected between the person's own words and the buttons, shoving the row down, saying the same instruction the button label already said. In the hand, pressing the identically-labelled control refuses nothing and opens a blank peel. Two further defects ride on the refusal path: focus is thrown to `document.body` (I checked `document.activeElement` after the press — BODY), so a keyboard user who presses the primary loses their place entirely; and the instruction auto-dismisses after 4,200ms with a second repaint, so the only guidance the product ever gives on its most important flow is on a timer and is gone if the person looks away.
  [fix] Route `d-task` through `startPeel(note.id)` so both rooms obey one precondition and one behaviour. In the refusal branch set `refocus = { kind: "act", sel: state === "review" ? '[data-act="d-task"]' : '[data-act="peel"]' }` before `paint()`. Delete the 4200ms `setTimeout` that clears `nudge`; clear it in `offerPick()` (already done) and on closing the note, so the instruction persists until the person acts on it. Drop the `I.alert` glyph from `.nudge` — this is an invitation, not an error, and the icon is the only thing on the surface reading as a warning.
  [verdict] Verified live and in source. The label "Pick the words, then send" is identical at notebook.js:984, :1189 and :1297, and it drives THREE behaviours, not two. (1) Read view on a note with no seed refuses: .nudge injected with the I.alert warning triangle (icons.js:54 - a triangle containing a bar and a dot, i.e. a bang, on a surface whose gate section is titled "the queue never scolds" and whose copy lock forbids exclamation marks), no peel. (2) Read view on index rows 2, 10 and 11 SILENTLY opens the peel seeded with an authored suggestion nobody picked (measured field value "Ask the venue whether the ballroom can be accessed from 8am on the Saturday") with .peelFrom count 0, so the seam does not even print where those words came from - caused by `const words = o.words || picked || note.pick` at :499, sitting under a comment claiming it does not "quietly send a sentence they never chose" and contradicting the hand's own ratified rule at :363. (3) The hand opens an empty peel that asks in its field. Focus loss confirmed by keyboard as well as mouse: focused [data-act="peel"], pressed Enter, document.activeElement.tagName === BODY, because the refusal branch calls paint() with refocus unset. The 4200ms auto-dismiss confirmed: .nudge present at T+0.4s, absent at T+4.5s, second repaint. This is not a restatement of the baseline: notes-interaction-check.mjs:1091 and :1321 assert only the label TEXT and never press the read-view primary with nothing picked, so the ratified claim "every primary states its precondition at rest instead of refusing after the press" is measurably false on the desk. ONE SUB-CLAIM REFUTED: the nudge does not shove the row down. .pickSlot has min-height 54px (master.css:793) and .topFoot top measured 328.28125px identically before and after the press. Also the proposed fix as written is wrong: routing d-task through startPeel would import the `|| note.pick` fallback into the hand and break the gated assertion at check line 184.
  [sharpenedFix] Make it one behaviour by adopting the hand's, which is already the better one and already gated. In startPeel, delete the `|| note.pick` fallback so nothing the person did not pick is ever seeded, and delete the whole refusal branch with its nudge, its I.alert glyph and its 4200ms setTimeout. Both rooms then open the peel unconditionally: with a pick, the field is seeded from exactly those words and .peelFrom prints them; without one, the field opens empty carrying its placeholder "What should the task say?", and the peel's own primary already states the remaining precondition ("Write the wording") while .peelBoundary already asks. Route d-task through that same startPeel(note.id) once the fallback is gone. Result: one label, one precondition, one behaviour across desk, phone sheet and hand; the focus-to-BODY path and the timed instruction both cease to exist rather than being patched. If any refusal branch is kept instead, it must set refocus = { kind: "act", sel: state === "review" ? '[data-act="d-task"]' : '[data-act="peel"]' } before paint(), and the nudge must persist until offerPick() or closing the note clears it. Add gate assertions that press the read-view primary with nothing picked on BOTH a seeded and an unseeded note and require identical outcomes, and that .peelFrom is present whenever .peelField is non-empty at open.
======================================================================
Brand and copy | deferring-is-counted-as-deciding | cost 5
  [element] ?state=review · the undo strip under the hand plus the card counter (button.act "Decide later" → strip + "N of M")
  [problem] Pressing "Decide later" makes the product state a falsehood in the one room built on trust. At rest: "1 of 8" and "8 still to decide. Nothing is decided until you say so, and every decision can be put back." One press of Decide later: "2 of 9" and "7 still to decide. 1 decided just now, and every one of them can be put back." Nothing was decided. The strip contradicts the exact clause it replaced, claims a decision the person explicitly declined to make, and reports the queue shrinking (8→7) while the counter reports it growing (of 8 → of 9). Two presses compound it: "3 of 10 · 6 still to decide. 2 decided just now." A venue manager who defers three cards is told she decided three things. Not clumsy, untrue.
  [fix] Deferral is not a decision. On data-act="decide-later" do not decrement "still to decide" and do not increment "decided just now". The strip after a defer reads: "8 still to decide. That one goes to the back." Reserve "N decided just now, and every one of them can be put back." for keep, send and delete only. Hold the denominator at the true remaining count so the next card reads "1 of 8", not "2 of 9" — a re-queued card is the same card, not a ninth one.
  [verdict] CONFIRMED, and understated. Source: on data-act="d-later" (notebook.js:409-412) the note keeps pending=true so it stays in queue(), queueAt increments, AND it is pushed onto decided[]. Because total = queue().length + decided.length (line 1253), every deferral is counted twice. Driven live at ?state=review: rest reads handOf "1 of 8", pips aria-label "0 of 8 decided", deckNote "8 still to decide. Nothing is decided until you say so, and every decision can be put back."; after one Decide later it reads "2 of 9", "1 of 9 decided", "7 still to decide. 1 decided just now, and every one of them can be put back."; after three it reads "4 of 11", "3 of 11 decided", "5 still to decide. 3 decided just now". The denominator inflates without bound, the remaining count shrinks while nothing left the queue, the strip contradicts the exact clause it replaced, and the accessible name is worse than the visible text (deferred pips render data-done and are announced as decided). The finding also stops short of the endpoint: defer all eight and the desk prints "Everything is decided. 8 notes went through. 0 became tasks and the rest stayed here." while the head in the same frame still reads "1 still to decide" - eight notes the person explicitly declined to decide are reported as gone through, in the one room whose copy is built on "nothing is decided until you say so". Refutations attempted and failed: (a) "deferring is itself a choice" fails because the same paragraph uses decide in two contradictory senses - the note is still to decide after being counted as decided - and "8 still to decide" to "7 still to decide" is flatly false with 8 remaining; (b) the source comment at 1249-1252 justifies decided.length as the denominator, but that reasoning holds only for keep/send/delete, which actually leave queue(); (c) not covered by the measured baseline - notes-interaction-check.mjs:208 asserts only that deckNote matches /\d+ still to decide/, a regex that passes on a wrong number, and no assertion in the file exercises d-later at all; (d) the fix is copy and arithmetic only - no fourth hue, no third weight, no architecture change.
  [sharpenedFix] Deferral must not enter the decided ledger. Record a defer in its own re-queue list (so Ctrl+Z still restores queueAt and order) and leave decided[] to keep, send and delete only. Three invariants follow. (1) The denominator is the fixed size of the hand as the person entered it, so it stays "of 8" no matter how many cards go to the back; today it climbs 8 to 9 to 10 to 11. (2) "N still to decide" must not decrement on a defer - the deferred note is still to decide, by definition - so the deck reads "8 still to decide. That one goes to the back." and the "N decided just now, and every one of them can be put back." clause is reserved for real decisions. (3) The pips aria-label must count only real decisions ("0 of 8 decided" after three defers), and a deferred pip must not carry data-done. Do not simply freeze the numerator: holding "1 of 8" across two consecutive cards reads as stuck. Make the numerator position-in-pass and the pips the decided ledger, or label the line as position rather than as decisions. Finally, the exhausted-hand state must branch: with zero real decisions it cannot say "Everything is decided. 8 notes went through." It should say the hand came round - the notes deferred are still waiting - and offer one first move back into them, and the head's "N still to decide" must agree with it in the same frame.
======================================================================
Brand and copy | privacy-claim-overstates-the-promise | cost 4
  [element] button.headAct[data-act="privacy"] > span, and span.topMeta in the capture footer (notebook.js:110, 742, 872)
  [problem] The sheet makes the privacy promise in two invented, unconditional forms — "Private to you" in the header and "Nobody else can read this" under the capture field — while the product's own conditional promise, loaded into the master as N.copy.privacyLong ("Yours until you send something on"), is rendered nowhere: grep returns 0 uses, as it does for N.copy.emptyBody. The master honours notes-copy.ts verbatim for the seam's boundary sentence and silently substitutes stronger, flatter claims for the standing one. "Nobody else can read this" is a claim the product cannot keep the moment a task crosses into a shared project; the true sentence, better English and better law, sits unused in data.js. The header control is also a button whose entire accessible name is a promise, so a screen reader hears "Private to you, button" with no verb and no second clause.
  [fix] Render the product's own string: the capture footer takes N.copy.privacyLong verbatim ("Yours until you send something on") in place of the literal "Nobody else can read this" at notebook.js:110 and 872. Keep "Private to you" as the header chip but set aria-label="Private to you. Yours until you send something on." so the conditional survives in the accessible name. No literal privacy string anywhere in the master.
  [verdict] I tried to kill this and could not. Every load-bearing claim checks out, and the product source makes the charge worse than the finding puts it.

VERIFIED IN SOURCE. `notebook.js:110` and `:872` emit the literal `<span class="topMeta">Nobody else can read this</span>`. `:742` emits `<button class="headAct" data-act="privacy">` whose only text is `N.copy.privacy` = "Private to you". `:1466` emits a third invented literal ("Nobody else can read what you write here. Notes only sends something on when you pick the words yourself.") in place of `N.copy.emptyBody`. grep over notebook.js returns zero uses of `N.copy.privacyLong` and zero of `N.copy.emptyBody`, both of which are defined in data.js:323 and :325 under a comment that reads "Verbatim from notes-copy.ts... Not rewritten by any direction; a direction may only decide where a string lives." The master breaks its own stated contract in three places.

VERIFIED LIVE (Playwright, 1440x960). ?state=notebook renders topMeta "Nobody else can read this" and the header button with textContent "Private to you" and aria-label null, so the accessible name is exactly "Private to you" — a promise with no verb on an interactive control. ?state=nothing renders the invented emptyBody. Typing into the field leaves the privacy line standing.

THE REAL SOURCE MAKES IT WORSE. src/modules/notes/lib/notes-copy.ts does not merely have one unused conditional string; it has two purpose-built slots the master ignores. `capture.privacyAssurance` = "Private until you send it on" is documented as "Standing line next to the capture label". `capture.headerAssurance` = "Yours until you send something on" is documented as "Standing line in the notebook header". The master invents a literal for each slot and uses neither. "Private to you" appears nowhere in either register of the product. The module's own docstring states the rule the master is violating: "the privacy sentences name what crosses and what does not, in both registers, because that boundary is the promise the venue offer rests on." "Nobody else can read this" and "Private to you" name no boundary at all. The wedding register — the audience in this brief — reads "Only the two of you can see this", so the flat form is not even the register's own voice.

NOT TASTE, NOT ALREADY HANDLED, NOT BASELINE. grep over scripts/design/notes-interaction-check.mjs finds no assertion on privacy copy or on N.copy fidelity, so the 102/276 gates do not cover this. The seam honours handoffBoundary verbatim, which is exactly the asymmetry the finding names: the boundary sentence is respected and the standing sentence is overwritten with something stronger and flatter. It also hits a checklist item head-on — facts stated in two grammars: the header promises unconditional privacy while the seam two clicks away states that Tasks receives the words you pick.

ONE CORRECTION TO THE FIX, WHICH IS WHY I AM SHARPENING RATHER THAN CONFIRMING AS WRITTEN. The proposed mapping is backwards against notes-copy.ts: it moves the header string ("Yours until you send something on") into the capture footer and keeps the invented "Private to you" in the header. The product already assigns one string to each slot. The finding stands; its fix should follow the product rather than reshuffle it.
  [sharpenedFix] Delete all three invented privacy literals and render the product's own slot-correct strings.

1. data.js:322-323 — replace the invented pair with the real keys so the lab data matches notes-copy.ts: privacyAssurance: "Private until you send it on" (generic) and headerAssurance: "Yours until you send something on". Drop "Private to you", which exists in neither register.

2. notebook.js:110 and :872 — the capture footer's idle line takes N.copy.privacyAssurance verbatim ("Private until you send it on"), which is the slot notes-copy.ts documents as "standing line next to the capture label". It is one word longer than the literal it replaces, so the footer's layout is unaffected.

3. notebook.js:742 — the header chip takes N.copy.headerAssurance verbatim ("Yours until you send something on"), its documented slot.

4. notebook.js:1466 — the empty state takes N.copy.emptyBody ("The capture field is ready above."), which offers the one first move the empty-state checklist item asks for, instead of repeating the privacy claim a third time.

5. The accessible-name half still needs answering after the swap, because "Yours until you send something on, button" is a promise with no verb. Either give the control an action name that keeps the sentence — aria-label="What stays private. Yours until you send something on." — or, since data-act="privacy" is deliberately inert here and belongs to another screen, render it as a non-interactive chip so a screen reader hears a statement rather than an unexplained button.

After this, grep for a literal privacy string anywhere in notebook.js should return zero, and that is the assertion worth adding to notes-interaction-check.mjs: every rendered privacy sentence must be identical to a value in the copy object.
======================================================================
Product taste and emotional resonance | voice-handoff-drops-focus | cost 0.4
  [element] ?state=voice → [data-act="voice-stop"] → STATES.readback (notebook.js:1348, 1857)
  [problem] Pressing "Stop and read it back" leaves document.activeElement === document.body — verified true while .pieceField exists and is focusable. The live region says only "Two notes came back from that.": a count, with none of the person's words and no next move. Every other transition in this build places focus deliberately (close button on read, field after keep, peelField after T). The single moment the product's own checklist says must be trustable without looking at the screen — a venue owner who just spoke, phone down, hands full — is the one moment the product lets go of her. She hears a number, has no idea what the two notes say, does not know whether they are already safe, and her next Tab starts from the top of the document.
  [fix] In the readback transition set refocus = { kind: "field", sel: ".pieceField" } and place the caret at the end of the first piece, exactly as keepDraft() already does for the capture field. Change the announcement to name the words and the next move: `Two notes came back. First: Ask the venue whether the ballroom can be accessed from eight in the morning. Edit either one, or keep both.` Cap the spoken preview to the first piece so the region does not read a paragraph.
  [verdict] I tried to kill this and could not. Driven live in chromium against notebook.html?v=locked: click the voice verb (focus correctly lands on [data-act="voice-stop"], as the gate asserts), then click "Stop and read it back" — document.activeElement === document.body, isBody true. The next three Tab presses land on the suite rail tiles ("Notes, the page you are on", "Tasks", "Timeline"), i.e. the top of the document, not the two notes she just spoke. The only live text is the polite .sr region reading exactly "Two notes came back from that." — a count, no words, no statement that nothing is kept yet.

Source confirms it is not an oversight of my reading: notebook.js:1856-1859 is the one branch in the whole action handler that changes state without setting `refocus`. Every neighbour sets one — 262 (.topField/.phoneField after keep), 386/521 (.peelField), 566, 572 (#q), 584 (read close), 594/618 (row), 1732, 1774, 1781, 1818, 1839, 1854 (voice-stop on entry). So the takeover is the one modal in the build that traps focus correctly on the way in and drops it on the way out.

Not covered by the measured baseline. notes-interaction-check.mjs's voice block (lines 699-751) asserts focus lands inside the takeover, that Tab cannot walk out of it, that Shift+Tab works, and that nothing is inert afterwards — then immediately does `field.click()` on .pieceField, which manufactures the focus the product failed to place. Every activeElement assertion in the file (119, 127, 149, 160, 252, 258, 535, 569, 706, 717, 727) is listed; none checks where focus goes after voice-stop. This is the standing checklist's "repaints that annihilate focus" and its Notes-specific third addition, "a voice flow that cannot be trusted without looking at the screen", landing in the same moment.

The finding also understates the defect: the same branch handles voice-cancel, and Escape out of ?state=voice routes through it too. I verified all three — after cancel: BODY; after Escape (state returns to notebook): BODY. Phone at 390 is identical: after stop, BODY, with two .pieceFields on screen. So it is three exits from a modal takeover, all dumping a keyboard or screen-reader user at the document root.

No constraint is threatened: no colour, no weight, no architectural change, the voice disclosure is untouched.

One half of the proposed fix would make things slightly worse, so I have sharpened rather than confirmed it verbatim — see sharpenedFix. Putting the first piece's words into the polite live region AND moving focus into that same field makes a screen reader read the sentence twice in a row: once from the region, once as the textarea's value under its "Note 1 of 2" name. The words should be delivered once, by focus; the region should carry only what focus cannot say.
  [sharpenedFix] Fix all three exits from the takeover, and let focus carry the words rather than duplicating them.

1. notebook.js:1856-1859 — split the branch. On voice-stop set `refocus = { kind: "field", sel: ".pieceField" }` and place the caret at the end of the first piece, exactly as the keep path already does for the capture field (the `caret` path in paint() at 1650-1660 already supports this). On voice-cancel, and on the Escape route that lands in the same branch, return focus to the control that opened the takeover — `refocus = { kind: "act", sel: '[data-act="voice"]' }` — which resolves on both the desk verb and the phone dock affordance (verified present at 390).

2. Announcement: name the state and the next move, not the words, since the focused field will speak the words as its own value. Something like "Two notes came back. Nothing is kept yet. Edit either one, or keep both." Count, safety, next move, in that order, no words repeated. It also closes the second half of the charge — as shipped, nothing anywhere on that screen tells a person who is not looking that the two notes are still unsaved.

3. Add the missing assertions to notes-interaction-check.mjs so this cannot regress: after voice-stop focus is inside .pieceField with the caret at the end; after voice-cancel and after Escape from ?state=voice focus is back on [data-act="voice"]; and at no exit is document.activeElement the body. Note the existing block at 735 masks this today by clicking the field before testing it — that click must move after the focus assertion.
======================================================================
Product taste and emotional resonance | lede-splits-mid-phrase | cost 0.4
  [element] ledeOf(), notebook.js:210 → .idxText <b> in every index row
  [problem] The sentence-boundary search requires whitespace after terminal punctuation, so any single-sentence capture — the modal case for a three-second product — falls through to lastIndexOf(" ", 46), a raw character budget. I typed one real note and the row came back "Ring the marquee company back about the side" in 600 with "panels before Friday." in 400. The 600 weight is the only hierarchy this product has inside a person's own writing, and on real input it lands mid-noun-phrase, breaking the sentence at the exact instant after the flying-words moment hands over. Every seeded fixture has two sentences, which is why five rounds have never seen it. The already-fixed sibling rule (a note with no remainder gets no lede rather than being set entirely semibold) left the inverse case uncovered.
  [fix] In ledeOf(), after the terminal-punctuation search fails, look for a clause boundary inside the 88-char budget in this order: a comma, colon, semicolon or en dash followed by a space; then a space preceding one of "and but so before after because if when". Cut there. If no clause boundary exists inside the budget, return null so the row renders in 400 end to end — the same honesty the no-remainder case already gets. Add the single-sentence case to the interaction gate.
  [verdict] Verified live. ledeOf() (notebook.js:210) falls through to lastIndexOf(" ", 46) for any capture without whitespace-followed terminal punctuation — the modal case for a three-second product. Typing "Confirm with housekeeping that the bridal suite is ready before eleven" and pressing Ctrl+Enter rendered `<b>Confirm with housekeeping that the bridal</b> <span>suite is ready before eleven</span>` in the index row AND on the desk at reading size, splitting a compound noun. Across eight realistic single-sentence captures the cut landed on a determiner or preposition every time ("the good | olives", "the arch in | white", "June 2027 | availability list"). The product's own comment sets the standard it violates: a cut landing mid-word "reads as damage" — this is the same failure one phrase level up, and it is the only place in the build where a person's own words are rendered as though something went wrong. The finding is WRONG on two supporting points, and I correct them: the single-sentence case IS already in the interaction gate (notes-interaction-check.mjs:1286 types "ring the marquee people about the side panels before four on friday"), and it passes by luck — that sentence happens to cut after "panels", a real phrase edge — and because the two assertions only check "not entirely semibold" and "desk lede under 80% of the note", never where the cut lands. So the case is gated and still ships the defect, which strengthens the charge. The proposed fix as written is too loose: an 88-char clause window pushes the same example to 80% of the body, grazing the existing <80% desk invariant and reintroducing the near-whole-row semibold an earlier round removed. Not a restatement of the "600-weight lede is a budget, not a full stop" item — that fix is what created this case; this asks the budget to be phrase-safe as well as word-safe. No constraint touched: no colour, no weight, no architecture.
  [sharpenedFix] In ledeOf(), replace the raw lastIndexOf(" ", 46) with a phrase-safe search inside the SAME budget window (index 12 to 52 — not 88, which breaks the existing <80% desk invariant), in this order: (1) the latest comma, colon, semicolon or en dash followed by a space, cutting after the punctuation; (2) otherwise the latest space preceding one of "and but so before after because if when then with about for", cutting before it so the run ends on a complete phrase; (3) otherwise return null, so row and desk render 400 end to end — the same honesty a body of 52 characters or fewer already gets. Cap the lede at 60% of the body so the existing desk invariant keeps a margin. Then fix the gate rather than adding to it: notes-interaction-check.mjs:1286 currently asserts only that the row is not entirely semibold and that the desk lede is under 80%. Add the assertion that actually catches this — for three single-sentence bodies, either there is no lede, or the lede's final word is not a closed-class word (the, a, an, of, in, at, on, to, for, with, and, or a bare number). That one rule flags every failure observed and would have failed the current build.
======================================================================
Product taste and emotional resonance | readback-prints-your-words-twice | cost 0.3
  [element] .saidWas + .pieces, STATES.readback (notebook.js:1350-1360), ?state=readback
  [problem] The transcript is printed in full under the label "What you said, once", and then printed again in full, verbatim, split across the two editable pieces directly beneath it. Four lines of identical sentences stacked on one card, on the surface whose own heading claims it happened once. This is the duplication defect the seam already paid for (the picked words appear beside the wording only once it has been edited away) and it is unfixed on the more vulnerable surface. It also strands the entire right half of the desk in white while forcing the person to read the same sentences twice to work out that nothing has changed.
  [fix] Drop the .saidWas block. The pieces already carry the person's words verbatim, in the product's own type, and they are editable — they are the truthful record. Replace the heading with a one-line statement of what happened, set in the label ramp: `What came back, in two notes.` If the unsplit transcript must remain reachable, put it behind the existing "Add another"/foot row as a quiet act reading `Read it as one note`, which rejoins the pieces rather than showing a second copy.
  [verdict] Verified in the source, the frame and the driven file. `STATES.readback` (notebook.js:1350-1360) prints `N.speech.transcript` in `.saidWas` and then prints `N.speech.separated` in the two `.pieceField` textareas. Driving `?state=readback&v=locked` returns `fields.join(" ") === transcript` → true: the two editable notes are byte-identical to the paragraph directly above them, with not one word of divergence at first paint. In locked-readback--1440x960.png that is four lines of the same thirty words stacked on one sheet, under a label that reads "What you said, once". The sting is that the heading's own claim is the thing the layout contradicts.

This is not taste asserted as a defect. It is the exact pattern this programme already adjudicated on the sibling surface and fixed: "The seam prints the picked sentence once — the picked words appear beside the wording only once it has been edited away from them." The rule exists, it was paid for, and the more vulnerable surface (machine transcription, legally disclosed, a venue owner checking whether the microphone heard them right) does not follow it.

But the proposed fix is wrong and I am not confirming it. Driving the state further — editing piece 1 to "Ask the venue about 8am access." and dropping piece 2 — leaves `.saidWas` still holding the full original transcript. That is correct and it is load-bearing. On a speech surface the raw transcript is the receipt: it is the only reference against which a person can check that the machine did not mishear, and the only surviving record once they have edited or dropped a piece. Dropping the block, as the finding's headline fix proposes, removes the verification on the one surface in the product where the words were not typed by a human. It would also duplicate `deskOf`'s own sheet label ("What came back") in the replacement heading.

Two secondary claims are overstated and do not carry weight: the empty margin is 371px of a 1000px sheet, not "the entire right half", and the plain `.top` sheet (not `data-two`) behaves the same way in capture, so that is the sheet's standing form rather than a readback fault.

Confirmed on the duplication alone. The block's own behaviour proves it only does visible work after divergence, which is the seam's rule stated in code.
  [sharpenedFix] Do not delete the block — suppress it until it earns its place, exactly as the seam does. Render `.saidWas` only when `pieces.join(" ") !== N.speech.transcript`, i.e. once a piece has been edited or dropped. At rest, the two notes stand alone as the record, since they are verbatim and editable. The moment the person changes a word, the original returns above them and now carries information: this is what the microphone heard, this is what you made of it. Keep the copy in notes-copy.ts terms; if the heading survives, "What you said, once" only stops fighting the layout once the paragraph is genuinely the only place those words appear once. While suppressed, leave the transcript reachable from the existing foot row as a quiet act reading `Read it as one note`, which rejoins the pieces rather than printing a second copy beside them.
======================================================================
Product taste and emotional resonance | two-counts-one-noun | cost 0.3
  [element] .head lede span (notebook.js:722) and .chip (notebook.js:732); .idxDay tails (notebook.js:1092)
  [problem] The head prints "Saturday 18 July, in 2 days, 1 still to decide" and then, twenty pixels later, "8 still to decide". Same five words, two different scopes, nothing on screen distinguishing them but a pill. Under pressure it reads "3 still to decide / 18 still to decide" and a venue owner has to work out which number is her Saturday. The index then repeats the same phrase five more times as inert H3 text — I confirmed no group tail contains a button or link — so of seven identical claims on screen exactly one is actionable, and it is the one that opens all eight rather than the subject she was reading.
  [fix] Give the two scopes two grammars. Leave the chip as "8 still to decide"; change the head lede tail to name its own scope: `${waiting} of these still to decide` reads wrong, so use `Saturday 18 July, in 2 days · ${waiting} undecided here`. Second, make each .idxDay tail a button carrying aria-label `${n} still to decide in ${label}. Go through them.` that opens the hand pre-filtered to that subject, so the count the person is actually reading is the one she can act on.
  [verdict] Verified live, not inferred. At ?state=notebook&v=locked the head's innerText is "notes / The Orchard, events / Mara & Finn / Saturday 18 July, in 2 days, 1 still to decide / 8 still to decide"; at ?state=pressure it is "...in 2 days, 3 still to decide" then "18 still to decide". Measured geometry: the two counts are 13px apart on one line, the lede at 13px/400 in rgba(17,17,17,0.72), the chip at 12px/600 white on #111 in a 999px pill. Both carry the identical five-word phrase and neither states its scope: notebook.js:718 scopes the lede to `n.aboutKey === "mara-finn"` while notebook.js:732 uses `counts().pending` for the whole notebook, and the chip's aria-label ("8 still to decide. Go through the notes waiting on a decision.") names no scope either. The index repeats the phrase five more times (four in pressure) and I confirmed every one is an inert span inside an h3 — `hasBtn:false, innerCtl:false` on all of them.

It is not covered by the baseline. notes-interaction-check.mjs line 91-96 reads the chip only: it asserts chip-vs-index-total agreement and that the head matches /\d+ still to decide/. Nothing compares the lede's count to the chip's. The lexicon gate at 1069 covers the name for the place and the verb for the crossing, not the state noun. So this is new ground, and it lands squarely on the standing checklist item "facts stated in two grammars or contradicted between header and body" — the source's own comment at line 90 says "The counts on screen may never disagree with each other," and on screen, 13px apart, 3 and 18 disagree.

Where I part company with the finding is its second half. Group headings that state a count are not obliged to be controls — Linear, Things and Bear all print section counts as inert summaries — and turning five sticky h3 tails into buttons would put five focusable controls inside the index region, cutting across the single-tab-stop-and-arrow-walk model the check gates at lines 140 and 1049, for a per-subject filtered hand that is new function rather than polish. The first half stands on its own; the second should be dropped.
  [sharpenedFix] Fix only the collision, and fix it without adding a second word for the state (the product has one lexicon for this: "still to decide" — "undecided here" would break it, and "here" names nothing).

The chip is the global count and is the one that must stay, because it is the only actionable one and it is what the review queue opens. So take the count out of the head lede at notebook.js:722, leaving `${soon.when}, in ${soon.days} day${...}`. The head then carries the one fact only the head can carry — the day the house is running and how long there is — and the per-subject number does not vanish: it is already printed, unambiguously, on the Mara & Finn group rule a few hundred pixels below, next to the name it belongs to (notebook.js:1092). Result: one count in the head, scoped to everything, in a pill you can press; one count per group, scoped by the heading it sits inside. No two bare twins on one line, no new vocabulary, no new tab stops, and line 96 of the interaction check still passes because it reads the chip.

If the earlier round's decision to have the head lead with the outstanding count must be preserved, the alternative is to scope the pill rather than the lede — "8 still to decide, all notes" — but the removal is the cleaner of the two and loses no information from the screen.
======================================================================
Measured evidence | reduced-motion-deletes-the-arrival | cost 0.6
  [element] notebook.js:263 `if (reduced) { paint(); return; }` in the keep path; master.css `.idxRow[data-arriving]` / `@keyframes arrive`
  [problem] Under prefers-reduced-motion the entire arrival branch is returned out of, so a reduced-motion user loses two things that are not motion: the `arriving` mark on the row the words became, and `rowEl.scrollIntoView({block:"center"})` that brings that row into the pile. Probed at 1440x700 with reducedMotion:'reduce', capturing a note filed under the last subject leaves the resulting row at y=871 while the index viewport ends at 682 (189px below the fold) and idx.scrollTop stays 0; the same capture with motion allowed scrolls to 398 and marks the row. The person is told "Kept. 15 notes in the notebook" and shown nothing. The CSS comment above the keyframe calls this "the one moment the whole direction exists for"; the file deletes it rather than translating it, and the gate's reduced-motion assertion only checks that animations do not run, not that the information survives.
  [fix] In notebook.js move the row lookup, `rowEl.scrollIntoView({ block: "center", behavior: "instant" })` and `arriving = note.id` ABOVE the `if (reduced)` guard, and keep the existing 620ms `arriving = null` timeout in both branches. In master.css add `@media (prefers-reduced-motion: reduce) { .idxRow[data-arriving] { animation: none !important; background: var(--indigo-wash); box-shadow: inset 2px 0 0 var(--indigo); } }` so the mark is a static hold for its 620ms rather than an animation flattened to 0ms. Apply the same to the readback multi-keep path at notebook.js:1891.
  [verdict] Reproduced exactly, and every attempt to kill it failed.

SOURCE. notebook.js:263 `if (reduced) { paint(); return; }` sits ABOVE both `arriving = note.id` (287) and `rowEl.scrollIntoView({ block: "center" })` (294). The early return is the whole rest of the keep path, so the scroll is not a motion effect that got flattened, it is code that never executes.

MEASURED. Drove the live file in chromium at 1440x700, typed a note, changed the filing chip to the last subject in the picker (`the-enquiries`, 5 of 5), Ctrl+Enter. Motion allowed: `.index` scrollTop 398, the new row's top at y=473 inside a scrollport running 305 to 682, i.e. in view. reducedMotion:'reduce', identical steps: scrollTop 0, the new row's top at y=871 — 189px below the fold — and the live region says "Kept. 15 notes, 9 waiting on a decision." The person who asked for stillness is told a note exists and is not shown where it went; the person who did not is carried to it. Filing under the default (mara-finn) hides this because that group is already at the top of the pile — the divergence only appears once the operator files under another couple, which is the ordinary case for a venue, not an edge case.

NOT ALREADY HANDLED, NOT A RESTATEMENT. The gate's only reduced-motion assertion (notes-interaction-check.mjs:507-520, "a machine that asks for stillness gets it, and still gets the note") checks `.idxRow` count === 15 and `.fly` count === 0. It asserts the ghost is absent; it asserts nothing about where the row is. The measured audit covers palette/type/contrast/hit/radii/motion tokens, none of which touch scroll position. And the canon line "the pile scrolls to that row first" is stated as a property of capture — this finding is that the property is conditional on a media query, which is the inverse of a restatement.

NO CONSTRAINT BROKEN. `--indigo-wash` and `--indigo` are existing palette tokens, no new hue, no new weight, no architecture change. A static 620ms row hold is not vestibular motion; it is the standard target highlight, and it carries the same information the animated version carries.

ONE CORRECTION TO THE PROPOSED FIX, which is why the sharpened version differs: moving the row lookup above the guard as literally written would break it. `mount.querySelector('.idxRow[data-id=…]')` cannot find the row until `paint()` has rendered the new note, and `arriving` must be set before that paint or the attribute is not in the markup. The three moves are not one block.

Weakest part of the finding, stated honestly: the "same applies at notebook.js:1891" tail is loose. The readback multi-keep path has no `reduced` guard at all — it sets `arriving` for everyone — so what it actually lacks is the scroll, for every user, not a reduced-motion regression; and those notes are filed under mara-finn so they land at the top anyway. That tail is a separate, much smaller item. It does not weaken the keepDraft claim, which is measured.
  [sharpenedFix] In `keepDraft()` (notebook.js ~263), stop returning out of the arrival. Restructure so the information survives and only the motion is dropped, respecting the fact that the row does not exist until after paint():

1. Set `arriving = note.id;` BEFORE the reduced check (it must be in the markup paint() produces).
2. Capture `from`/`size` from the field only when `!reduced` (they are only used by flyWords).
3. Call `paint()` once, unconditionally.
4. AFTER paint(), do the row lookup and `rowEl.scrollIntoView({ block: "center", behavior: reduced ? "instant" : "auto" })` unconditionally — this is the line that must reach reduced-motion users.
5. Keep inside `if (!reduced)` only: the `row.style.opacity = "0"` breath-hold with its 470ms restore, and `flyWords(...)`. The hold must NOT run under reduced motion or the row sits blank for 470ms with nothing flying in to fill it.
6. Schedule the existing `setTimeout(() => { arriving = null; paint(); }, 620)` on BOTH paths, so the mark clears either way and `arriving` cannot stick.

In master.css, next to the existing `@media (prefers-reduced-motion: reduce)` block at 1200 (which flattens all animations to 0ms and would otherwise resolve `arrive` straight to its transparent end state, making the mark invisible), add:

@media (prefers-reduced-motion: reduce) {
  .idxRow[data-arriving] {
    animation: none !important;
    background: var(--indigo-wash);
    box-shadow: inset 2px 0 0 var(--indigo);
  }
}

so the mark is a static hold for its 620ms rather than an animation flattened to nothing.

Then add the assertion the gate is missing. Extend the "stillness" block: after Ctrl+Enter under reducedMotion 'reduce', with the filing chip switched to the LAST subject in the picker, assert the new row's bounding rect is inside the `.index` scrollport — the current assertion (15 rows, 0 `.fly`) passes today with the row 189px below the fold, so it cannot catch a regression of this. Assert the same for the default filing so both paths are locked.

Leave notebook.js:1891 out of this fix. That path has no `reduced` guard and its notes are filed under mara-finn at the top of the pile; if the scroll is wanted there it is a separate, lower-value item, not part of this regression.
======================================================================
Measured evidence | phone-head-amputated | cost 0.5
  [element] .head / .headNext / .headActions at 360, 390 and 414; `.sheet { overflow: hidden }`
  [problem] The head is one flex row of six items with no phone treatment beyond `.headName { display:none }`. Measured: at 360, 390 and 414 `.headNext span` computes to width 0 — the string "Saturday 18 July, in 2 days, 1 still to decide" and the `<b>Mara & Finn</b>` are laid out inside a 0-width `overflow:hidden` flex item, so the head's lead fact is invisible on screen at every phone width while still sitting in the accessibility tree and being read aloud. At 360 the head overflows the sheet by 50px: "Private to you" is cut mid-word at the sheet edge and the 37px "Notes options" button is entirely off-sheet yet still focusable and still in the tab order. This passes the behaviour gate because `.sheet { overflow: hidden }` means the document never scrolls sideways, so "no sideways scroll at six widths" is true while the content is silently amputated. It is the standing checklist's invisible-but-focusable control, on the surface the locked architecture designates as the product's home.
  [fix] In the `max-width: 720px` block give the head two rows: `.head { display: grid; grid-template-columns: auto 1fr auto; grid-template-rows: auto auto; height: auto; padding: 8px var(--gutter) 6px; row-gap: 2px; }` with `.word` and `.headActions` on row 1 and `.headNext` spanning row 2 at `white-space: normal` so the couple, the date and the group count actually print. Move `.chip` to row 1 beside the wordmark, and collapse `.headAct` down to the single "…" button below 400 (fold "Private to you" into that menu) so nothing is laid out past `.sheet`'s right edge. Add an assertion to notes-interaction-check.mjs that no focusable element's rect exceeds its nearest overflow:hidden ancestor at 360/390/414.
  [verdict] Verified live. `.headNext` computes to width 0 with overflow:hidden at 360, 390, 414 AND 430 — "Mara & Finn" and "Saturday 18 July, in 2 days, 1 still to decide" render as nothing on every phone width (screenshots _vrf_head_360.png / _vrf_head_390.png show the head as wordmark + chip + "Private to you" only), while ariaSnapshot of .head still exposes the full string, so screen-reader and screen users get different products. At 360 the head's scrollWidth is 392 against a 342 client box; .headActions right = 401 against sheet right = 351, so "Private to you" is cut mid-word and the 37px "Notes options" button is fully off-sheet, focusable and in the tab order. It is worse than reported: focusing that button scrolls the overflow:hidden .sheet from scrollLeft 0 to 50 and it never returns (no scrollbar, no gesture into a clipped box) — the wordmark lands at x=-25 and every index row at x=-35, so one Tab permanently shears the left edge off the product's home surface (_vrf_headfocus_360.png). The baseline does not cover this: the invisible-but-focusable sweep (notes-interaction-check.mjs:427) runs at desktop width and tests only display/visibility/own zero-rect, never ancestor clipping; the sideways-scroll checks (:491, :498) read documentElement.scrollWidth, which overflow:hidden on .sheet keeps clean, and 360 and 414 are never tested. No constraint is broken by fixing it — this is a phone media-query layout, not a colour, a weight or an architecture change.
  [sharpenedFix] In the max-width:720px block give the head two rows and let the lead fact print: `.head { display:grid; grid-template-columns:auto auto 1fr auto; grid-template-rows:auto auto; height:auto; padding:9px var(--gutter) 7px; row-gap:3px; align-items:center; }` with `.word`, `.headRule`, `.chip` and `.headActions` on row 1 and `.headNext` spanning row 2 (`grid-column:1/-1; white-space:normal; overflow:visible;`) with `.headNext b, .headNext span { white-space:normal; }` so the couple, the date and the count actually set. Do NOT fold "Private to you" into a menu — the sealed-edge promise is the product's second promise and belongs on the surface; instead below 420 hide only its label (`.headAct[data-act="privacy"] span { display:none }`) and keep the lock glyph with its existing aria-label, which alone brings .headActions inside the sheet at 360. Then close the gate loophole with two assertions in notes-interaction-check.mjs, run at 360/390/414/430: (1) no focusable element's rect exceeds its nearest overflow-clipping ancestor's rect; (2) after tabbing the whole order, every overflow:hidden container still reports scrollLeft === 0 and scrollTop unchanged — that second one is what catches the unrecoverable 50px shear, and the existing documentElement.scrollWidth check never will.
======================================================================
Measured evidence | filing-picker-has-no-keyboard | cost 0.4
  [element] `[data-act="filing"]` + `.pickerPop[role="listbox"]` in notebook.js:829-846
  [problem] The one control that decides where a person's words are filed advertises a listbox (`aria-haspopup="listbox"`, `aria-expanded`, `role="listbox"`, `role="option"`, `aria-selected`) and implements none of its keyboard model. Driven live: Enter opens it; ArrowDown does not enter the list, it falls through to the index's own arrow walk and moves focus to an idxRow BEHIND the open popup while the popup stays open; Escape closes it and drops focus to `<body>`, so a keyboard user who changes their mind loses their place and has to Tab from the top of the document. The note-open path already restores focus correctly (Escape from an opened note returns focus to its row, verified), so the product knows how to do this and this control was left out.
  [fix] In the keyboard handler in notebook.js, when `picker` is non-null claim ArrowDown/ArrowUp/Home/End before the index walk and move focus between `.pickerPop [role="option"]` elements; on Enter/Space commit the focused option; on Escape close and call `.filingBtn` focus restore via the existing `refocus` mechanism (`refocus = { kind: "sel", sel: '[data-act="filing"]' }`). Add Tab-closes-and-commits so the popup cannot outlive focus leaving it.
  [verdict] CONFIRMED by driving the file, not by reading it. Source: notebook.js:829-846 renders aria-haspopup="listbox"/aria-expanded on `.filingBtn` and role="listbox" + role="option" + aria-selected on `.pickerPop`; the keydown handler (notebook.js ~1940-2038) never mentions `picker` except in the Escape branch, which does `picker = null; paint();` with no `refocus` set. paint() only restores focus from `refocus` or from a captured TEXTAREA/INPUT caret, so a focused button is annihilated by the innerHTML rewrite.

Driven at 1440x960, ?state=notebook: focus `[data-act="filing"]` → Enter → aria-expanded=true, popup open, focus still on the chip. ArrowDown → activeElement becomes `.idxRow` ("Mara & Finn's menu tasting at The Orchard...") while `.pickerPop` is still in the DOM and aria-expanded stays "true" — the index arrow walk claims the key and the advertised listbox never gets it. Escape → activeElement is BODY. Reproduced identically on the open-note `[data-act="refile"]` picker at 1440 and on the phone sheet's refile at 390x844: both land on BODY.

The finding's contrast claim also verified: focus a row, Enter, Escape → activeElement returns to `.idxRow`. The product's note-open path restores focus correctly; the control that decides where a person's words are filed does not.

Not a restatement of the baseline: notes-interaction-check.mjs touches the picker only at lines 887-895, 929-933, 1088, and only by mouse click — there is no keyboard assertion on `.pickerPop` anywhere in the 102/276 gate. It is squarely on the standing checklist ("keyboard models advertised but not implemented"). Nothing here needs a colour, a weight, or an architecture change.

Two corrections to the finding as written, neither fatal: (1) the focused idxRow is not literally "BEHIND the open popup" — measured, the popup occupies y -4 to 184 and the index rows start at y 356, so it is focus leaving the open popup into content below it, not focus hidden under it; (2) Tab is not broken — the option rows are real buttons in DOM order, so Tab and Shift+Tab already walk them; only the arrow model and the Escape return are missing.
  [sharpenedFix] In the keydown handler in notebook.js, claim the keys while `picker` is non-null, before the index walk:

1. ArrowDown/ArrowUp/Home/End move focus between `mount.querySelectorAll('.pickerPop [role="option"]')`, wrapping, starting at the `aria-selected="true"` option. Enter/Space then commit via the existing `file-capture`/`file-note` click path (the options are buttons, so the browser already does this once focus is inside).

2. Escape must restore, not drop: set `refocus = { kind: "act", sel: picker === "capture" ? '[data-act="filing"]' : '[data-act="refile"]' }` before `picker = null; paint();`. Note the existing mechanism's kind is `"act"`, not `"sel"` as the finding wrote — see notebook.js:1756, which already uses exactly this shape after a commit.

3. REJECT the finding's "Tab-closes-and-commits" clause. Tab already walks the options correctly, and committing on Tab would silently refile a person's words because they tabbed away — that is worse than the bug. Instead close without committing on `focusout` when the new focus target is outside `.filing`, mirroring the existing click-outside close at notebook.js:1718.

Gate it: add keyboard assertions for the picker to notes-interaction-check.mjs beside the existing mouse-only ones at 887 and 929 — after Enter on the chip, ArrowDown lands inside `.pickerPop [role="option"]` and the index cursor has not moved; after Escape, `document.activeElement` matches `[data-act="filing"]` (and `[data-act="refile"]`, including at 390 wide).
======================================================================
Measured evidence | decision-counts-in-four-grammars | cost 0.3
  [element] ?state=review — `.headNext span`, `.handOf`, `.deckNote`, `.indexHead .cnt`; and `.headNext span` vs `.chip` in ?state=notebook
  [problem] In review the screen states the same idea four ways within 600px: head "1 still to decide", card "1 of 8", deck line "8 still to decide.", index head "STILL TO DECIDE / 7 left". 8 and 7 are one list counted with and without the card on the desk, and nothing on screen says so. In notebook the head reads "...in 2 days, 1 still to decide" and the black pill 13px later reads "8 still to decide" — same five words, different number, neither stating its scope, and the chip's accessible name ("8 still to decide. Go through the notes waiting on a decision.") does not state it either. A venue owner cannot tell whether she has one decision or eight. The gate passes because it only asserts `/\d+ still to decide/` matches the head.
  [fix] Scope the group-level count in notes-copy.ts: `.headNext span` becomes "Saturday 18 July, in 2 days, 1 of these still to decide" and the chip's aria-label becomes "8 still to decide across the notebook. Go through the notes waiting on a decision." In review, change `.indexHead` from "STILL TO DECIDE / 7 left" to "STILL TO DECIDE / 7 behind this one" so 8 and 7 stop reading as a contradiction.
  [verdict] Verified in the live file and the frames. In ?state=notebook the header prints ".headNext span" = "Saturday 18 July, in 2 days, 1 still to decide" ending at x=817 and ".chip" = "8 still to decide" starting at x=830 on the same line: five identical words, two different numbers, a 13px gap, no scoping word on either. The bold "Mara & Finn" is the only scope signal and sits 250px left with the date and days clause intervening, so it cannot hold the count once a heavier black pill repeats the same phrase. In ?state=review the same fact family appears at y=41 ("1 still to decide"), y=116 ("1 of 8"), y=410 ("8 still to decide.") and y=459 ("STILL TO DECIDE / 7 left") inside 418px. Not already handled: log.html:524 shows an earlier round killed a three-way version by suppressing the chip in review (notebook.js:727), but the head clause (notebook.js:718-722, scoped to aboutKey === "mara-finn") and the index head (notebook.js:1315, count `${left-1} left`) were never reconciled, and the notebook-state collision was never addressed. Not covered by the measured baseline: notes-interaction-check.mjs:96 and :208 assert only /\d+ still to decide/, never the numbers or their scopes, exactly as the finding states. No constraint is touched: copy only, no hue, no weight, no architecture. One weakening: in review the 8/7 pair is partly derivable, because "1 of 8" and the pips aria "0 of 8 decided" do let a reader work out that 7 sit behind the card. That defence does not exist in the notebook header, where 1 and 8 have no bridge at all, and this is the checklist's own named defect (a fact contradicted between two places) at the product's most-scanned line.
  [sharpenedFix] The proposed fix under-powers the part that matters: "1 of these still to decide" leaves "these" unbound, and scoping only the chip's aria-label leaves the visible pill collision intact. Break the collision in the visible words instead. (1) notebook.js nextUp() — the head clause names its subject before the shared phrase: "Saturday 18 July, in 2 days, 1 note about this wedding still to decide". The chip's visible text stays "8 still to decide"; the two strings then no longer share an unbroken five-word tail, so the eye reads them as different facts rather than a contradiction. (2) The chip's aria-label gains the matching scope: "8 still to decide across all your notes. Go through the notes waiting on a decision." (3) notebook.js:1315 — index head count goes from "${left - 1} left" to "${left - 1} behind this one", as proposed; that one is right and is the correct pairing with the card's own "1 of 8". (4) Extend notes-interaction-check.mjs:96 from the regex to an assertion that compares the head's number against the chip's number and requires the head's string to carry a scoping phrase whenever the two differ, so the collision cannot return.
======================================================================
Measured evidence | undeclared-leadings-and-a-dead-tracking-token | cost 0.2
  [element] master.css :root tracking curve; `.nudge` and `.peelBoundary` (`var(--tr-13, -0.01em)`); `.emptyBody`, `.darkNote`, `.state p`, `.peelWhy`, `.specWhy`, `.darkSaid`, `.emptyTitle`
  [problem] The file's own comments claim "Three leadings, declared" and "Every letter-spacing in this file is a token from it." Both are false and the gate cannot see it — notes-audit.mjs's leading check only flags `line-height: normal`, and there is no tracking check at all. Measured across ten states: 17px reading text renders at five different leadings (1.22, 1.45, 1.58, 1.62, 1.28), 11px at three, 12px at three, 13px at two; 1.22 / 1.38 / 1.50 / 1.55 / 1.58 / 1.60 / 1.64 are all off the declared three-step ladder. `--tr-13` is never defined in `:root`, so `.nudge` and `.peelBoundary` silently take the hardcoded -0.01em fallback and sit off the tracking curve, giving 13px text three trackings (-0.156px, -0.13px, -0.078px). Same class of defect the file says it fixed for `.readBody`/`.handBody`, left standing on every surface that was not in that one selector list.
  [fix] Add `--lh-note: 1.58` and `--lh-title: 1.22` to :root if two more steps are genuinely wanted, then replace every raw ratio with a token: `.emptyBody`/`.state p`/`.peelWhy`/`.specWhy`/`.darkNote` to `var(--lh-note)`, `.emptyTitle` to `var(--lh-title)`, `.idxText`'s literal 1.45 to `var(--lh-base)`, `.darkSaid` 1.38 to `var(--lh-tight)`. Define `--tr-13: -0.01em` in the :root curve and drop the fallbacks. Extend notes-audit.mjs check 9 to assert every computed line-height ratio is within 0.01 of a declared `--lh-*` value and every letter-spacing within 0.01px of a declared `--tr-*`.
  [verdict] I tried to kill this and could not — every number in it reproduces exactly, and the file convicts itself by its own stated standard.

VERIFIED IN SOURCE (master.css):
- Line 87-88 comment: "Three leadings, declared". :root declares --lh-tight 1.28 / --lh-base 1.45 / --lh-read 1.62 (lines 89-91). Eleven raw ratios are then written past the ladder: 1.45 literal (550 .idxText), 1.38 (657 .darkSaid), 1.6 (669 .darkNote), 1.55 (879 .peelWhy, 915 .specWhy, 922 .state p), 1.45 (882 .peelSent), 1.22 (906 .emptyTitle), 1.58 (907 .emptyBody), 1.6/1.64/1.56 (1014-1016 expressive), 1.5 (1073 mobile field).
- Line 93-94 comment: "Every letter-spacing in this file is a token from it." Also false, and in two ways the finding names plus one it misses: `var(--tr-13, -0.01em)` at lines 812 and 820, AND three hardcoded literals — `letter-spacing: 0.04em` at 221 (.railAvatar) and 588 (.dockAvatar), `0.05em` at 649 (.darkTag).
- `--tr-13` is defined nowhere. `grep -n "tr-13" master.css notebook.js notebook.html` returns only the two consuming sites. Confirmed dead token; the -0.01em fallback is what actually renders.

VERIFIED LIVE (Playwright, chromium, ten states x locked room, computed styles):
- 17px renders at FIVE leadings: 1.22, 1.28, 1.45, 1.58, 1.62. Exactly the five claimed.
- 11px at three (1.28, 1.45, 1.62), 12px at three (1.45, 1.55, 1.6), 13px at two (1.45, 1.55). Exactly as claimed.
- 13px renders at THREE trackings: -0.156px (--tr-14), -0.078px (--tr-12), and -0.13px on .peelBoundary in ?state=seam — which is the dead --tr-13 fallback surfacing on screen. Exactly as claimed.
- 11px renders four letter-spacings including 0.44px (.railAvatar, the 0.04em literal) and 0.825px (--tr-cap); 12px includes 0.6px (.darkTag, the 0.05em literal). So the "one tracking curve" has three uppercase trackings, not one.

VERIFIED IN THE GATE (scripts/design/notes-audit.mjs lines 183-189): check 9 is `if (cs.lineHeight === "normal")` and nothing else. There is no letter-spacing check anywhere in the file. The brief's own claim — "An eight-step ramp and a three-step leading ladder, both gated" — is half true: sizes are gated at line 178-181, leading is gated only against browser default, tracking is not gated at all. This finding attacks a blind spot in the measured baseline rather than restating it, so the "worthless if it restates the baseline" bar does not catch it.

WHY IT SURVIVES THE ADVERSARIAL PASS. The obvious refutation is that different roles legitimately take different leading at the same size, so five leadings at 17px is a system, not a defect. That fails on the file's own terms. 1.55 / 1.58 / 1.60 / 1.62 are four prose leadings separated by less than a pixel per line at their sizes — no reader can tell them apart and no designer chose four of them on purpose. That is precisely the condition the file's own type-ramp comment names and rejects thirty lines earlier: "eighteen ad-hoc sizes on half-pixel increments is not a system, it is a habit." The same habit, one property over, left standing.

The dead --tr-13 is not taste at all. It is a typo-class defect that renders a third tracking onto 13px text in the seam — the surface carrying the product's headline promise — and it is exactly the class of bug the file says it already fixed for .readBody/.handBody, left in place on every selector outside that one list.

Not out of scope: no colour, no weight, no architecture. Fix is contained to :root and a gate extension.

Deduction from the finding: it calls all five 17px values "reading text", which is loose — 1.22 is .emptyTitle in the spec variant and 1.28 is .word, neither is prose. The count and the values are right; only the label is sloppy. Not enough to refute.
  [sharpenedFix] Two moves, both inside the locked constraints.

1. Make the declaration true. Either collapse to the declared three, or declare what is actually needed and name it. Collapsing is the stronger answer and costs almost nothing visually: .emptyBody 1.58, .peelWhy 1.55, .specWhy 1.55, .state p 1.55, .darkNote 1.6 and expressive .readLong 1.64 all become `var(--lh-read)` (1.62 — max delta 0.07 ratio, under a pixel a line); .idxText's literal 1.45, .peelSent 1.45 and mobile field 1.5 become `var(--lh-base)`; .emptyTitle 1.22, .darkSaid 1.38 and expressive .handBody 1.56 / .readBody 1.6 are the only genuine outliers — if display leading really wants to be tighter than 1.28 at 27px and 34px, add ONE token `--lh-display: 1.22` and give .darkSaid and .emptyTitle that, rather than the finding's two. Four steps, all named, beats three claimed and eleven shipped.

2. Kill the dead token and the literals. Define `--tr-13: -0.010em` in the :root curve (it sits correctly between --tr-14 -0.012em and --tr-12 -0.006em) and drop both fallbacks at lines 812 and 820 — or, cheaper and more honest, point .nudge and .peelBoundary at `--tr-14`, which is the declared step for 13px text everywhere else in the file, and delete --tr-13 entirely. Replace the three hardcoded uppercase literals — 0.04em at 221 and 588, 0.05em at 649 — with `var(--tr-cap)`, so the curve's one uppercase step is actually the only uppercase step.

3. Close the blind spot so this cannot come back. Extend notes-audit.mjs check 9 from a `=== "normal"` test to a ladder test: read the declared `--lh-*` and `--tr-*` values off :root at run time (do not hardcode them, so the check tracks the tokens), then fail any text-bearing element whose computed line-height / font-size is more than 0.01 from a declared ratio, and any whose computed letter-spacing is more than 0.01px from a declared em step at its own size. Report it as a tenth check with its own zero, alongside the nine. Run it across all ten states and all four rooms including [data-type="expressive"] and the 390 breakpoint, which is where four of the eleven stray ratios live and where a locked-room-only pass would miss them.