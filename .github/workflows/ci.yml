name: ci

# Production safety gate for Signal Tasks.
# Rewritten 2026-06-18 (production-readiness audit): the previous version
# used `npm ci` / package-lock.json, but this repo is pnpm-only now
# (pnpm-lock.yaml) — the old workflow could not install. Required gates:
# lint + typecheck + design-system drift + tests (all blocking). The parser
# test remains informational because it is date-sensitive (Show HN beat).

on:
  push:
    branches: [main, release/january-2027]
  pull_request:
    branches: [main, release/january-2027]

# Cancel in-progress runs for the same ref so a fresh push supersedes
# the previous one — saves CI minutes on rapid pushes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: typecheck · test
    runs-on: ubuntu-latest
    # A green run takes 8-10 minutes; slow runners have taken twice as long, so keep headroom without skipping gates.
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.9.0

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Migration ledger contract
        run: pnpm db:contract

      - name: Isolated January migration rehearsal
        run: node scripts/db/verify-january-rehearsal.mjs

      - name: Typecheck
        run: pnpm typecheck

      # The D-022 access-term differential test (venue-access-term.test.ts)
      # hard-fails in CI without a live studio checkout to compare against.
      # Checked out here, after typecheck, so no other gate
      # ever scans a second repository's sources.
      - name: Checkout studio for access-term parity
        uses: actions/checkout@v4
        with:
          repository: ethanmcn2013-droid/studio
          # Every trigger needs this reviewed compatible fixture runtime.
          ref: 4ed9be64be4f351c5a3e7aa2b396e92d9be570bf
          path: studio-parity

      - name: Install immutable paired Studio dependencies
        working-directory: studio-parity
        run: corepack pnpm install --frozen-lockfile
      - name: Record paired Studio revision
        run: git -C studio-parity rev-parse HEAD

      - name: Project Drive suite is wired into pnpm test
        run: node -e "const s=require('./package.json').scripts.posttest||''; if(!/(^|&& )pnpm test:project-drive( &&|$)/.test(s)){console.error('posttest must run pnpm test:project-drive; it is the only CI entry point for the Project Drive suite');process.exit(1)}"

      # `pnpm test` runs the Project Drive lifecycle and recovery suite through
      # its `posttest` hook. Do not add a separate `pnpm test:project-drive`
      # step: it ran the same 401 tests twice and cost 70-180s per run.
      - name: Test (includes calendar, Signal, Timeline, suite truth, and Project Drive gates)
        run: pnpm test
        env:
          STUDIO_REPO_PATH: ${{ github.workspace }}/studio-parity

      - name: Actual Venue payment-evidence to use and erasure rehearsal
        run: |
          pnpm test:venue-rehearsal --studio-root studio-parity
          pnpm test:sponsored-use:paired --studio-root studio-parity

      # The parity checkout exists only for the differential step above;
      # removed so lint never sweeps a second repository's sources.
      - name: Remove parity checkout
        run: rm -rf studio-parity

      - name: Parser unit test (informational — date-sensitive)
        run: pnpm run test:parser
        continue-on-error: true

      - name: Lint
        run: pnpm run lint
