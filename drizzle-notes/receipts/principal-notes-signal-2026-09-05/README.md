# Notes/Signal correction — principal acceptance

**Accepted for local/test tooling only.** Source53295ee7d1d9b395513872aed2d958feebfd0557, authored seal d0668885,
is integrated at0dcf954517753f0ce9571e11c5e16754c51c7def. Mandatory registration
is in6cae91b00e0f02eb96440413e264c07d30dd8a49. Remote/production adoption is unsupported and remains open.

Independent native ESM verification passes48/48 declared groups and the original
four extra-table refusal controls now pass4/4. The only alteration to those
four controls is the recorded release SHA; original279c tests and their four
failures are preserved. Original native21 and populated rollback/retry evidence
remain attributed to279c; transaction code is unchanged by the correction.

Both author ZIPs pass complete payload integrity checks:889 original and813
successor payloads. All28 successor source bindings match current Git blobs and
canonical file readbacks. Ten inspected SQL/snapshot/journal/original receipt
inputs are byte-identical between279c and5329. The successor ledger explicitly
names new proof receipts. Author evidence includes two provisional-registration
refusal cases; these were source/evidence reviewed, not independently rerun.
They do not establish a supported migration for retained provisional databases.

The principal composition passes its actual mandatory database command:114/114
runtime cases, strict owning source/command contract and registration lint.
Eight explicit Notes/Signal inventory/status/adopt/migrate commands are registered.
Each still requires an explicit absolute local file and local/test environment;
no ambient URL or credentials are consumed. The first registration lint failure
and variable-name correction are retained. No full receiving/Linux/build or
production verdict is claimed by this scoped composition.

The review archives retain original failures, scripts, four per-module/registration
database controls and JSON outcomes, command logs, source inventories and composition
registration inputs. Large duplicate temporary native-test trees and the copied
rollback source directory are not archived here; author native databases remain in
their separately verified archives. The rollback control script reconstructs its
synthetic proof condition from the pinned original source. Original live output
directories are preserved.

Limits: live legacy inventory, production transport/adoption, retained-fixture
conversion, actual production backup/restore, cross-process file replacement and
network filesystem behavior remain unverified. A backup reference/hash is not
restore certification. The separate existing-tools d8a3af6b correction has its own
review and is not accepted by this receipt. Held Drive/RC3/Atlas work is untouched.
