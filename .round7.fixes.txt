--- 1. UI composition | hand-body-collapse | cost 0.5
  WHERE: .handBody inside .handTop, ?state=review, card 2 of 8 (press K then T)
  FIX: Give .handBody a min-height of 3 lines (calc(var(--lh-read) * 3em)) and let the card grow instead of the body shrinking: change .handTop from a fixed height to min-height with the same value, and cap the body at max-height: 46vh with its existing overflow:auto so a long note scrolls rather than the card growing without limit. Verify by asserting in notes-interaction-check that .handBody.getBoundingClientRect().height >= 40 on every card in the hand, before and after T and K.
--- 2. UI composition | undo-strip-over-rows | cost 0.3
  WHERE: .undo (position:absolute; bottom:78px) over .index at any scroll offset other than the end
  FIX: Stop padding the scroller and start shortening it. While .sheet[data-undo] is set, subtract the strip's band from the index's height rather than adding it to its padding: .sheet[data-undo] .indexWrap { padding-bottom: 58px } with .index keeping its existing 132px foot padding, so the strip lives in reserved layout space below the scroll box and no row can ever pass under it. Assert in notes-interaction-check that no .idxRow rect intersects the .undo rect at scrollTop 0, mid-scroll and at the end.
--- 3. UI composition | hand-card-width-jitter | cost 0.3
  WHERE: .handTop in ?state=review, across cards and across T
  FIX: Fix the frame instead of the content: .handTop { width: min(100%, calc(var(--measure) + 276px + 34px + 48px)) } — the same writing column plus margin the desk already resolves to — and let .handFoot wrap inside it. The card then sits on the desk's own column, its left edge lands on 261 like the paper, and the pips, body and actions stop moving between cards.
--- 4. Typography | phone-clamp-slices-a-glyph | cost 0.5
  WHERE: .idxText[data-clamped]::after, inside the max-width:700px block of master.css — ?state=notebook at 360, 390 and 414
  FIX: Delete the ::after and its gradient, and extend the existing JS trim to the two-line mobile box: after paint, while text.scrollHeight > text.clientHeight, pop whole words off the preview span and re-measure, then write the ellipsis as the last inline character of the text itself — text.innerHTML = `<b>${lede}</b> <span>${words.join(" ")}…</span>` — using the same resize re-run and the same never-below-the-lede floor the desktop trim already has, and leaving data-full untouched so the accessible name keeps the whole sentence. If the pseudo must survive, replace it with mask-image: linear-gradient(90deg, #000 calc(100% - 52px), transparent) on .idxText itself so the fade is continuous across the whole last line, and never a hard edge mid-glyph.
--- 5. Typography | lede-spent-on-every-row | cost 0.4
  WHERE: .idxText b — ?state=notebook (14 of 14 rows) and ?state=pressure (36 of 36)
  FIX: Keep the 48-character sentence rule but stop paying it in weight. Set .idxText b to font-weight: 400 with color: var(--ink-1) and leave .idxText span at var(--ink-2) — the density split the palette already owns — so the lede reads as the darker half of one sentence rather than a different typeface. Reserve 600 in the index for exactly two things: the row under the keyboard cursor and a search hit. Then measure it: load ?state=pressure and count semibold runs on screen; more than three means the emphasis is spent again.
--- 6. Typography | writing-is-smaller-on-the-phone | cost 0.3
  WHERE: .phoneField in the dock at 390 — 15px/21.75px/-0.016em, against .topField and .readBody at 17px/27.54px/-0.021em
  FIX: Add .phoneField to the shared writing rule so it inherits font-size: var(--t-17); line-height: var(--lh-read); letter-spacing: var(--tr-18) exactly as .topField does, and remove the --t-15 declaration from the ≤700px .dock .phoneField block. The dock already gives the field a full-width row of its own (grid-column: 1 / -1), so 17px at 1.62 fits two lines in 88px against the current 76px; take the 12px from the dock's 4px 8px 8px padding rather than from the type.
--- 7. Typography | count-pill-breaks-in-half | cost 0.25
  WHERE: .chip in .head — "8 still to decide" at 360 and 390
  FIX: Add white-space: nowrap to .chip. At nowrap the pill measures 109px, which the head's two-row phone layout already has room for — allow the head row to wrap the chip onto the second row instead of wrapping inside it, by giving the head's first row flex-wrap: wrap and the chip flex: none. Apply the same nowrap to .idxDayTail and .headNext b, which carry counts and dates on the same rows and can break the same way at 360.
--- 8. Interaction and states | crossed-ledger-never-records-the-crossing | cost 0.5
  WHERE: ?state=seam · [data-act="send"] → .pileHead "What has crossed into Tasks · 3 so far, and every note stayed here" (notebook.js:1569)
  FIX: On send, unshift the sent item into the crossed list with its picked words as the title, "To do" as the state and "just now" as the time, and derive the ledger's count from that list rather than the fixture constant N.counts.sent, so the header, the receipt and the rows all resolve from one object. Undo must remove that row again.
--- 9. Interaction and states | discard-speech-is-the-one-irreversible-act | cost 0.4
  WHERE: ?state=readback · [data-act="discard-speech"] ("Discard", notebook.js:1990)
  FIX: Route discard-speech through the same path as drop-piece: keep the pieces in a pending buffer, raise the standard undo strip with the same thirty-second window and the same Ctrl+Z, and announce "Discarded. Undo for thirty seconds." Move Discard out of the primary's 24px neighbourhood to the far left of the tray, level with "Add another".
--- 10. Interaction and states | search-cursor-is-the-same-ink-as-hover | cost 0.35
  WHERE: ?state=search · .idxRow keyboard cursor vs :hover — both computed rgba(17, 17, 17, 0.035)
  FIX: Give the search cursor the indigo it is already entitled to — a hit is one of the three sanctioned indigo jobs — as a 2px indigo left edge on the cursor row plus indigo-wash fill, and suppress the hover wash on .idxRow entirely while a search query is live so exactly one row is ever marked. Cap the hit marks at the first two per row.
--- 11. Interaction and states | receipt-promises-an-edit-the-product-cannot-do | cost 0.3
  WHERE: ?state=seam receipt line, data.js:343 stayedPut, concatenated with sentReceipt into #say
  FIX: Cut the duplicated clause: the announcement should read "Sent to Tasks. Your note stayed here, and only those words crossed." Until the desk can edit, the drawn receipt must say what is true — "Your note is still here and still private" — and drop "still yours to edit".
--- 12. Interaction and states | pressure-long-note-has-never-rendered | cost 0.25
  WHERE: notebook.js:1377 — work().find(n => n.words > 300) against N.long (~205 words); .readLong count 0 in every run
  FIX: Change the guard to n.words > 150 or select N.long by id, and put the long note in the base index so it can be opened by hand. Then make the desk honest under it: cap .deskWrite at the viewport with its own overflow-y: auto and a reserved band, and add Home and End to the pick model so the last sentence of a long note is one key away rather than forty.
--- 13. UX and information design | defer-lowers-the-count | cost 5
  WHERE: ?state=review — the rest line under the card ("N still to decide."), the pile header ("N behind this one"), the pile rows, and `deferred[]` in notebook.js
  FIX: Compute both visible numbers from outstanding work, not from the undealt remainder: rest line and header count = queue().length + deferred.length. Keep deferred notes in the pile below, appended after the undealt ones, carrying the state label "Left for later" instead of "To decide", so the row is still reachable and still readable as undecided. Assert in notes-interaction-check that after any number of "Decide later" presses the drawn count, the pile row count plus one, and the spoken sentence all name the same number.
--- 14. UX and information design | hand-deals-in-silence | cost 4
  WHERE: ?state=review — entering the hand via the head chip `.chip`, and the repaint after T / K / Decide later / Delete
  FIX: On entering the hand, move focus to the card's `.handBody` (which already carries a name). After each decision, return focus to the same `.handBody` on the newly dealt card, and extend the announcement to carry the card: "Kept in Notes. 7 left. Next, 2 of 8: Idea for the studio newsletter, short piece on what a calm Saturday actually looks like behind the bar." Use the same phrasing shape the index walk already uses, so one grammar covers both planes.
--- 15. UX and information design | undo-strip-over-the-pile | cost 3
  WHERE: .undo — measured at 1440 covering `.idxRow` "Follow up with the motion designer about the venue…", and at 390 covering the group heading "The studio" outright
  FIX: Render `.undo` in the desk's margin column instead of over the measure. The composition already reserves a second column that prints nothing at or above 1080: pin the strip to the top of that margin, aligned to the arriving row, so it never crosses text. Below 1080 and on the phone, where the margin folds, place it in the band directly above the dock and add its height to the pile's padding-bottom while it is up, so no row at any scroll position can sit beneath it. Gate it: assert no `.idxRow` or group heading intersects `.undo` at three scroll positions and at 390, 768 and 1440.
--- 16. Brand and copy | kept-means-two-things | cost 3
  WHERE: [aria-live=polite] after Ctrl+Enter (notebook.js:262) vs .idxTag[data-quiet] (notebook.js:1190) vs .undo strip vs the hand's K
  FIX: Pick one verb per act and never reuse it. Capture: button "Save it", strip "Saved.", announcement "Saved. 15 notes, 9 still to decide." The hand: "Just keep it" stays, its receipt becomes "Kept in Notes", its pill becomes "Kept in Notes" or simply "Decided". Then unify the state noun everywhere: the head chip, the group rule, the row pill, the index title and every announcement say "still to decide" — today the announcement says "waiting on a decision", the pill says "To decide" and the chip says "still to decide", which is one state in three grammars.
--- 17. Brand and copy | seam-ignores-the-filing | cost 2.5
  WHERE: .peel .picker (notebook.js:1071, hardcoded N.projects[0]) against .filingBtn on the desk
  FIX: Default the destination to the note's own subject and say so in the person's words: "To Mara & Finn" with "the Saturday 18 July wedding" as the quiet second line, falling back to the workspace only for a note filed under The house or The studio. Make the chevron open the same subjectList() the filing chip opens, so there is one list and one noun for where a thought belongs. If a picker cannot be wired for this round, remove the chevron and print the destination as text rather than shipping a dead affordance on the product's most consequential control.
--- 18. Brand and copy | search-sentence-grammar | cost 1.5
  WHERE: .indexHead .cnt and .emptyBody in ?state=search (notebook.js:1617, 1624)
  FIX: Render "1 of 14 notes has “marquee” in it" / "3 of 14 notes have “marquee” in them" from an explicit singular branch, and gate plural agreement in notes-interaction-check.mjs at n=0, 1 and 2. Change the miss line to the specimen's grammar: "The closest is one about the marquee sides, from 35 minutes ago." Strip the trailing stop before concatenating the announcement, and shorten it to "The words you picked: … . These words cross, nothing else from this note."
--- 19. Product taste and emotional resonance | dictation-floor-is-a-still-life | cost 4
  WHERE: notebook.js:1500 `<span class="darkTime tab">0:07</span>` and :1492 the 28 sine-derived `.darkWave` bars — ?state=voice / pressing Dictate
  FIX: Start a `setInterval` at 1000ms on entering the voice state that increments a `voiceSeconds` counter from 0 and writes it into `.darkTime` as `m:ss` via `textContent` only (never a repaint, so the transcript is not rebuilt under a speaking person), and clear it on `voice-stop` and `voice-cancel`. Render the timer as `0:00` on arrival. Replace the static bar heights with a 60ms `requestAnimationFrame`-throttled loop that writes each `<i>` height from a smoothed random walk clamped to 6–40px, so the wave breathes; drive it from a single amplitude variable so it can later be fed by real input level. Under `prefers-reduced-motion` hold the bars at their current heights and keep the timer ticking — the count is information, not decoration.
--- 20. Product taste and emotional resonance | undo-announces-the-thing-it-undid | cost 4
  WHERE: notebook.js:195 `say(`Undone. ${label}`)` with labels from :267 "Saved.", :672/:681 "Sent to Tasks.", :1936 "Moved to …", :1984 "Dropped one of them."
  FIX: Give `offerUndo` a second string. Change the signature to `offerUndo(label, undoneLabel, revert)` and have `doUndo` call `say(undoneLabel)` instead of `` `Undone. ${label}` ``. Supply, in order: "Put back. Your note is in the field again." for the keep at :267; "Taken back. Nothing crossed into Tasks." for both sends at :672 and :681; `` `Put back under ${previous.label}.` `` for the move at :1936; "Put back. Both notes are here." for the dropped piece at :1984; `` `Put back. Nothing was kept from what you said.` `` for :2091. Every one of them must state the state the person is now in, never the act that was reversed.
--- 21. Product taste and emotional resonance | search-hides-why-it-matched | cost 4
  WHERE: notebook.js:730 `visible()` substring filter, :1617 the count line, and the `#say` region — Ctrl+K, "marquee sides"
  FIX: In the search room, render each row's subtext from the match rather than from the head of the body: find the query index in `n.body`, take a window from the last space before `index - 40` to the first space after `index + query.length + 40`, and set that as the row's rest, with the matched span wrapped at weight 600 by the existing `esc`-safe highlighter at :1158. Ellipsise both ends. Separately, debounce `say` at 220ms on every input into `#q` and announce `rows.length ? `${rows.length} of ${counts().total} notes.` : `No note says “${query}”. The closest is ${nearest(query).title}.`` so the region always states the current result set and never the previous one.
--- 22. Measured evidence | undo-timer-eats-focus | cost 0.5
  WHERE: notebook.js offerUndo() → setTimeout(…, UNDO_SECONDS*1000) → paint(); paint() only restores focus for TEXTAREA/INPUT or an explicit `refocus`
  FIX: Stop repainting on expiry. In the offerUndo timeout, replace `undone = null; paint();` with `undone = null; const strip = mount.querySelector('.undo'); if (strip) { if (strip.contains(document.activeElement)) { const back = mount.querySelector('.idxRow[data-cursor]') || mount.querySelector(refocus_field_sel); back && back.focus({preventScroll:true}); } strip.remove(); mount.querySelector('.sheet')?.removeAttribute('data-undo'); }` — a node removal costs no focus unless focus is inside it, and that one case is handled explicitly. Keep paint() for acts the operator initiates.
--- 23. Measured evidence | search-cursor-invisible | cost 0.4
  WHERE: master.css:564-565 `.idxRow[data-cursor]` / `.index:focus-within .idxRow[data-cursor]`; the search field #q lives in `.searchBar`, outside #index
  FIX: In paint(), alongside `root.setAttribute('data-group', group)`, add `root.toggleAttribute('data-searching', Boolean(query))`. In master.css add `[data-searching]` to the existing lit selector so it reads `.index:focus-within .idxRow[data-cursor], [data-searching] .idxRow[data-cursor] { background: var(--indigo-wash); box-shadow: inset 0 0 0 1px var(--indigo-line); }` (and the same for the `.idxMark i` halo pair at 573-574), then add `[data-searching] .idxRow:hover:not([data-cursor]) { background: none; }` so the pointer cannot impersonate the cursor.
--- 24. Measured evidence | phone-dock-clips-last-row | cost 0.35
  WHERE: #index padding-bottom in the phone media query (22px) against `.dock` (103px tall, floating) at 390×844, isMobile
  FIX: Measure the dock once and reserve it: in paint(), after mounting, `root.style.setProperty('--dock-h', mount.querySelector('.dock').offsetHeight + 'px')` (re-run from the existing resize handler). In the phone media query set `#index { padding-bottom: calc(var(--dock-h, 103px) + 16px); }` and add the same `scroll-padding-bottom` so the arrow walk cannot park a row under the dock either.