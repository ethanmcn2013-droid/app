Pre-GTM Week 0 — Templates Cycles T-1 + T-2.0 + T-2.1c closed 2026-05-12 (wedding lifted to canonical four-layer source; workspaces.template_id field shipped; Tasks remix toast now invites a Roadmap for canonical templates). T-2.2 (Notes lazy expression) is the next templates cycle. Plan 8 · Cycle 8.3 (Venue Editions Tasks-side wiring) closed 2026-05-13 — venue-welcome detection helper joins entitlement→comp_code→sponsor JSON; welcome page short-circuits first-run for venue redemptions, auto-applies wedding-planning-workspace template + sets workspaces.template_id + active_domain='wedding'; new VenueWelcomeCard client component shows dismissible "Compliments of [sponsor]" card on /app/board; welcome-picker techy copy replaced ("Pick a starter so you have something to play with" → "Pick a starter. Your workspace fills with a real example to edit."; "Loaded · ready to open" → "Your starter is in"); ties into studio's reconciled /redeem/[code] landing + dual-write issue-codes.ts pipeline. Three LAMBSHIL test codes seeded in Tasks comp_codes for Lamb's Hill pilot. End-to-end live test deferred to first deploy. Cycle 8.4.5 closed 2026-05-13 — redemption polish bundle shipped (commit d7215f7): sign-up page now renders sponsor eyebrow + sentence + code strip above Clerk SignUp when redirect_url matches /redeem/CODE (new lookupSponsorByCode helper in src/server/db/venue-welcome.ts), /welcome cut from venue path (wedding-template apply + workspaces flag moved inline into redeemCompCodeAction, gated on first-redemption to avoid double-apply on idempotent re-hits; result card now deep-links to /app/board?welcome=venue&v=<slug>, /welcome retained as defensive fallback), success card palette swapped from emerald to --aud-wedding rose #be185d (check icon + entrance motion retained). Typecheck + build clean. Cycle 8.5 next, gated on (a) Clerk webhook secret rotate + redeploy and (b) one incognito end-to-end walk. Cycle 8.4.7 closed 2026-05-13 — "did the next person finish?" signal shipped: schema gains entitlements.reached_board_at (nullable timestamp), markVenueEntitlementReached helper stamps it idempotently on first /app/board?welcome=venue render (wrapped in try/catch so a measurement failure can't break board render), drizzle/0001_add_reached_board_at.sql migration written; studio /hq/partners gains "Reached board" column with try/fallback SELECT pattern so the cross-DB read survives the pre-migration window; partner-digest gains a sentence narrating the gap when reached < redeemed. Conscious non-builds: per-event funnel table, email open tracking, engagement column — all deferred per pilot-scale restraint. Tasks commit pending push. Operator action: apply drizzle/0001 ALTER to prod Turso. Cycle 8.4.6 closed 2026-05-13 — Sentry capture wired on Clerk webhook (missing-secret-in-prod fires captureMessage; handler dispatch wrapped in try/catch + tag-and-rethrow with webhook=clerk + eventType + svixId + eventDataId) and on redeemCompCodeAction (try/catch around impl + captureException with action + code tags); studio-side `active30d` renamed to `redeemed30d` across stats helper + /hq/partners column + partner-digest CLI; page descriptor copy updated to spell out the metric's honest meaning (new redemptions in 30d, NOT engagement). Typecheck + build clean. Plan-9-era follow-ups deferred per cycle scope: comp_redemption_events funnel table, tasks_created_after_redemption usage column.
