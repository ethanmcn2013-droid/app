# Home Project context materiality review

Reviewed source: `aaadb498df493766ae86acbb906b7f81feb228a4`, applied without conflict onto integration `c52876589691fb4d2a4aa8232601a5f39457327b` as `e7a13fab65fd56d2d2de22ae52ff06c12d5a344e`. The changed registry source is `src/app/app/home/page.tsx`, with companion Home data, route snapshot, selector and Tasks naming changes in the same reviewed commit.

The change lets a bare Home visit by a Tasks creator with no saved Signal scope find a Project through the Tasks-owned, current-member route resolver. Home then requests that exact internal Project ID from Signal, whose catalog independently authorizes it before returning content. An explicit Home scope never falls back to an ambient Project or writes a preference. The Project name in shared chrome comes from a Tasks-authorized Project summary, and a pending switch settles only after the matching Project, route and epoch publish a verified snapshot. The Add Project row uses the guarded V3 switch after creation; the Floor title prefers a saved board title, then the persisted Project name, then example copy.

The source review accepts that bounded change. The `tasks.page.app-home` entry is a mapped critical fixture, so `node scripts/experience/critical-fixtures.mjs --write` refreshed its source materiality hash from `a1c347b2c0706831` to `ef053744589ec7e0`. That official command changed only this hash in `experience/registry.json`. Its historical `lastReviewedAt`, coverage fields and fixture manifest are unchanged; the date of this review is 2026-09-23.

Checks on integrated `e7a13fab`: Home, Project state-machine and Floor naming tests 31/31; active Project contract 25/25; module-boundary check and diff check passed. The source author separately ran a production-classified demo build and TypeScript check on frozen `aaadb498`, plus a local desktop and phone Home browser check at HTTP 200 with no overlay or console errors. [Desktop](./home-desktop.png) and [phone](./home-phone.png) are the saved captures from that exact Home source. They show demo seed content (`The Orchard, events`), not a fresh real account. The existing mapped Playwright attestation is historical regression context; it did not verify the new no-preference creator branch. Real authenticated provider behavior and the remaining critical fixture states still require receiving proof before release.

This integration worktree shares a dependency junction missing `@clerk/testing/playwright`; direct TypeScript here reported only that missing package and its consequent type errors. The source author's isolated install passed TypeScript and build, while independent receiving CI on `e7a13fab` is the current combined gate. No provider, migration or deployment action was performed for this registry update.
