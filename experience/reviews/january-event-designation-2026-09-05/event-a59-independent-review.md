**Scoped verdict: one P2 test correction required; no runtime business-rule or migration defect reproduced on the reviewed paths.** This independent review covers Event foundation `a59dbd3e5a2f6fb8badb3a6de8a3c40cc6f7ffec` against `8172364e3f7ba8332b4ce7a73ae4563a34b7867c`. The foundation's transaction, designation, negative-precedence and erasure behavior passed the checks below. Correct EV-TEST-01 before adopting the proposed mandatory Event suite. This is not approval of the later Event enforcement milestone or sales reopening.

**EV-TEST-01 — P2: the new mandatory test fails in the January2027 launch year.** Exact location: `src/server/db/event-designation.test.ts:375`, with the year-dependent fixture at `src/server/db/event-designation-test-fixture.ts:33` and `:37`.

The test labels a shared account grant as “future” but hardcodes its start to `2029-01-01`. The fixture sets `year = current UTC year + 1` and evaluates archive access at February21 of `year + 1`. With the process clock at January21,2027, that evaluation is February21,2029: the supposedly future Studio grant has already started. The actual evaluator correctly returns `editable`, while line380 expects `read_only`. The unchanged test exits1. It passes with today's2026 clock, which explains why the original36-test run did not expose it.

Reproduction ran the actual immutable test, fixture, writer and evaluator with real disposable SQLite, changing only the process clock in a review-owned preload. `launch-year-counterexample.json/.log` preserves exit1 and the `editable` versus `read_only` assertion. A separate test copy changes only the grant's start expression to `AFTER.getTime() + 86_400_000`; the same January2027 clock then passes1/1, exit0 (`launch-year-control.json/.log`). Both original and control remain preserved; the author test was not edited. Recommended fix: derive the future grant from the test's evaluation instant and retain a launch-year clock control. This is a test time-dependence defect, not an entitlement overgrant.

Reviewed author checkout: `C:/Users/ethan/signal-studio-workspace/worktrees/app/feat-january-event-designation`. Execution used separate task scratch at `C:/Users/ethan/Documents/Codex/2026-09-04/here-are-6-prompts-i-have-2/work/event-a59-review/source`. All17 changed inputs in scratch and the author tree match immutable Git content after CRLF normalization; raw/archive/Git hashes are recorded in `source.json` and `final-integrity.json`. The author stayed at a59 with no tracked diff. Principal and author sources, hooks and existing evidence were not changed.

Policy was read from App `docs/execution/january-2027/EVENT-ACCESS-CLOSURE.md`, the new `EVENT-DESIGNATION-MILESTONE.md`, and Studio `content/hq/decisions/event-project-funding-2026-09-04.md`; readback copies are in `reproduction/`. The erasure treatment and explicit non-enforcement boundary were assessed as declared, without inventing a broader release requirement.

| Reviewed mechanism | Evidence and result |
| --- | --- |
| Checkout → settlement binding | The actual action carries an explicit Event project and an intent idempotency key; current primary ownership is checked inside both local checkout transactions. Settlement binds the intent/reference/purchaser/project and original term in the local purchase transaction. An independent actual-lifecycle case substitutes another project's valid intent: rejected without grants; the exact original delivery succeeds and leaves the other intent pending. |
| Owner transfer and partial shared/local failure | Independent late-local-insert failure leaves a pending intent and a shared mirror. Transferring ownership before redelivery produces immutable `paid_undesignated`, no local positive grant; restoring ownership does not adopt it. A refund subsequently resolves it negatively and retires the shared mirror. This is recoverable separate-store ordering, not cross-store atomicity. |
| Owner account versus project cover | Existing tests distinguish current-primary-owner account grants, other members' personal grants, other-project grants and explicit project-covering venue grants. Independent erasure/cover checks confirm a member's personal Studio benefit does not rescue project access; an explicit valid project cover does. An outsider remains unauthorized. |
| Multiple purchases and negative precedence | Existing tests retain an independently completed purchase after another refund. New FK-on/off cases use two different primary-owner purchasers, refund the former owner's purchase, transfer ownership and erase that former purchaser: the second completed term still supplies read-only access. Repeated erasure and delayed old settlement do not remove/recreate its authority. |
| Original expiry and revocation | An independent altered-expiry positive duplicate cannot extend local, shared or designation dates. Refund then late-positive replay leaves local epoch0 and sticky designation revocation. SQL constraints/trigger tests reject changed reference/project/purchaser/settlement dates and revocation reversal. |
| Purchaser/project erasure | Original tests execute the actual account eraser with FK ON/OFF for positive and refunded purchases after transfer. Independent BEFORE UPDATE fault cases prove the user deletion and designation anonymization roll back together; retry succeeds. Surviving effects remove purchaser/reference/checkout identity, preserve the original term/negative fact, and disappear on project deletion. FK and integrity checks pass in the two-purchaser cases. |
| Migration/ledger | The exact additive0031, journal, receipt and ledger pass27 existing ledger tests, including populated pre0031 history, forced-proof rollback, retry and no-op. One new test populates an Event fact after migration: ordinary no-op still passes; replacing the immutable trigger with an inert same-name trigger is rejected while the fact and ledger row remain. |

All executable commands used **Node22.23.2**, installed dependencies through a read-only target from a junction in review scratch, and an OS-only child environment with explicit review/preview mode. `--import ../deny-network.mjs` rejects fetch/HTTP/HTTPS/TCP calls before the test imports. The existing Event fixtures additionally forbid fetch, use a disposable local file DB bootstrapped from the owning SQL, and a separate in-memory shared fixture. The latter uses the repository's existing synthetic entitlement table fixture; this review does not claim a full shared-store migration rehearsal.

| Receipt | Actual result |
| --- | --- |
| `original-event-tests.json` | Original two-file Event suite36/36, exit0 |
| `independent-event-final.json` | Eight independent SQLite cases,8/8, exit0 |
| `independent-migration.json` | Populated no-op / exact-trigger drift control1/1, exit0 |
| `billing-regressions.json` | Declared five-file billing command40/40, exit0 |
| `migration-ledger.json` | Existing Tasks ledger suite27/27, exit0 |
| `migration-contract-complete-inputs.json` | Existing migration contract check, exit0;32 Tasks SQL entries and2 Timeline entries |
| `scoped-contracts.json` | Existing tenant/Stripe contract files28/28, exit0 |
| `ambient-contract.json` | Existing ambient-workspace check, exit0 |
| `launch-year-counterexample.json` | Actual new test under2027 clock:0/1, exit1, preserved |
| `launch-year-control.json` | Single-expression review-only test control under the same clock:1/1, exit0 |

Exact command arrays, cwd, timestamps, exits and log hashes are in each JSON receipt. The unchanged author suite and main counterexample commands, after the absolute Node executable and network preload, were:

```text
--import tsx --import ./src/test/register-server-only.mjs --test --test-concurrency=1 src/server/db/event-designation.test.ts src/server/db/event-designation-webhook.test.ts

--import ../clock-2027.mjs --import tsx --import ./src/test/register-server-only.mjs --test --test-name-pattern="expired/revoked/future independent grants" src/server/db/event-designation.test.ts
```

Reviewer failures were not discarded. The first full archive extraction hit Windows path length in an unrelated historical evidence directory; the partial scratch remains unused. A source-only extraction initially omitted `drizzle-timeline`, so the shared migration-contract command exited1 for its missing ledger. Adding that exact immutable prerequisite allowed the unchanged check to pass. Both preparation failures are recorded separately from EV-TEST-01. The first independent8-case pass is also retained; the final rerun only clarifies one test title and retains its assertions. No native process exit occurred in the reviewer runs. The author milestone document reports its earlier monolithic native exits; their separate sealed bundle was not available at the stated output path during initial inspection, and this review does not independently attest those historical logs.

No production consumer of `readEventProjectAccessWith` was found. Existing role/manual-archive policy still controls current routes and writes; an expired Event can therefore remain writable through today's adapters, exactly as the milestone explicitly documents. This is open next-milestone work, not a new bypass finding. Erased positive provenance deliberately yields `verification_unavailable` without independent current cover; it is not asserted as Free or an archive. Project-deletion conflicts record `paid_undesignated`; the existing account-deletion fence retains its separate terminal/no-positive-write behavior. The sales hold still refuses real Event checkout before auth/writer/provider work. The test-only action override is not a runtime reopening path.

0031 must precede this writer code on any later authorized target: even legacy Event settlement now consults the new table. A proposed additive migration and passing local ledger tests do not establish that any receiving/production store has applied it. Owner recovery/export, private content and attachment enforcement, Timeline/public/cache adapters, rendered capabilities, historical designation reconciliation and sales reopening remain explicitly absent. No full typecheck/lint/build/receiving CI or browser acceptance was rerun or claimed here; the unmodified Timeline test suite was not rerun as part of this bounded Tasks migration review.

No provider/network/production/browser/server, Atlas, RC-3 or workbench action was performed. No source fix was made. This report requests only the bounded EV-TEST-01 fixture correction and preserves the successful foundation results without promoting them into full Event access closure.
