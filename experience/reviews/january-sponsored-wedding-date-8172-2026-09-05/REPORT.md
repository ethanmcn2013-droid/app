# Sponsored wedding date: final8172 component renewal

All **32 component state/viewport checks and3 keyboard checks passed** against frozen principal `8172364e3f7ba8332b4ce7a73ae4563a34b7867c`, using completed normal build `wnk1OREKC5EKuma6I3VoO`. No runtime, package, principal or Studio files were edited. This is the owning component evidence packet; full receiving gates and registration remain principal-owned.

## Source and build

| Item | Exact identity |
| --- | --- |
| Read-only source checkout | `C:/Users/ethan/signal-studio-workspace/worktrees/app/feat-january-final-journeys` |
| Captured source | `8172364e3f7ba8332b4ce7a73ae4563a34b7867c` |
| Production correction | `054b3b83`: exactly five `min-h-11` → `min-h-[44px]` replacements in TasksPage, WeddingDateForm and VenueWelcomeCard |
|8172 change | Recipient test fixture uses the real calendar frame constructor; outside `src` |
| Normal build | Node22.23.2 / Next16.2.11, exit0; original receipt retained as `principal-build.receipt.json` |
| Evidence owner | `feat/january-sponsored-wedding-date`, base `0b9d132ac5b5f47579c8630becb94063b1db947d`, pre-evidence head `3a6594f2d0cd771d8b2029dac29d32cd09eb15d5` |
| Fresh task scratch | `work/sponsored-wedding-date-8172-renewal-2026-09-05` under the originating chat |

Ten actual source inputs have immutable Git blobs/readbacks and working-file SHA256 identities. All remained unchanged after rendering. Twelve normal build CSS files and eleven WOFF2 files were copied and hashed before capture, then matched against the read-only build afterward. The browser reported Geist ready in every state. This run used completed normal build styles/fonts, without a cached-font mock or a new build in this lane. The fixture served those local assets; it did not contact a font provider.

## Executed checks

| Check | Result |
| --- | --- |
| Sixteen states at1440×1080 and390×844 |32/32, exit0;56 saved PNGs including form crops and keyboard captures |
| Native date field → Save by Tab; Save by Enter; member arrival link → read-only form by Enter |3/3, exit0 |
| Date field, Save, Clear, persistent arrival link and welcome link | Computed `min-height:44px` and actual height≥44px at both widths;59 repeated visible-control measurements across the32 states |
| Horizontal overflow | None in all32 state assertions |
|32-state browser console/page errors, HTTP≥400, external requests |0 each |
| Isolated seed and readiness | Exit0; HTTP200 |
| Owned synthetic fixture | Port4494 verified free; PID21944 stopped after checks; no listener remained |

The states cover missing date, later/earlier saves without shortening, clearing, injected entitlement-write failure retaining both grant and draft, successful repair/retry, expired and revoked access, date-save preserving revocation, member read-only form, ordinary non-wedding project, manager/member welcome and persistent arrival paths, dismissal, and exact-project arrival-to-form navigation. Ten representative saved renders were visually inspected, including failure/retry, revoked, member, mobile arrival and keyboard focus. Existing generic target copy stays visibly separate from the canonical wedding date.

Exact commands, arguments, working directories, timestamps and exits are in the per-command receipts. Execution was sequential: source preparation → completed-build binding → isolated seed → free-port/server/readiness →32-state capture →3-keyboard capture → source/asset verification. No command failed in this renewal. Broader suites were not rerun merely for this evidence task. Principal-reported broad gates are not counted as this lane's evidence.

## Retained fixture boundaries

The actual TasksPage, VenueWelcomeCard, ProjectOverview and WeddingDateForm render in Chromium. Client action transport goes through a loopback endpoint that executes the actual `saveSponsoredWeddingDate` action body with the actual reader/writer, project authorization and isolated SQLite transactions.

The existing adapters explicitly fix identity to synthetic `david`, resolve Tasks arrival to an explicit ready project, replace the board body/runtime mount and toast, supply synthetic venue welcome and overview metadata/statistics/member list, make entitlement-reached/revalidation/router-refresh calls no-ops, and inject a disposable local database. Source and adapter text are retained in `component-fixture-boundaries.json` and the harness files. Seed rows, time and original32+3 assertions were retained; scratch imports/root/port and normal asset path were adapted. The only additional assertions measure the five corrected touch targets.

## Limits and preservation

This is ordinary component/browser proof, not full Next/Clerk, Flight/session ordering, provider, customer or council evidence. The original keyboard helper does not separately collect console/page errors; its three interactions and zero external requests passed. Remote-client transaction contention remains unknown; this run's rollback/retry uses a local isolated SQLite trigger. Independent c1 ordering verification remains a separate retained report and was not rerun or claimed as new here.

The earlier Clerk denial was the fixture's intentionally enforced external-request guard/missing test configuration, not an automatic approval rejection; it was not retried. No RC3, Atlas or provider retry occurred. No publication or January outreach was authorized.

The original189-file packet, legacy45, WDATE01 61, WDATE02 102, ffef142 and stopped7e199 69 files all still match their retained manifests. The ffef32 remains non-final;7e199 had preparation/seed only and0/32+0/3 checks. These packets were not overwritten. Four untracked Git LFS hook files in the owning worktree remain untouched.

`artifact-index.json` identifies every ZIP entry and SHA256; `archive.receipt.json` records full decompression/hash verification. Scripts, synthetic fixtures, source readbacks, normal assets, logs and screenshots are inside the LFS archive to avoid exposing historical `.cjs` evidence as active lint inputs. No lint exclusions or package/CI changes were added. The new chat output includes an unpacked evidence copy and final commit receipt in `SEAL.json`.
