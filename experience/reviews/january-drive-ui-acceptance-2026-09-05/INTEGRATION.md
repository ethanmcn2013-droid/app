# Drive UI follow-up: internal composition

Principal candidate `29193167888f70f4ab3d5b0cab09021a709a5f30` merges runtime
`8ae94ffb` and author evidence `27af50c0` with the preceding `ce81e066` evidence
checkpoint. The source changes are the two views and their six-test follow-up
file. No backend, schema, entitlement or provider behavior changes in this slice.

The principal verified all 624 archive entries, the ZIP checksum/CRC and all 22
recorded source files against immutable Git. The independent reviewer reproduces
the parent disclosure failure and passes 35 focused plus four independent checks.
That complete 21-file review is preserved in `independent-8ae.zip`; its manifest
and checksum are in `independent-8ae.json`. The original author archive is unchanged.

Full App typecheck, full lint, the declared 82-test Drive UI command and the
first-contact language gate pass on the composed source. Commands, environment
allowlist, exits and logs are under `integration/`. The principal also inspected
the saved mobile pre-consent disclosure, restored handover focus and settled
Resources completion. No new browser or provider run is claimed by those reads.

Only the two changed surfaces receive new source/materiality receipts. Existing
coverage levels and design approvals remain unchanged. The receipt's referenced
132-case artifact is explicitly the preceding 8172/6eb suite regression context;
the separate author component packet and independent review verify this UI patch.
Registry, critical-fixture and stored-attestation validation pass. A fresh composed
Linux/build/full-story pass remains required after publishing this candidate.

Persistent unconfirmed-disconnect visibility after reload is a separate active
service/read-model investigation. Provider lifecycle, full Next/Clerk journeys,
screen-reader/human/council acceptance and receiving integration remain open.
The stopped RC-3 and Atlas actions have not been retried or replaced by this work.
