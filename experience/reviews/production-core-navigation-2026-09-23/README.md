# Core navigation review

Source: `feat/production-core-navigation`, based on App integration `e47fc8974c3cfaffa5b99a5a5441c35a18cd0df3`. This is an unshipped candidate. The screenshots were captured from this branch's final application code using a local optimized build; the only subsequent edits were to tests and this review record.

Run: `NEXT_PUBLIC_SIGNAL_ACCESS_MODE=demo VERCEL_ENV=preview corepack pnpm build`, then `corepack pnpm start --hostname 127.0.0.1 --port 4398`, then `node experience/reviews/production-core-navigation-2026-09-23/verify.mjs`. On Windows PowerShell, set the two environment variables in the process before each build/start command. The test uses Chrome at `http://127.0.0.1:4398`; no connected provider or real account is involved. This preview was started as the owned `next start` process on port 4398 and stopped after the review.

The browser pass covers 1440×900, 390×844 and 320×720. It follows Home → More → Notes → Projects → Tasks; checks the desktop rail and standalone Tasks Floor, Escape focus return, the Project active state, visible Tasks views, a full-height phone Floor, and Add task in More at 320px. See [the machine-readable result](./result.json) and the screenshots in this directory. The screenshots show [Home on desktop](./home-desktop.png), [Home on phone](./home-phone.png), [More on phone](./more-phone.png), [private Notes](./notes-phone.png), [Projects](./projects-phone.png), [Tasks on desktop](./tasks-desktop.png), [Tasks More on phone](./tasks-more-phone.png), and [Tasks More at 320px](./tasks-narrow-phone.png).

The final browser pass had no page errors or non-cancelled local request failures. Next cancelled in-flight prefetch/script requests during quick route transitions; the result counts those `net::ERR_ABORTED` requests separately, and each destination was checked after rendering. The build passed with an existing Turbopack dynamic-filesystem tracing warning in `src/server/storage.ts`; this navigation change does not touch that path.

Checks: `pnpm typecheck`; focused ESLint (no errors; one pre-existing `useCallback` dependency warning in `hybrid-workspace.tsx`); `pnpm test:suite-url-and-switcher` (32 contract tests); `node --test src/server/projects/route-authz-contract.test.mjs` (28 tests); `pnpm test:floor-theme` (6 tests and generated CSS parity); `pnpm check:suite-switcher`; `pnpm build`. The no-project setup state is source-guarded but was not exercised with a real account. No invitation, provider, or production release claim is made here.
