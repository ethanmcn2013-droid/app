# January programme —6 September checkpoint

This internal checkpoint preserves reviewed work and records a deliberate stopping point for today. Commercial launch and first outreach remain21 January2027. Receiving App77d22a6b/Studio4ed9be64 are unchanged and held. The programme estimate remains approximately60%; S6 alone is closed. Nothing here authorizes production, provider mutation or customer sends.

App9be3943d composes exact-project recovery3f276/3b8bd, the exact host/module exception edf9edbb, and reviewed shared schema57c5. Recovery34 cases,7 boundary controls, full typecheck/scoped gates and14 component renders pass;18 rendered source inputs match9be. Original3f and3b archives verify26 and20 bindings. The new recovery registry entry remains core/preview with partial coverage and no approval.

Shared review passes104 owning cases and86 independent controls. Both immutable historical-retry failure and new-receipt downgrade are fixed. Historical unversioned receipts retain limited coverage; fresh adoption requires a complete versioned fingerprint. Production adoption/executor and old backup completeness are not certified.

The Windows composed DB gate and serial diagnostic each report125 passing assertions but fail on a Notes/Signal native process exit3221225477. Linux9be's actual combined database gate, isolated migration rehearsal and typecheck pass. Its CI/Verify then fail a stale demo-order source assertion referring to the previous revocation helper. The test-only correction packaged with this receipt targets the actual identity, reviewed recovery writer and revalidation boundaries. A mutation control rejects removed/late guards;16 focused tests and lint pass. Fresh final Linux results must be read from the programme acceptance register, not inferred here.

Floor80b835d remains separate and unaccepted. Independent182/192 assertions pass;10failures are preserved. Plain-arrow focus and local-store sibling cancellation ordering reproduce on baseline. Mobile mixed-history focus is unresolved, with a new regression unproven. Existing geometry is preserved252rules plus exactly2 additions; the unchanged calibration gate remains failed. No Floor code, gate relaxation or package registration is included in the integration.

Archives are individually hashed in archives.json, ZIP integrity checked, and preserve original failures. Floor's252 indexed payloads were independently read and hash-verified during packaging. Principal76 payloads include original recovery serialization failure and both Windows native failures. The independent schema archive contains859payloads plus its manifest; recovery contains241payloads. Earlier author archives retain their own sources and limits.

Open: full Next/Clerk and recipient stories, Event denial/enforcement, provider upload/revoke/handover/erasure, actual restore/retention/operations, human/council acceptance and all final release gates. Reference usability and eventual project deletion fence completion remain limitations. RC3 and Drive lifecycle review stopped for possible cybersecurity risk; Atlas server start was rejected with only blocked by policy. No held review was retried or rerouted. Resume from current Git and the canonical Studio programme register.
