# Event export — integrated internal prerequisite

Runtimeac7a913d and sealed evidence6eba1335 were reviewed by the principal,
separately from their author, then integrated unchanged asa6293a82.
Independent27 export and24 tenant tests pass. Both original author archives under
the January recipient packet retain all failures, raw hashes and source controls.
The principal's archive verification binds the final eight-case author receipt to
the ten exact source inputs. No full audit or independent browser repeat was claimed
by that first review.

The principal composition then passes six commands: full typecheck,27 actual
export tests, focused lint (12 inherited warnings, zero errors), first-contact,
module boundaries and a fresh eight-case component/actual-HTTP browser run.
The latter includes host500, connection abort and same-page success after each.
The owning listener and disposable databases closed. All ten rendered source inputs
still equal6eba. Full commands, bundle/CSS, screenshots and downloaded synthetic
JSON are in composition.zip. The serial12-case Event export command is mandatory
in pnpm test; its owning browser command is in the existing Design workflow and
uploaded evidence path. Earlier test/workflow gates remain unchanged.

Profile materiality reviews the changed imported component while preserving the
unchanged page hash and all coverage/scores/approvals. The inherited registry job
and action encoding was repaired to plain account language. Registry83, critical
37/37 and preceding132 stored attestation validation pass. That stored attestation
is regression context, not a fresh complete profile story.

Native Chromium cancels failed downloads without a completed file. The page remains
available for retry. There is no inline app error or progress, and the physical
download-panel/other-browser presentation remains unverified. The real Next/Clerk
route, all profile states, provider file bytes, human/council and receiving acceptance
remain open. Event recovery must precede content-denial enforcement; sales stay held.
The distinct held Drive lifecycle, RC3 and Atlas actions are not replaced or retried.

See preservation.json for both new immutable archives and exact source comparisons.
Fresh complete Linux gates for this composition remain next. No production migration,
provider call, release, customer send or venue outreach is included.


## Exact Linux acceptance

Candidate6d2d53d3 passes CI33994297426, Verify33994297429 and Design33994297432.
Tested merge6dd6b4ff equals candidate treef78bea7a. The complete132-case built
matrix and eight-case profile-export fixture pass. All290 route,33 Floor,25 calendar
and10 export source inputs match exact Git bytes after documented newline handling.
The owning archive linux-6d2d53d3.zip retains489 payload files plus CHECKPOINT.json,
25,547,707 bytes, SHA2564c71266240357e46aa5b2b5040c2d50214ba62d85150f33ff81b128fedb91427.
Its adjacent JSON records every file and source binding. This supersedes the pending
Linux statement above, preserving all earlier failures and scope limits. No receiving,
RC3, held Drive lifecycle, Atlas, provider, human/council or release gate is closed.
