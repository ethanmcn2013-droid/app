# Event export — principal independent review

Scoped acceptance of runtimeac7a913d and evidence6eba1335, authored by Plato.
The principal reviewed the exact exporter, account HTTP endpoint, changed profile
control, unified/actual SQLite tests, and the component failure/retry fixture.
The principal separately ran27 export and24 tenant tests: both commands exit0,
with exactly27/24 passes and zero failures. Complete commands and logs are retained.

Purchaser facts remain tied to the freshly resolved account identity, independently
of current project membership. Current primary-owner project effects omit the former
payer, checkout/payment reference and erased row identity; the added ownership join
rechecks the effect projection. The existing Event entitlement projection is similarly
bounded. Other entitlement shapes and the independent profile DangerZone remain.
No stored term is inferred from a revoked expiry, and missing host schema fails the
download rather than silently omitting Event data.

Both immutable author archives match their manifests and pass ZIP integrity. All
final browser source inputs match exact6eba Git bytes after documented newline
normalization. The principal inspected saved mobile dark, desktop light and host500
renders. The eight-case receipt retains cancelled500/connection failures and working
same-page retries. A native browser download is used: no inline error or app-owned
progress is rendered, and the physical browser download-panel presentation is not
verified. These limits prevent any complete experience/human certification claim.

This is source review plus independently repeated declared tests and evidence
verification, not an exhaustive audit or an independently repeated browser session.
No full Next/Clerk/provider flow, Event content-denial enforcement, receiving, council
or production acceptance is supplied. The separate stopped Drive lifecycle and RC3
reviews are not repeated or substituted by this unrelated export review.

Principal integration must register the new12-case Event export file and owning
browser command, preserve these archives, update the changed profile materiality,
and run the combined candidate's declared gates. Event recovery precedes enforcement;
sales and production migrations stay held.
