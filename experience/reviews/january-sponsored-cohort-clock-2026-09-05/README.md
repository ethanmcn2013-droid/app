# Sponsored-use reporting cohort clock correction

Source6d49de54 changes only the paired acceptance fixture, its four-clock wrapper
and package registration. App/Studio runtime, schema and privacy thresholds are
unchanged. This is a test-fixture repair, not a changed customer metric.

Both a8c Linux CI/Verify failed after the third synthetic grant began on the next
Dublin day; Design and its132-case artifact passed. Independent diagnosis also
reproduces the same evening failure on preceding3be. The real rollup correctly
counts only two projects on the original event day and withholds their metric.

Quiet grants now keep original app.now-relative terms while the maintenance
clock advances. The fixture asserts the actual rollup has three eligible projects,
retaining the two-project withheld and three-project lower-bound expectations.
The mandatory paired command runs the actual nine-check route/action/SQLite story
at08:00,15:00 and21:00 UTC on5September2026 and21:00 UTC on21January2027. Each child
uses an OS-only environment, real-network denial and an explicit synthetic clock;
it must exit0 and emit exactly one completed nine-check report. No empty run passes.

Principal: all four complete stories pass9/9, and focused lint passes. Independent
diagnosis separately passes the proposed term anchor at morning/boundary/evening
and preserves original failures and actual derived cohorts. Final6d wrapper review
and fresh Linux remain pending. The earlier a8c failed logs are preserved in the
Event foundation's linux-a8c225c7 archive; they are not waived or relabelled.

gates.json records exact commands, paired4ed revision and outcomes. source.json
binds the three tested files to6d. The archive retains author evidence and the
complete immutable independent diagnosis with its source/clock/countercontrols.
No provider, human/council, receiving, RC3/Atlas or production gate is closed.

## Independent correction acceptance

Exact6d49de54 now passes scoped independent verification: four actual9-check
stories, original mutable-term refusal at the Dublin boundary, missing-completion
and nonzero-child controls, environment filtering and real-network denial. Runtime,
schema and original diagnosis archives are unchanged. INDEPENDENT-FIX.md and its
separate archive retain exact commands and limits. Fresh full Linux remains next.

## Exact Linux checkpoint

Candidate0c93a5cb now passes CI33992802862, Verify33992802886 and
Design33992802885. Tested mergee893094b has exactly candidate tree2419ad23.
The complete logs and uploaded132-case artifact are preserved in linux-0c93a5cb.zip:
468 payload files plus CHECKPOINT.json,24,985,596 bytes, SHA256
aa76f913a6176202621411152fefe212e7125c82c758ce6e30861c265a27ef0c.
All290 route,33 Floor and25 calendar source inputs match the exact Git candidate
after the receipts' newline normalization. The original faileda8c runs and independent
controls remain intact. This supersedes the pending Linux wording above. It does
not close receiving, RC3, provider, human/council or release acceptance.
