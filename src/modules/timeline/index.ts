/**
 * Timeline module public surface.
 *
 * The module home is a server-component placeholder rendered inside the
 * /app chrome while Timeline migrates into Signal Studio. Import only from
 * this barrel; do not reach into src/modules/timeline internals directly.
 */
export { TimelineHome } from "./home";
