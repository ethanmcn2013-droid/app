import type { ConceptMeta } from "../../types";

export const meta: ConceptMeta = {
  view: "overview",
  n: 5,
  title: "The route map",
  thesis:
    "A project is a set of workstreams converging on one destination, so Overview is a transit-style map where lines run to the big day, stops are milestones, a held stop visibly holds up everything downstream and people ride their lines.",
};
