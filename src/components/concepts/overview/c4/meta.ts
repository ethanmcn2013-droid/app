import type { ConceptMeta } from "../../types";

export const meta: ConceptMeta = {
  view: "overview",
  n: 4,
  title: "Your calls first",
  thesis:
    "The morning job is not to look at the project but to unblock it, so Overview is a focused, keyboard-driven run through the handful of decisions only you can make, each with its consequences shown before you choose.",
};
