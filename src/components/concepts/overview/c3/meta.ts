import type { ConceptMeta } from "../../types";

export const meta: ConceptMeta = {
  view: "overview",
  n: 3,
  title: "Health atlas",
  thesis:
    "A lead needs to diagnose, not just list, so Overview is a vital-signs matrix across projects and dimensions where every coloured cell explains itself in one sentence and three facts.",
};
