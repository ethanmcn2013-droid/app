import type { ConceptMeta } from "../../types";

export const meta: ConceptMeta = {
  view: "projects",
  n: 5,
  title: "Living project page",
  thesis:
    "A project is best understood as one living page that writes its own status in plain sentences, with every project a keystroke away in a slim portfolio rail.",
};
