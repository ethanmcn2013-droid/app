import type { ConceptMeta } from "../../types";

export const meta: ConceptMeta = {
  view: "projects",
  n: 4,
  title: "Project map",
  thesis:
    "A spatial map where every project sits by when it lands and how it is going makes collisions, crunches and drift visible in one glance, and zooming in on any project becomes its home.",
};
