import type { ConceptMeta } from "../../types";

export const meta: ConceptMeta = {
  view: "files",
  n: 3,
  title: "What changed",
  thesis:
    "Files is a newsroom of change: a readable feed tells you what moved since you last looked, with the actual differences shown inline, so you trust the latest version without opening anything.",
};
