import type { ConceptMeta } from "../../types";

export const meta: ConceptMeta = {
  view: "files",
  n: 1,
  title: "Inspector",
  thesis:
    "Files is a keyboard-first desk where the file you're on is always open: a live previewer shows its content, its versions and every task it belongs to, so you never open a new tab just to check something.",
};
