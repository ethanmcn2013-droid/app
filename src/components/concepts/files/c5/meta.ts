import type { ConceptMeta } from "../../types";

export const meta: ConceptMeta = {
  view: "files",
  n: 5,
  title: "Ask the files",
  thesis:
    "Nobody browses files, they look for one: a search-first librarian answers questions like 'the latest seating plan Mara approved' with the answer quoted from inside the file, source attached.",
};
