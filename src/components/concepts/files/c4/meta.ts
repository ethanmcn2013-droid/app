import type { ConceptMeta } from "../../types";

export const meta: ConceptMeta = {
  view: "files",
  n: 4,
  title: "The moodwall",
  thesis:
    "For venues, planners and agencies, files are mostly pictures: a masonry wall that looks like the project, where you drag assets into boards and share a board with a client as a page they can approve.",
};
