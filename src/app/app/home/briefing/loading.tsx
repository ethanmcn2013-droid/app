/**
 * /app/home/briefing loading boundary. Mounts the Signal Briefing
 * Assembly skeleton (loading canon pitch 10: once chrome exists, loading
 * stays inside the content region — no full-screen takeover).
 */
export { default } from "@/modules/signal/app/signal-brief-loading";
