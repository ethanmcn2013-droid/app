import { HybridWorkspace } from "@/components/hybrid/hybrid-workspace";

export const metadata = { title: "Calendar · Tasks · Signal Studio" };

export default function TasksCalendarPage() {
  return <HybridWorkspace view="calendar" />;
}
