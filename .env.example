# Required only for a real production boot. Keep secrets in the local
# environment or deployment provider; never commit populated values.
TASKS_DATABASE_URL=
TASKS_AUTH_TOKEN=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
